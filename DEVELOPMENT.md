# MEDial 개발 기준

이 문서가 저장소의 현재 상태를 말합니다. **구현됨 / 부분 구현 / 미구현**을 섞지 않습니다.
연구 배경은 [docs/research/](docs/research/), 설계 결정은
[docs/research/DECISIONS.md](docs/research/DECISIONS.md)에 있습니다.

## 이번에 정한 중심 구조

MEDial은 완성된 의료 서비스를 평가하는 도구가 아니라, core item에서 출발해 정책 조건을 바꾸며
여러 attempt를 실행하고 결과를 비교하면서 아이템을 구체화하는 **탐색적 디자인 도구**입니다.
그래서 아래 다섯 가지는 편의가 아니라 요건입니다.

1. **세계 / 관측 / 근거를 분리한다.** 엔진만 진실을 알고, 각 행위자는 자기에게 주소가 지정된
   사건만 읽습니다. MEDial도 예외가 아닙니다.
2. **기본 일과 / 개입으로 수정한 계획 / 실제 수행 이력을 분리한다.** 아무도 순간 이동하지
   않습니다.
3. **에이전트는 제안하고, 엔진이 검증한 사건만 상태를 바꾼다.**
4. **기록은 다시 쓰지 않는다.** 시도는 삽입 전용이고, 정책 편집은 언제나 새 revision입니다.
5. **모르는 것은 모른다고 적는다.** 원자료에 없는 값을 채워 넣지 않습니다.

---

# 지금 구현되어 동작하는 것 (v0.2, 엔진 `medial-sim/0.2.0`)

## 실행 방법

처음 한 번:

```bash
python -m pip install -r server/requirements-sim.txt
npm install
```

그다음부터:

```bash
./run.sh
```

서버(8010)와 개발 서버(5173)를 띄우고 <http://localhost:5173> 을 엽니다. 이미 떠 있는 쪽은
그대로 쓰므로 두 번 실행해도 세션이 두 벌 생기지 않고, `./run.sh --stop` 으로 내립니다.
Ctrl+C 는 **이 스크립트가 띄운 것만** 정리합니다. 직접 띄우려면 `python server/sim_main.py` 와
`npm run dev` 를 각각 실행하면 됩니다.

기본 실행에 API 키·GPU·모델 호출이 없습니다. 원자료가 없으면 합성 마을·합성 페르소나로 돌고,
화면 상단 배지가 어느 쪽인지 항상 표시합니다.

### 모델 키는 `server/.env` 에 있습니다

키는 **서버 프로세스의 환경변수에서만** 읽습니다. 그 값을 담아 두는 곳이 `server/.env` 이고,
git-ignored 이며 `run.sh` 가 서버를 띄우기 전에 읽어 넣습니다. 양식은 `server/.env.example`.
직접 띄울 때는 `set -a; . server/.env; set +a; python server/sim_main.py`.

**어느 모델을 쓰는가 (2026-09-11 기준).** 한 세대가 주민 12명 + 기관 담당자 리뷰 13건에 종합과
개선 후보까지 요청하므로 한 cycle이 수십 번의 호출이고, 하는 일은 열린 추론이 아니라 **근거가
정해진 추출**입니다 — 이 사건들을 읽고, 여섯 차원을 등급 매기고, 인용한 사건 id를 대라. 그래서
기본값을 각 provider의 **중간 등급**으로 둡니다.

| provider | 기본 모델 | 키 | 비고 |
|---|---|---|---|
| `google` (기본) | `gemini-3.8-flash` | `GOOGLE_API_KEY` | OpenAI 호환 엔드포인트로 붙습니다 |
| `openai` | `gpt-5.6-terra` | `OPENAI_API_KEY` | 지능과 비용의 균형 등급 |
| `anthropic` | `claude-sonnet-5` | `ANTHROPIC_API_KEY` | 이 파일에는 키가 없습니다 |

더 어려운 판단이 필요하면 `MEDIAL_LLM_MODEL` 로 `gpt-6-astra` · `claude-opus-5` ·
`gemini-3.8-flash` 상위 등급을 지정합니다. **모델 id는 코드보다 자주 바뀌므로** 기본값은 기본값일
뿐이고, health 응답은 실제로 설정된 id를 그대로 보고합니다.

**Gemini는 새 provider 코드를 쓰지 않습니다.** Google이 OpenAI 호환 엔드포인트
(`/v1beta/openai`)를 제공하고 거기서 `response_format: json_schema` 를 받으므로, 이미 있는
OpenAI 요청 형태를 그대로 보냅니다. `provider` 는 표시용 이름이고 `wire` 가 요청 형태입니다 —
둘을 다시 합치면 `GOOGLE_API_KEY` 가 **설정된 것처럼 보이면서 동작하지 않는** 상태가 되므로,
`test_each_provider_is_reachable_from_its_own_key_alone` 이 그것을 고정합니다.

| 그 밖의 변수 | 쓰임 |
|---|---|
| `MEDIAL_LLM_PROVIDER` | 비우면 있는 키로 정합니다 (anthropic → openai → google). `gemini` 는 `google` 의 별칭 |
| `MEDIAL_LLM_MODEL` · `MEDIAL_LLM_BASE_URL` · `MEDIAL_LLM_TIMEOUT_S` | 온라인 어댑터 설정 |
| `MEDIAL_SIM_PORT` · `MEDIAL_SIM_DB` · `MEDIAL_VILLAGE_PATH` · `MEDIAL_PERSONA_PATH` | 서버 설정 |

키가 있어도 **rule 어댑터를 고르면 모델을 한 번도 부르지 않습니다.** 키가 없으면 온라인
어댑터를 고를 수 없고(session 생성 400), 호출이 실패해도 규칙 결과로 대체하지 않습니다.
응답·로그·문서·health 어디에도 키가 들어가지 않습니다.

**온라인 호출은 아직 한 번도 하지 않았습니다.** 어댑터·스키마·호출 기록·실패 처리·예산 정지는
오프라인 경계 테스트로만 확인했습니다. 위 모델 id는 각 provider의 모델 목록에서 확인한 것이지
이 저장소에서 실행해 본 것이 아닙니다.

원자료가 있을 때만:

```bash
cd scripts && python -m import_village
```

`local-data/normalized/` (git-ignored)에 레지스트리와 지도 래스터를 만듭니다. 페르소나는 서버가
실행 시점에 컴파일하며 저장소에 복사하지 않습니다.

## 구현 범위

### 사건 엔진
- 결정적 순서 `(simTimeMs, priority, stableSeq)`. 같은 규칙이면 같은 로그가 나옵니다.
- `ActionProposal → 검증 → DomainEvent → 상태`. 제안은 `actorId`/`attemptId`/`requestId`/허용
  행동/관측 참조를 모두 검사받고, 실패하면 **엔진 오류**로 기록됩니다 — 주민의 거절이 아닙니다.
- 사건 payload는 필수 키뿐 아니라 **값 타입**까지 Pydantic이 검증합니다. `bool` 자리에 `int`가
  오면 거부됩니다.
- 두 시나리오 deck: `deck-p1-no-response-v1`(P1 09:30 안부 확인 무응답),
  `deck-p9-transport-v1`(P9 읍내 이동 필요 + 두 번째 요청 충돌).

### 관측 경계
- MEDial은 각 주민의 **`home_or_away` 투영**만 보유합니다. "지금 집에 있나"는 알고 "그럼 어디
  있나"는 얻지 못합니다. 이장의 장소 단위 지역 지식은 **그의 사적 기억**이고, MEDial은 그가
  선택해서 보고할 때만 `confidence: reported` 로 획득합니다.
- 동의는 `verified / assumed / unknown` 으로 기록합니다. 이 조율 시스템에 대한 동의는 원자료에
  없으므로 전부 `assumed` 이고 근거 문구가 함께 다닙니다.
- 무응답의 **세계 쪽 이유**(밭에 있어서 못 받았다)는 연구자 전용
  `world.reachability_resolved` 에만 있습니다. 연락한 쪽이 읽는 사건에는 "응답이 없었다"만
  들어갑니다.
- 화면의 'MEDial이 아는 것' 모드는 목록 필터가 아니라 **투영**입니다. 지도에서 사람이 사라지고
  "MEDial이 위치를 모르는 사람 N명" 목록이 대신 나오며, 주민 상세도 실제 일과 대신 관측과 그
  근거를 보여 줍니다.

### 판단의 경계
- `emergencyRuledOut` 을 제거했습니다. 분류는 `clinicalStatus: unconfirmed`,
  `locationStatus: unconfirmed`, `emergencyEvidence: []` 이고, "응급이 아니라고 판정했다"가
  아니라 **"판정할 근거가 없다"** 로 기록·표시합니다.
- 이 deck에 악화 신호가 없다는 것은 **세계 설정**이고, MEDial이 가진 근거와 분리되어 있습니다.

### 기록과 계보
- 시도 ID는 UUID 기반이고 저장은 **삽입 전용**입니다. 같은 ID를 다시 쓰면 덮어쓰지 않고
  `AttemptExists` 로 실패하며, 트랜잭션이 롤백되어 부분 잔존이 없습니다.
- **rerun**(같은 초기 상태에서 정책만 바꿔 처음부터)과 **fork**(커서 시점까지의 상태·기억·
  예약·사건 큐·로그 접두부를 보존하고 그 지점부터 정책 교체)를 별도 엔드포인트·별도 라벨로
  구분합니다. fork는 부모 로그 접두부와 일치하지 않으면 409로 거부합니다.
- 정책 편집은 새 revision을 만들고 **변경 이유(자유 서술)와 변경 전후 값(계산)** 을 둘 다
  저장합니다. 부모 revision은 바뀌지 않습니다.
- `commandId` 재사용은 같은 인자면 멱등, 다른 인자면 409 충돌입니다. 명령 기록과 커서 이동은
  한 트랜잭션입니다.

### 재생
- 커서는 **시각이 아니라 사건 번호**입니다. 같은 밀리초를 공유하는 사건이 10개여도 한 번에
  하나씩 넘어갑니다. 슬라이더도 seq 단위입니다.
- 커서는 DB에 있으므로 서버·브라우저를 다시 켜도 보던 자리에서 열립니다.
- 하루 종료 평가와 커서 이후 사건은 주민 상세의 **회고 보기** 뒤에 있습니다. 09:30에 하루 전체
  결과가 섞여 보이지 않습니다.

### 정책 조건 (전부 엔진이 읽습니다)
`retryCount` · `retryIntervalMin` · `quietWindowMin` · `helperContactCap` · `disclosure` ·
`escalateToInstitutionAfterMin` · `allowHeadContact` · `rideCandidateOrder` · `maxRideDetourMin`.

정책 편집기는 이 목록을 **서버에서 받아** 폼을 만듭니다. 엔진이 읽지 않는 조건은 화면에 나올 수
없고, 범위를 벗어난 값은 API가 400으로 거절합니다.

### 기관
- 직원별 busy 캘린더. `staffCount`·`shiftStartMs`·`shiftEndMs` 가 실제로 대기와 처리 시각을
  바꿉니다. 근무시간에 들어가지 않는 업무는 흡수하지 않고 거부합니다.
- `visitTravelMinutes` 는 **편도**로 통일했고 방문 1건은 `편도×2 + 체류` 를 씁니다. 예전에는
  라벨이 왕복이라 말하고 계산은 편도를 두 번 하는 불일치가 있었습니다.
- 인계는 업무 이전이며 완료가 아닙니다. 받는 쪽 시간을 따로 셉니다.

### 이동 지원 (T004)
이동 필요 → 요청 표현 → 후보에게 제안 → 수락/거절/조건부 → **좌석 예약** → 픽업 → 도착 →
귀가 → 일과 복귀까지 동작합니다.
- 원본 일과의 동승 **결과**는 baseline에서 빼고, **이동 필요**만 남겼습니다. P9가 baseline에서
  집에 있는 것은 "외출 필요 없음"이 아닙니다.
- 좌석 수는 원자료에 없습니다. 페르소나 컴파일러가 `unknown` 으로 기록하고, 실제로 쓰는 값은
  `ResourceRevision` 의 실험 가정값이며 화면과 지표에 그렇게 표시됩니다.
- 두 번째 요청이 기존 예약과 충돌하면 `transport.conflict_detected` 가 남고 다른 사람에게
  넘어갑니다. 중복 예약·순간 이동·취소 후 잔존 좌석은 예약 장부가 막습니다.
- 운전 가능 여부가 unknown인 사람은 `driving_status_unknown` 으로 거절합니다. 운전자를
  지어내지 않습니다.

### 페르소나
- 서버 측 컴파일러가 원자료 JSON을 읽어 **행동 구조 + 근거 카드**만 만듭니다. 이름·인터뷰
  인용·역할 프롬프트는 컴파일 결과에 담기지 않고 해시만 남습니다.
- 근거 카드마다 RFC 6901 JSON pointer와 **사실 / 해석 / 가정 / 모름** 구분, 신뢰도가 붙습니다.
- P10·P11 합동 인터뷰는 신뢰도 `medium`, 인터뷰가 없는 P12는 전 카드 `low` 상한입니다.
- 주민 상세에서 근거 카드를 펼쳐 판단의 출처를 볼 수 있습니다.

### 반복 설계 흐름
정책 편집 → 새 시도(rerun/fork) → 조건·결정·결과 비교 → **DesignFinding** 기록 → 그 발견으로
다음 revision·다음 시도 생성까지 저장되고 서로 연결됩니다.

비교는 자유 서술이 아니라 **계산**입니다. 정책을 필드 단위로 diff하고 deck·자원·마을·페르소나·
baseline·엔진 버전·seed·어댑터·데이터 출처를 해시로 비교해서, 정책 말고 다른 것이 다르면
`controlled: false` 와 함께 무엇이 다른지 이름을 댑니다. **입력이 다르면 통제 비교라고 쓰지
않습니다.**

### 화면 (v0.4 재설계, 15번 문서 + 1:1:1 배치)

**최상위는 세 개뿐입니다: 실험 준비 / 마을 관찰 / 결과 비교.** 12번 문서의 여섯 연구 단계는
없어지지 않았습니다 — 서버의 루프는 여전히 cycle → 리뷰 → 종합 → 개선 → 평가 → 선택 순으로
돌고, 헤더 아래 44px 진행 표시가 어디쯤인지 말합니다. 달라진 것은 그 여섯이 **누를 곳이 아니게**
된 것입니다. 읽기 전용 진행 표시이며 네 개의 액션 버튼이 아닙니다.

| 예전 탭 | 지금 위치 |
|---|---|
| setup | 실험 준비 (`screens/PrepareScreen.tsx`) |
| cycle | 마을 관찰 (`screens/ObserveScreen.tsx`) |
| reviews | 관찰 우하단 '하루/리뷰' 전환 + 결과 비교의 주민 영향 행 |
| improve | 결과 비교의 '바꾼 운영 조건'과 '다음 시도에서 바꾼 이유' |
| generations | 결과 비교 본문, 전체 표는 '전체 시도 보기' |
| field | 결과 비교에서 여는 측면 시트 (`components/FieldSheet.tsx`) |
| manual · 발견 | 결과 비교 > 추가 도구 > 직접 비교 방식 (연구 방법으로 계속 실행 가능) |
| 해시 · JSON 경로 · 미구현 목록 | 각 화면의 '기술 정보 / 근거 펼치기' |

- 버전 선택은 **select 하나**입니다. 버전마다 버튼을 만들지 않습니다. 현재 처리 중인 버전과
  현재 읽고 있는 버전이 다르면 "읽는 중 v0 · 처리 중 v1"로 짧게 구분합니다. 과거를 열어도
  실행 중인 루프는 바뀌지 않습니다.
- **기록 재생과 루프 실행은 다른 컨트롤입니다.** 지도 아래 재생 바는 저장된 로그의 커서이고,
  진행 표시의 일시정지·재개·중지가 서버 루프의 제어입니다. 재생을 멈춰도 서버는 계속 돕니다.

**실험 준비**는 질문 · 초기 운영안 · 접힌 실험 설정 · 시작 버튼 하나입니다. 초기안은
`param`이 아니라 "먼저 이장에게 물어봅니다. 응답이 없어도 다시 걸지 않습니다" 같은 자연어
2~3줄로 설명합니다. 질문은 실행 명령으로 해석되지 않으며, 정책 생성 기능이 없으므로 질문이
운영 규칙으로 변환된 것처럼 표시하지 않습니다. 실행을 막는 오류·누락·상한은 접힌 설정 안에
숨기지 않고 기본 화면에 둡니다. 초기 운영안 목록에는 **뿌리 정책만** 나옵니다 — 지난 실험이
만든 revision(`-r<hash>`)은 이름이 겹쳐 선택을 방해했습니다. revision에서 이어가려면 결과
비교에서 버전을 고르고 새 실험을 만듭니다.

**시작 버튼 하나**가 create와 start를 잇습니다(`iterationStore.startExperiment`). 네 결과를
따로 처리합니다: create 실패(남은 것 없음) / start 실패(세션은 남아 있고 **그 세션만** 재시도) /
409 이미 실행 중(중복 클릭으로 보고 진행 중 실행을 그대로 채택) / 성공. 중복 클릭은 잠금으로
막습니다. 새로고침은 start를 반복하지 않습니다.

**불러오기는 실행하지 않습니다.** 예전 bootstrap은 로그가 비어 있으면 정책 A와 B를 한 번씩
돌려서, 새 브라우저로 열기만 해도 시도 두 개가 생겼습니다. 지금 최초 상태는 조작 가능한
준비 화면이고, 기존 session 복원은 GET뿐입니다.

#### 마을 관찰: 1 : 1 : 1

15번 5장은 지도를 왼쪽에 두고 오른쪽 400px를 요청 흐름과 사건 목록으로 나눴습니다. 사용자
요청에 따라 **세 열을 같은 폭으로** 바꿨고, 브라우저에서 본 뒤 열의 **구성**을 한 번 더
바꿨습니다. 5장이 각 패널에 **담으라고 한 내용은 그대로**입니다.

| 열 | 무엇 | 파일 |
|---|---|---|
| 1 | 실제 지도 하나. 위아래를 꽉 채우고, 아래에 마을 밖 목록과 재생 바 | `VillageMap` |
| 2 | 무슨 일이 있었나 + MEDial이 그것으로 무엇을 했나. 위 '지금 일어난 일', 아래 'MEDial · 조율 현황' | `HappeningPanel`, `OrchestratorPanel` |
| 3 | 평소에는 **마을 사람들 현황판**, 한 사람을 누르면 그 사람의 상세 | `PeopleBoard`, `PersonPanel` |

**왜 이렇게 바꿨는가** — 처음 배치는 지도와 사건 목록이 1열을 나눠 썼고, 조율 현황이 2열
전체를 혼자 썼습니다. 브라우저에서 재니 1440에서 지도가 **454×303**이었습니다. 남북 1.15 km
마을을 그만한 상자에 넣으면 표식이 서로 붙습니다. 반대로 2열은 처리 중인 요청이 없을 때
762px를 거의 비운 채로 남았습니다. 지금은 지도가 **454×606**이고, 2열은 사건과 조율이
번갈아 차서 비지 않습니다.

3열은 예전 `ResidentDetail` drawer를 대체합니다. 이전 배치에서는 **항상 한 사람을 보여 주되
아무도 고르지 않았으면 정렬 순서상 첫 사람**을 골랐습니다 — 화면이 P1에 대한 것이라고
주장하지만 아무도 P1을 물어본 적이 없는 상태였습니다. 지금은 고른 사람이 없으면 **12명 전부**를
현황판으로 보여 주고, 한 사람을 누르면 그 위로 상세가 열리며 ← 로 돌아옵니다. drawer가 지키던
두 구분은 그대로입니다: 커서 시점의 사실과 회고(하루 전체)를 토글로 분리하고, 'MEDial이 아는
것' 모드에서는 세계의 진실 대신 MEDial의 관측을 보여 줍니다. **현황판도 같은 경계를 따릅니다** —
MEDial 모드에서는 실제 위치 대신 보고받은 것만 나옵니다.

현황판의 오른쪽 숫자는 **이 시점까지** 그 사람이 겪거나 전달받은 사건 수입니다. 하루 전체
합계가 아니고, 만족도 점수도 아닙니다.

3열에 **만족도 점수는 없습니다.** 리뷰는 차원별 긍정/혼합/부정/판단 불가를 내놓고, 그것을
평균 내면 실행이 측정한 적 없는 숫자가 생깁니다. **자유 대화도 없습니다** — 이 빌드에는
대화 에이전트가 없으므로 '주고받은 말'은 로그에 실제로 저장된 발화만 보여 주고, 없으면 없다고
말합니다.

하루가 끝나면(커서가 로그 끝) 2열 위쪽이 '지금 일어난 일'에서 **주민 리뷰로 바뀝니다.** 별도
화면을 찾아가지 않습니다. 칩을 누르면 어느 쪽으로든 고정할 수 있고, 고정하면 더 이상 커서를
따라가지 않습니다.

1279px 미만에서는 지도 열이 넓어지고 조율·사람이 오른쪽에 위아래로, 899px 미만에서는 한 열로
쌓입니다. 가로 스크롤로 숨기지 않습니다.

지도:
- 실제 은점마을 **북쪽 위 평면 2D**, 원자료 비율 그대로. 남북 1.15 km 세로 마을이라 좌우
  여백이 남지만 **가로로 늘리거나 지점을 옮기지 않습니다.** (1:1:1로 열이 좁아져 여백이
  줄었습니다.) 배경은 원본 시뮬레이터의 OSM 래스터이며 git-ignored 레지스트리에서 로컬
  API로만 서빙합니다 (`지도 © OpenStreetMap 기여자` 표시 유지).
- **래스터 위에 도로망과 지명을 다시 그리지 않습니다.** 래스터에 이미 도로와 길 이름이 있어서
  중복이었습니다. 도로는 지금 **읽고 있는 요청의 경로만** 그리고, 지명은 회관·항구·읍내 방향
  등 필요한 것만, 줌 레벨에 따라 표시합니다. 실명 주택 라벨은 노출하지 않습니다.
- 표식은 화면 픽셀 기준입니다: 얼굴 지름 22px, P번호 13px, 상호작용 hit area 32px, 활동
  아이콘 8~10px **한 개**, 차량 28×16px 실루엣.
- **집단은 장소·차량 동일성으로 묶습니다** (`positions.clusterPoses`). 예전의 거리 기준
  묶음은 지나가던 이웃을 한 집단으로 만들고 줌을 바꾸면 구성이 달라졌습니다. 한 anchor에
  얼굴 최대 3개 + 인원수입니다.
- **픽셀 충돌 회피는 사회적 집단과 분리했습니다** (`positions.spreadOverlaps`). 겹치는 표식만
  화면 좌표에서 밀어내고, 밀린 표식은 실제 anchor로 leader line을 남깁니다. 같은 좌표에 사는
  P10·P11은 두 표식으로 남고 한 집단이 되지 않습니다.
- 지금 읽는 요청의 관련자와 경로만 진하게, 나머지는 약하게 유지합니다.
- 마을 밖 주민은 지도 아래 **단일 접힘 목록**입니다. 좌표가 없으므로 지도 여백에 놓지 않습니다.
- 재생 바는 지도 패널 레이아웃의 **별도 행**이고 지도 위 overlay가 아닙니다. 사건 단위
  앞뒤·처음으로는 '더' 안에 둡니다.
- 도구는 확대/축소/전체 한 곳에 묶습니다. 이름·얼굴·상태·경로 토글을 상시 나열하지 않습니다.
- 주민은 **작은 구분 가능한 얼굴**입니다. id에서 결정적으로 생성한 도형이고, 범례가 "실제
  주민의 모습이 아니다"라고 밝힙니다. 이장은 같은 한 사람이며 역할 표식만 다릅니다.
- 팝오버는 **동시에 하나만** 열립니다. hover·클릭·키보드 포커스가 같은 카드를 열고, 포인터를
  카드로 옮기는 동안 닫히지 않으며, Escape/바깥 클릭으로 닫고 원래 마커로 포커스가 돌아옵니다.
  화면 네 모서리에서 clamp/flip하고 자기 anchor를 16px 여유로 피합니다. 폭 262px, 장소명 ·
  사람 선택 가로 목록 · 현재 일 한두 줄 · '더 알아보기' 하나입니다. '더 알아보기'는 세 번째
  열이 그 사람을 보게 합니다 (중첩 모달 없음). 대화 발췌는 **로그에 그 사람의 utterance가
  있을 때만** 나오고 창작하지 않습니다.
- 원자료 좌표에서 계산한 거리는 **"원자료 기준 계산"** 으로 표시하고 현장 실측이라고 쓰지
  않습니다.

가운데 MEDial 조율 현황은 **요청 하나**만 봅니다 (`selectors/requests.ts`). 묶는 기준은 사건
자신의 `correlationId`이고 화면이 추측하지 않습니다. 예전 패널은 하루 전체에서 사건 *종류*를
세어 7단계 그래프를 칠했으므로, 다른 요청이 끝나면 이 요청도 완료로 보였습니다. 판단 이유
한 줄은 `medial.decided`에서, MEDial이 알고 있던 것과 제외한 후보는 `decisionId`로 이은
DecisionRecord에서 옵니다. `world.*` 사건은 요청의 correlation id를 갖고 있어도 흐름에서
제외합니다 — 무응답의 세계 쪽 이유는 연구자 전용이고 MEDial의 상태 문장에 들어가면 안 됩니다.

지도 아래 '지금 일어난 일'은 사건을 사람이 읽을 문장으로 바꿉니다
(`selectors/story.ts`). **원 로그는 그대로 두고 화면에서만 요약합니다**: 각 줄은 자기 사건
번호를 들고 있어 클릭하면 정확히 그 지점을 재생하고, 요약에서 뺀 기술 사건은 펼쳐서 원
이름과 순서로 볼 수 있습니다. 하루가 끝나면 같은 영역에서 '리뷰'로 전환됩니다 — 별도 화면을
찾아가지 않습니다. 부담이 늘었다고 답한 사람과 소수 의견을 먼저 두고, 12명 전부 펼칠 수
있습니다.

사람 이름과 조사는 **한 곳에서** 만듭니다 (`story.personName`, `story.withParticle`). 이장은
어느 패널에서나 "이장"이고, 조사는 받침으로 고릅니다 — "이장가"가 아니라 "이장이", 그리고
P1은 '일'로 읽히므로 "P1이", P2는 '이'로 읽히므로 "P2가"입니다.

결과 비교는 "무엇이 달라졌나요?" 표 하나입니다: 바꾼 운영 조건(이전 → 이후와 이유), 요청
해결/미해결과 소요 시간(단위·분모·집계 범위 함께), 누구의 일과가 바뀌었나(총량 + 가장 오래
바뀐 사람), 서비스 이용(이용함/거절/미경험 구분), 주민 리뷰 집계, 새로 생긴 문제. **합산 만족도
점수, fake percent, 승자 트로피, 자동 채택은 없습니다.** 데이터에 없는 값은 `미수집`이고
0으로 채우지 않습니다. 정책 외 입력이 달라졌으면 "통제 비교가 아닙니다"라고 먼저 말합니다.
기본 쌍은 **루프가 실제로 이어간 최신 버전과 그 부모**입니다 — 최신을 '최고안'이라고 쓰지
않으며, 양쪽 모두 select로 바꿀 수 있습니다.

### 만족도를 어떻게 다루는가
세 가지를 절대 섞지 않습니다.
- **객관 지표** — 로그에서 계산한 대기·부담·공개 범위. 연구자 지표이고 하루 전체 합산입니다.
- **모의 경험 평가** — 그 사람이 *실제로 겪은 사건만* 근거로 하는 규칙 기반 회고. 전체 공개
  기록이나 남의 경험은 들어가지 않습니다. 별점이 없고, 근거가 없으면 `unknown` 입니다.
- **실제 사람 평가** — v0.3의 현장 검토 폼으로 입력할 수 있습니다. `source: "human"` 을 만드는
  코드 경로는 그 제출 엔드포인트 하나뿐이고, 자동 루프는 그것을 호출하지 않으며 예시 제출을
  만들지 않습니다. 사람이 실제로 내기 전에는 0건입니다.

이용량도 요청/제안/수락/실제 사용을 나누고 "이동이 필요했던 횟수"라는 분모를 함께 표시합니다.

## 검증 결과

```bash
python -m pytest server/tests/simulation -q      # 87 passed
python server/tests/simulation/test_simulation.py # 38/38 (원자료 없이 단독 실행)
npm run build                                     # tsc + vite, 통과
```

전부 합성 픽스처를 씁니다. 원자료가 있을 때만 도는 대조 테스트는 없으면 조용히 통과합니다.

빌드 산출물은 시뮬레이터 한 덩어리입니다. 라우터와 lazy 분할은 companion 데모를 함께
담고 있을 때만 필요했으므로 같이 없앴습니다.

### 브라우저에서 확인한 것 (원자료 레지스트리, 실서버)
- 원본 지도 그림 위에 도로·집·건물·밭·항구와 주민 얼굴이 표시됨
- hover 팝오버에서 같은 위치 3명(P5·P7·P8)을 각각 고르고 '더 알아보기'로 상세 열기
- 주민 상세에서 페르소나 근거 카드 7개와 JSON pointer 확인
- 'MEDial이 아는 것' 모드에서 지도의 마커가 사라지고 "위치를 모르는 사람 12명" 목록으로 대체됨
- 정책 편집기에서 조건 9개를 서버 스펙대로 렌더링, rerun 실행 → 새 시도와 변경 전후 값 기록
- 비교 화면에서 통제 비교 배너·정책 필드 diff·결정 차이·결과 차이 확인
- 발견 기록 → 그 발견으로 새 revision·새 시도 생성까지 연결 확인
- 이동 지원 시나리오에서 예약 1건, 차량 표식, 두 번째 요청의 `driver_double_booked` 충돌 확인
- 서버 재시작 후 재생 커서(사건 6)가 복원되고 구버전 기록이 덮어써지지 않음

### 아직 브라우저에서 보지 못한 것 (정직하게)

브라우저 검증을 두 번 했습니다. 지금 **확인된 것**은: 1:1:1 배치(네 폭 실측), 3열 가독성,
지도가 열을 채우는 것, 현황판↔사람 상세 전환, 지도 팝오버의 '더 알아보기'와 Escape,
이름·사유·장소 표기, create 실패 화면, '실험 시작' 3연타에 세션 1개, 새로고침에 세션 0개,
근거 장면 클릭이 커서를 옮기고 모델을 부르지 않는 것, 서버 재시작 후 session·버전·커서 복원.

**아직 보지 못한 것:**

- `needs_decision` **화면**. 2차가 지시받은 설정(두 deck · `maxGenerations=2` ·
  `maxCandidatesPerGeneration=3` · `pareto_then_stop`)으로 실행했지만 `ready_for_designer` /
  `reached_max_generations` 로 끝났습니다. `iteration/selection.py`는 **비지배 후보가 2개
  이상**일 때만 이 상태를 냅니다. 서버 검사
  `test_a_tied_pair_stops_and_offers_its_branches_on_the_parent` 는 이 상태에 실제로 도달해
  후보 2개와 선택 이유가 있는지 확인하지만, **그 화면을 사람이 본 적은 없습니다.**
- **start만 실패하는 경로와 '이 실험 실행 다시 시도'.** 서버를 내리면 create부터 실패하므로
  이 상황을 만들지 못했습니다. 세션이 늘지 않는다는 것은 서버 계약 검사로만 확인했습니다.
- 팝오버가 지도 **네 모서리**에서 잘리는지, 차량 동승 구간에 탑승자가 보이는지, 래스터 위
  도로 이중 렌더.
- 현장 검토에서 **이유를 채우면** 기록 버튼이 풀리는지 (막히는 것만 확인).

**사용자 사용성 평가는 한 번도 하지 않았습니다.** 지금까지 전부 개발자 내부 확인입니다.
남은 절차는 `docs/research/18_BROWSER_VERIFY_PROMPT.md` 에 있습니다.

### 12명 회관 집합은 원자료에 없다 (DI 확인)

15번 9장 검증 항목의 "12명 회관 집합"을 만들지 않았습니다. 원자료 레지스트리
(`local-data/normalized/village.v1.json`)의 `plan.baseline`을 12명 전부 확인한 결과,
**HALL을 방문하는 사람은 이장(P6) 한 명뿐**이고 나머지 11명의 기본 일과에 회관이 없습니다.
하루 중 3명 이상이 한 장소에 함께 있는 가장 큰 집합은 12:10의 마을식품 4명입니다.
없는 장면을 만들지 않았고, 다인 집단 렌더링(얼굴 3개 + `+N`)은 그 4명으로 확인했습니다.

### 부담 지표는 도와준 사람과 도움받은 사람을 구분하지 않는다 (미해결)

`residentBurden.addedTaskMinutes`(= `ActorRuntime.task_ms`)는 **기본 일과에서 벗어난 모든
구간**을 셉니다. 부탁을 들어준 이웃의 우회 20분과, 읍내에 가야 했던 P9 본인의 이동 180분이
같은 필드에 들어갑니다. 화면이 이것을 "가장 부담이 큰 사람"이라고 부르면 지표가 하지 않는
구분을 주장하게 되므로, **화면 문구를 고쳤습니다**: "누구의 일과가 바뀌었나 / 가장 오래 바뀐
사람"이고, 두 종류가 섞인다고 함께 적습니다.

**지표 자체는 바꾸지 않았습니다.** 정의를 바꾸는 것은 연구 계약 변경이고, 저장된 시도의 값과
비교 가능성에 영향을 줍니다. 나누는 것이 맞다면 `world.divert` 호출부에서 "부탁을 수행하는
과업"과 "본인이 수혜자인 이동"을 구분해 별도 필드로 세는 것이 최소 변경입니다.

### 이번에 브라우저에서 발견해 고친 것
회귀 테스트로 고정했습니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| 이웃 상한 0 + 인계 기한 90분 | 09:30에 미해결로 닫혀 기한이 발동할 수 없었다 | 기한이 남아 있으면 닫지 않고 대기. 기한에 인계 → 12:15 해결 |
| 이동 지원 결정 근거 | "P9에게 0회 연락했고 응답이 없다" — 있지도 않은 무응답을 근거로 적었다 | 필요의 종류에 맞는 문구로 분리 |
| 동승 | 한 번의 이동인데 운전자는 `offmap`, 동승자는 `drive` 였다 | 같은 leg·같은 mode. 차량 세그먼트가 탑승자를 기록 |
| 정책 편집기 | 선택형 조건이 빈 드롭다운으로, 5–600 범위를 잃었다 | `anyOf[int, null]` 을 풀어 타입·범위·nullable을 정확히 전달 |
| 부트스트랩 | 시도 하나를 못 읽으면 화면 전체가 죽었다 | 못 읽은 것만 건너뛰고 id를 알림 |

---

# 리뷰 기반 반복 (v0.3, 13번 문서의 I0~I4)

13번 문서의 I0~I4가 구현·검증되었습니다. I5(기관 역할 확장·119·다일)는 하지 않았고 아래
'아직 구현하지 않은 것'에 그대로 남아 있습니다.

## 실행 방법

서버·프론트는 위와 같습니다. 화면 상단 탭이 연구 루프의 순서입니다:
**1 설계 → 2 Cycle 실행 → 3 에이전트 리뷰 → 4 개선 → 5 세대 비교 → 6 현장 검토**.
기존 A/B 수동 비교와 발견 기록은 '수동 비교·발견' 탭에 그대로 있습니다.

'1 설계'에서 core item·초기 운영안·deck·세대 수·후보 수·예산·어댑터를 정하고 session을 만든
뒤 상단의 '반복 시작'을 누르면, **그 범위 안에서는 세대마다 승인을 묻지 않고** 진행합니다.

## 구현 범위

### 개인 리뷰 (AgentReview)
- 모든 주민 + 보건소 담당자에게 리뷰를 만듭니다. **미경험은 `no_experience` 이고 전 항목이
  `unknown`** 입니다. 미사용을 불만족으로 만들지 않습니다.
- 차원은 여섯 개입니다: 도움·해결 / 시간·노동 / 선택·거절 / 정보 공유 / 이해 가능성 /
  재이용 조건. '불편'은 별도 차원이 아니라 해당 차원의 `negative` 입니다.
- **`unknown` 이 아닌 항목은 반드시 자기가 겪은 사건 id를 하나 이상 인용**해야 하고, 성향
  해석은 자기 persona revision의 근거 카드만 인용할 수 있습니다. 위반은 저장 전에
  `ReviewBoundaryError` 로 거부됩니다 — rule 어댑터도 예외가 아닙니다.
- 경험 집합은 저장된 로그에서 그 actor의 `visibility` 로 투영합니다. 연구자 전용
  (`world.*`) 사건은 어떤 리뷰에도 들어갈 수 없습니다.
- 원자료가 "이 사람의 수락 조건은 확인되지 않는다"고 적어 둔 항목은 그 카드를 인용하며
  `unknown` 으로 남습니다. 채워 넣지 않습니다.
- 리뷰는 Cycle 종료 후의 회고 산출물이며 사건 로그에 주민 발화로 삽입되지 않습니다.

### 종합 (ReviewSynthesis)
- 차원별 문제 묶음에 **영향받은 사람과 같은 항목을 반대로 평가한 사람**을 함께 답니다.
- **소수 의견을 따로 표시**합니다: 반대한 사람보다 적게 제기했거나, 평가한 사람의 1/3 이하거나,
  기관 담당자 혼자 제기한 경우입니다.
- 주민 개선과 기관 부담이 반대로 움직이면 `conflicts` 로 보존합니다.
- 인과 문장은 **가설**로 표시하고 대안 설명을 함께 답니다.
- 연구자 객관 지표는 리뷰 **옆에** 두고 리뷰 안으로 합치지 않습니다.

### 개선 (ChangeProposal)
- 세계 밖의 개선 에이전트가 만들며, 화면에도 세계 안의 MEDial과 다른 역할로 표시됩니다.
- 세대당 최대 2개, **차원당 1개**입니다. 같은 불만에 다이얼 두 개를 돌리는 것은 후보 두 개가
  아닙니다.
- 후보마다 리뷰 참조 · **정확한 변경 전/후 값** · 기대 기제 · 영향받을 사람 · 예상 부작용 ·
  다음에 확인할 지표가 붙습니다.
- 기제는 그 Cycle이 **실제로 사용한 기능에만** 발동합니다. 안부 확인만 있었던 Cycle에서
  동승 우회 시간을 제안하지 않고, 아무도 도달하지 않은 상한을 낮추자고 하지 않습니다.
- 없으면 `no_supported_change` 와 이유를 반환합니다. 억지로 만들지 않습니다.

### 검증 (코드, 모델 판정 아님)
- 허용 경로: `contactStrategy` 와 `params/*` 아홉 개뿐입니다.
- **거부**: 페르소나 · 근거 카드 · 초기 기억 · deck/외생 사건 · 평가 기준 · 세계 사실 ·
  인력/차량/좌석 증원 · 지표 · 리뷰. 각 거부는 어떤 경계를 넘었는지 문장으로 말합니다.
- 변경 전 값이 현재 revision과 다르면 거부합니다(오래된 기준으로 쓴 제안).
- 타입·범위·열거값을 검사하고, **엔진이 실행을 거부할 정책**(head_first + allowHeadContact
  off)도 후보에서 뺍니다.
- 엔진에 handler가 없는 기능은 `requires_implementation` 으로 **기록만** 하고 실행하지
  않습니다. 지금 세 건이 그렇게 남습니다: 사전 가능 시간 확인, 종료 통지, 방문 전 통화 의무화.

### 세대 진행과 정지
- 상태: `created → running_cycle → collecting_reviews → synthesizing → proposing_changes →
  validating_changes → evaluating_candidates → selecting_next → …`
- 종료·차단 상태는 서로 다릅니다: `ready_for_designer` · `needs_decision` ·
  `budget_exhausted` · `no_valid_change` · `stalled` · `paused` · `failed` · `cancelled`.
  **어느 것도 '성공 완료'로 표시하지 않습니다.**
- 선택은 고정된 기준으로 계산합니다. **한쪽이 unknown인 차원은 우열의 근거로 쓰지 않고**,
  후보가 현재 세대에 지배되거나 지표가 완전히 같으면 진전이 아닙니다. 비지배 후보가 하나면
  자동 진행하고, 여럿이면 `needs_decision` 으로 멈춥니다(시작 시 우선순위를 지정했다면 그것으로
  선택하고 근거를 기록합니다).
- **마지막 세대를 자동 우승자로 만들지 않습니다.** 최종 채택은 디자이너의 `DecisionRecord`
  뿐이며, 어느 세대든 고를 수 있고 보류도 결정으로 기록됩니다.
- 선택되지 않은 후보 세대는 리뷰·지표까지 그대로 보존됩니다. 삭제하지 않습니다.

### 저장과 재시작
- `iteration_sessions` · `generations` · `agent_reviews` · `review_syntheses` ·
  `change_proposals` · `model_calls` · `model_results` · `designer_decisions` ·
  `field_packages` · `human_reviews` · `iteration_commands` 를 추가했습니다. 전부
  `CREATE TABLE IF NOT EXISTS` 이고 기존 표를 바꾸지 않으므로 **이전 빌드의 연구 DB가 그대로
  열립니다.**
- 모든 전이는 다음 전이 전에 DB에 씁니다. 각 전이는 이미 있는 산출물을 보고 건너뜁니다.
  모델 호출 결과는 (session, generation, role, subject, 입력 해시) 키로 캐시하므로 재시작이
  같은 세대를 두 번 실행하거나 같은 patch를 두 번 적용하지 않습니다.
- `commandId` 는 같은 인자면 멱등, 다른 인자면 409입니다.

### 통제 비교
- 기본 모드에서 모든 세대는 **같은 초기 하루·같은 주민 기억·같은 자원·같은 외생 사건**에서
  실행되고 정책만 다릅니다. 다음 세대가 이전 세대의 불만을 기억하지 않습니다.
- 비교 화면은 서버가 계산한 `controlled` 값을 그대로 씁니다. 정책 외 입력이 하나라도 다르면
  통제 비교라고 쓰지 않고 무엇이 다른지 이름을 댑니다.
- 세대 재생은 저장된 로그를 읽을 뿐 모델을 다시 부르지 않습니다.
- **다일(longitudinal) 모드는 이 빌드에서 실행하지 않습니다.** session 생성이 400으로
  거부하고 capability 목록에 미지원으로 나옵니다.

### 어댑터 모드 표시
- `rule` — 주민 행동·리뷰·개선 전부 규칙. 모델 호출 0.
- `hybrid` — 주민 행동은 규칙이고 리뷰/개선만 모델. 화면이 그렇게 씁니다.
  **전체 주민 판단이 LLM이라고 쓰지 않습니다.**
- `scripted` — 고정 리뷰로 흐름만 확인. 모델 결과가 아닙니다.

### 실제 LLM 어댑터
- provider 중립 HTTP 클라이언트(Anthropic Messages / OpenAI 호환 chat completions)를
  구현했습니다. 구조화 출력은 각각 tool input_schema / json_schema로 강제합니다.
- 키는 **서버 환경변수에서만** 읽습니다(`ANTHROPIC_API_KEY` 또는 `OPENAI_API_KEY`,
  `MEDIAL_LLM_MODEL`, `MEDIAL_LLM_BASE_URL`). 응답·로그·문서·health 어디에도 키가 없습니다.
- 호출마다 provider·model·prompt 버전·요청/응답 content hash·토큰·지연·검증 결과를
  기록합니다. 본문은 저장하지 않습니다.
- **키가 없으면 온라인 어댑터를 선택할 수 없습니다**(session 생성 400). 온라인 호출이 실패하면
  session이 `failed`/`model_failure` 로 멈추며 **scripted 결과로 대체하지 않습니다.** 모델 장애는
  주민의 거절이나 `unknown` 평가와 구분됩니다.
- 예산(호출 수·토큰)이 끝나면 `budget_exhausted` 로 멈추고 완료된 세대는 보존합니다.

### 디자이너 결정과 현장 검토
- 선택/보류, 이유, 성립 조건, 받아들인 trade-off, 반대 의견, 남은 질문을 저장합니다.
- 재방문용 장면 패키지를 만듭니다. 부정 평가가 많은 리뷰부터 3~5개를 고르고 기관 리뷰도
  포함합니다. 각 장면에 **모의 반응을 보여주기 전에 물을 질문**과 그 뒤에 물을 질문이 붙습니다.
- 실제 사람 응답 입력 폼이 있습니다. `source: "human"` 을 만드는 코드 경로는 이 제출
  엔드포인트 하나뿐이고, 자동 루프에서는 호출되지 않으며 예시 제출을 만들지 않습니다.
- 사람 응답은 모의 리뷰와 같은 표에 합산하지 않습니다.

### 화면에서 유지한 것
북쪽 위 2D 실제 지도, 작은 얼굴, 사람과 별도인 차량과 좌석 점, 같은 위치 팝오버(hover·키보드),
지도 밖 주민 목록, 'MEDial이 아는 것' 관측 모드, 재생 커서와 회고 보기가 그대로 동작합니다.
리뷰의 '근거 장면' 링크는 그 시도를 열고 해당 사건 번호로 커서를 옮깁니다 — 저장된 로그를
읽을 뿐이고 어댑터를 호출하지 않습니다.

v0.4에서 화면 구조가 세 개로 바뀌었지만 **위 계약은 그대로입니다**: 관측 경계, 불변 로그,
원자료 좌표, 합성/실제 구분, probe/fork, 세대 계보, `source: "human"` 을 만드는 단일 경로.
새 위치는 위 '화면 (v0.4 재설계)' 표에 있습니다.

## 이번에 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| 귀가 동승 (보고된 항목) | 귀가 leg에 탑승자가 기록되지 않아 지도의 차량이 빈 차로 보였다. 게다가 운전자와 동승자에게 **같은 Segment 객체**를 넘겨 `riders` 목록을 공유했다 | 두 사람에게 각각의 복사본을 주고 운전자 쪽에만 탑승자를 기록. `Segment.clone()` 이 가변 목록을 공유하지 않음. 회귀 테스트 2건 |
| `/events` 간헐 500 | 하나의 sqlite3 연결을 API 스레드풀과 반복 루프 스레드가 함께 쓰는데 **읽기에 잠금이 없었다** | 읽기도 같은(재진입) 잠금으로 보호. 쓰기 중 동시 읽기 회귀 테스트 |

기존 기록은 다시 쓰지 않으므로, 고치기 **전에** 저장된 시도는 여전히 빈 탑승자 목록을 갖고
있습니다. 이후 실행부터 올바릅니다.

## 검증 결과 (v0.3)

```bash
python -m pytest server/tests/simulation -q      # 139 passed
npm run build                                     # tsc + vite, 통과
```

전부 합성 픽스처를 쓰며 **모델을 호출하지 않습니다.**

리뷰 반복에 대한 테스트가 확인하는 것:
- v0→리뷰→종합→후보→v1→리뷰→v2까지 3세대가 저장되고 서로 연결된다 (좋아졌는지는 검사하지
  않는다 — 그것을 검사하면 '항상 개선된다'를 검사하는 것이 된다)
- 모든 actor가 리뷰를 받고 미경험은 전부 `unknown` 이다
- 긍정만 나오지 않는다(고정 리뷰 탐지)
- 남의 사건·남의 근거 카드·근거 없는 등급·세계 진실 사건 인용이 각각 거부된다
- 소수 의견과 반대 의견이 종합에서 살아남는다
- 주민 쪽 개선이 기관 부담 증가를 가리지 않는다
- 페르소나/근거/기억/deck/기준/세계/자원 경로 patch가 각각 거부된다
- 오래된 before 값, 범위 밖 값, 엔진이 거부할 정책 조합이 거부된다
- 미지원 기능 제안은 patch 없이 기록되고 실행되지 않는다
- unknown이 우열 판정의 근거가 되지 않는다
- trade-off는 `needs_decision` 으로 멈추고, 사전 우선순위가 있으면 그것으로 고르고 근거를 남긴다
- 필수 조건 위반 후보가 제외된다
- 재시작이 세대·리뷰·patch를 두 번 적용하지 않는다
- 예산 종료·모델 장애가 성공으로 표시되지 않고, 모델 장애가 unknown 리뷰로 둔갑하지 않는다
- 키 없는 온라인 어댑터 선택이 400이고 scripted로 강등되지 않는다
- 모델 설명에 키가 들어가지 않는다
- 세대 간 초기 하루·기억·자원·seed가 동일하다
- 다일 모드가 거부된다
- 저장된 세대를 읽어도 모델을 호출하지 않는다
- **사람이 제출하기 전에는 `source=human` 데이터가 하나도 없다**
- 사람 응답이 자기 패키지 밖 장면을 참조하면 거부된다
- 디자이너가 이전 세대를 고를 수 있고 보류도 기록된다

### 브라우저에서 확인한 것 (원자료 레지스트리, 실서버, rule 어댑터)
- 설계 화면에서 허용 수정 경로·미구현 목록·어댑터 선택(키 없어 온라인 비활성)을 확인하고
  session 생성
- '반복 시작' 한 번으로 v0 → v1 후보 2개 실행 → 자동 선택 → v2 후보 2개 실행까지 진행,
  `정체 (새 진전 없음)` 과 그 이유를 상단에 표시
- 리뷰 화면에서 actor별 카드, 미경험 회색 표시, 차원별 평가와 근거 사건·근거 카드 확인
- 리뷰의 '근거 장면' 클릭 → Cycle 화면이 그 시도로 바뀌고 커서가 사건 23으로 이동
- 개선 화면에서 문제 묶음·소수 의견·이해관계 충돌·후보의 변경 전/후 표·예상 부작용,
  '구현 필요 — 자동 실행 안 함' 3건 확인
- 세대 비교에서 통제 비교 배너와 snapshot 해시, 기준별 값, 정책 diff, 사람별 득실
  (P6 19.9분 → 0분, 기관 0분 → 25분) 확인
- 현장 검토에서 v1을 선택하고 이유·조건·trade-off·반대 의견·남은 질문 저장 → 장면 패키지
  5개 생성 → 실제 사람 응답 1건 제출(`source=human`), 제출 전에는 0건이었음
- 'MEDial이 아는 것' 모드에서 지도 마커가 사라지고 위치 미상 목록으로 대체되는 동작 유지
- 귀가 동승 구간에서 운전자 leg에 탑승자가 기록되고 동승자 leg는 비어 있음(원자료 실행)

**온라인 LLM 검증 범위: 없음.** 이 환경에 모델 키가 없어 실제 provider 호출은
**한 번도 수행하지 않았습니다.** 어댑터·스키마·호출 기록·실패 처리·예산 정지는 구현되어
있고 오프라인 경계 테스트로만 확인했습니다. 키가 있는 환경에서 online smoke test를 수행하고
호출 횟수·모드를 보고하는 것이 남아 있습니다.

---

## 검증 결과 (v0.4 화면 재설계)

`npm run build` 통과.
`python -m pytest server/tests/simulation -q` **151 passed** (기존 139 + `test_screen_contracts.py` 12건).
`npm test` (vitest) **41 passed** — selector 동작 23건 + 화면 mount 15건 + 그리드 정합성 3건.

새 검사는 화면이 서버에 요구하는 계약만 봅니다 (CSS 값을 복제하는 테스트는 만들지 않았습니다):
모든 사건이 `correlationId`를 갖는가, 하루에 두 요청이 스트림을 섞지 않는가,
`world.reachability_resolved` 가 MEDial 가시성에서 빠져 있는가, seek이 저장 커서를 옮기는가,
재시작 후 커서가 복원되는가, 같은 commandId 재전송이 커서를 두 번 옮기지 않는가, 같은
session에 두 번째 start가 409인가, create가 start하지 않는가, **GET이 루프를 시작하지
않는가**, session 목록 조회가 아무것도 만들지 않는가.

### 자동 검사가 무엇을 지키는가

`npm test`는 두 종류입니다. **정적인 CSS 값을 복제하는 테스트는 없습니다** (15번 9장 금지).

- `selectors/selectors.test.ts` — 화면이 하는 *주장*이 참인지. 두 요청이 섞이지 않는가,
  `world.*`가 흐름에서 빠지는가, 무응답의 세계 쪽 이유가 문장에 안 나오는가, 내부 타입 이름이
  상태로 출력되지 않는가, `knownFacts`가 DecisionRecord에서 오는가, 발화가 payload에 있을
  때만 인용되는가, 요약에서 뺀 사건이 전부 펼침 목록에 남는가, 이장이 어느 패널에서나 이장인가,
  조사가 받침에 맞는가.
- `screens/screens.test.tsx` — 두 화면을 **합성 마을에서 만든 실제 API payload**로 mount.
  세 열이 렌더되는가, `undefined`/`[object Object]`가 새지 않는가, 합산 점수·승자 표기가
  없는가, 부담 지표가 "둘을 구분하지 않는다"고 말하는가, 발화가 없을 때 없다고 말하는가.
  화면이 실제 데이터에서 throw하면 `tsc`는 모르고 이 저장소에는 CI 브라우저가 없으므로,
  이 mount 검사가 그 자리를 대신합니다.

fixture는 `fixtures/ui/` 이고 `scripts/gen_ui_fixtures.py` 로 다시 만듭니다. **합성 마을에서만**
생성합니다 — 원자료 파생 지명·나이·직업은 `local-data/`가 git-ignored인 이유 그대로 저장소에
들어오면 안 됩니다.

### 브라우저에서 확인한 것 (1440×900 · 1366×768, 원자료 레지스트리, 실서버)

아래는 **1:1:1 배치 이전** 상태에서 확인한 것입니다. 1:1:1 배치와 3열 사람 패널은 브라우저에서
보지 못했고, 위 mount 검사로만 확인했습니다 — 자세한 내용은 이 절 끝의 '아직 보지 못한 것'.
- **가로 넘침 0.** 1440/1366/1050/860 모두 `scrollWidth == innerWidth`. 예전 화면은 1440에서
  1448px였습니다 — 원인은 `body`의 기본 8px 여백과 `width: 100vw` 였고, index.html에 리셋을
  넣고 Root를 `width: 100%` 로 바꿨습니다.
- **A 실험 준비**: 질문 · 초기안(자연어 3줄) · 접힌 실험 설정(요약이 실제 상태에서 생성:
  "초기안 포함 최대 3개 버전 · 시나리오 2개 · 규칙 기반 리뷰 · 규칙 기반 개선 · 모델 호출
  없음") · '실험 시작' 하나만 보입니다. 미구현 목록·필드 경로·해시는 기본 화면에 없습니다.
- **시작 한 번**: '실험 시작'을 연속 3번 눌러 session이 1 → **2개**만 늘었고(중복 없음),
  루프가 끝까지 돌아 `stalled` 로 정지했습니다. 이후 새로고침에서 session 2개 · 시도 29개
  그대로 — **불러오기가 실험을 만들지 않습니다.**
- **B 관찰**: 그룹 팝오버가 열린 지도(은점마을식품에 4명 = 얼굴 3개 + `+1`, 사람 선택 가로
  목록, 현재 일, '더 알아보기', 'Esc 닫기'), 우상단 한 요청의 관측→판단→조율→결과 흐름,
  지도 아래 '마을 밖 · 3명 / P2 · TOWN / P3 · TOWN / P12 · TOWN'.
- **요청이 섞이지 않음**: 같은 하루의 `checkin-P1`과 `req-P1-1`이 별개 흐름으로 나오고
  select에서 '정기 안부 연락 · 응답 없음'과 '확인 요청 · 기관 인계'로 구분됩니다.
- **C 결과 비교**: 기본 쌍이 v0 ↔ v1(이웃 대신 본인 재연락)이고 "같은 하루, 운영 조건만 다름"
  으로 표시됩니다. 바꾼 조건 `연락 순서: 이장에게 먼저 → 본인 재연락 후 보건소`,
  `재연락 횟수: 0 → 1`, 소요 시간 `80.8 → 158.3 (+77.5분)`, 부담·이용·리뷰 집계, 그리고 v0의
  악화 항목(특정인에게 몰린 시간, 소수 의견 표시)이 함께 나옵니다. '다음 시도에서 바꾼 이유'가
  주민 인용 → 묶인 문제 → 제안 → 실제 변경 → 남은 질문으로 끊기지 않고 이어집니다.
- **현장 검토 시트**: 채택/보류/기각 선택, 이유·성립 조건·감수한 부담·반대 의견·현장 질문 입력,
  기록된 결정, 장면 5개와 사전/사후 질문, 그리고 `source = human` 라벨이 붙은 별도 입력 영역이
  한 겹의 시트에서 열립니다.

### 이번에 브라우저에서 발견해 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| MEDial 상태 문장에 `world.reachability_resolved` | 세계 사건이 요청의 correlation id를 갖고 있어서 흐름에 들어오고, 무응답의 **세계 쪽 이유**가 MEDial의 상태 줄에 노출됐다 | `requestFlows`가 `world.*` 를 제외. 기본 분기에서 내부 타입 이름을 상태로 출력하던 fallback도 제거 |
| "MEDial이 알고 있던 것 0가지" | `knownFacts`를 `medial.decided` payload에서 읽었는데 그 필드는 DecisionRecord에 있다. 매번 빈 목록이라 MEDial이 아무것도 모른 것처럼 보였다 | `decisionId`로 두 기록을 이음. 제외한 후보와 그 이유도 함께 (실제로 2가지 · 8명) |
| 요청 select에 같은 이름 두 개 | `checkin-P1`과 `req-P1-1`이 둘 다 "P1 안부 확인"이 됐다. id에서 사람을 뽑는 코드가 `checkin` 을 사람 이름으로 잘못 읽기도 했다 | 사람 토큰을 정규식으로 찾고, 접촉만 있는 묶음은 '정기 안부 연락', 인계가 있으면 '확인 요청 · 기관 인계'로 구분 |
| 기본 비교가 항상 "통제 비교 아님" | 기본 오른쪽이 index 최댓값이었는데 그건 대개 진행되지 않은 **분기**여서, 기본 화면이 부모 관계 없는 두 버전을 열고 차이를 설명할 수 없다고 정확히 말했다 | 기본을 '루프가 이어간 최신 버전 ↔ 그 부모'로. 최신을 최고안이라고 쓰지 않고 양쪽 모두 선택 가능 |
| 초기 운영안 select에 파생 revision 12개 | 지난 실험이 만든 revision이 전부 나왔고 "A · 이장 우선 확인 (수정)" 세 개가 이름까지 같았다 | `-r<hash>` 접미사를 가진 revision 제외. revision에서 이어가려면 결과 비교에서 버전을 고르고 새 실험을 만듭니다 |
| 버전 이름이 "v0 · v0 · …" | 저장된 label이 이미 `v0 · ` 로 시작하는데 index를 다시 붙였다 | `versionName()` 하나로 통일 |

### 브라우저 검증 1차 (1:1:1 배치) — 배치가 완전히 깨져 있었다

1:1:1로 바꾼 뒤 **처음으로** 브라우저에서 열어 본 결과, **1280px 이상에서 세 열이 나란히
있지 않고 한 자리에 겹쳐 있었습니다.** 1440에서 village · orchestrator · person 세 패널의
`getBoundingClientRect()`가 전부 동일한 `[393, 134, 1027, 746]` 이었고, 사람 패널이 위에
덮여 나머지 둘을 가렸습니다. 1366도 같았습니다.

원인은 `grid-template-areas` 가 **미디어쿼리 두 개에만** 있고 기본 규칙에 없었던 것입니다.
세 자식은 `grid-area: village | orchestrator | person` 으로 자기 자리를 **이름으로** 지정하는데,
그 이름을 정의한 areas가 없으면 CSS는 이름을 **line name**으로 해석합니다. implicit track이
생기고 셋이 같은 셀에 들어갑니다. 측정된 computed columns가
`103.14px 103.14px 103.14px 0px 1026.56px` (5트랙), areas는 `none` 이었습니다.

**mount 검사가 이것을 잡지 못한 이유**를 남겨 둡니다: jsdom은 레이아웃을 계산하지 않고,
세 열은 실제로 문서 안에 다 있었으므로 12건이 전부 통과했습니다. `tsc`도 볼 수 없습니다.
브라우저만 볼 수 있었습니다. 그래서 이제 `screens/layout.test.ts` 가 **CSS 값이 아니라 두
선언의 정합성**을 검사합니다 — 자식이 이름으로 자리를 잡는다면, 열을 정의하는 모든 폭이 그
이름들을 정의해야 한다. (수정을 되돌려 검사가 실패하는 것까지 확인했습니다.)

같은 라운드에서 899px 미만도 깨져 있었습니다. 쌓였을 때 조율·사람 열이 각각
`min-height: 420px / 480px` 를 요구해 이미 뷰포트를 넘으므로, `min-height: 0` 이 걸린 지도 열은
트랙 높이 **0px**을 받고도 자식들은 제 크기로 그려져 **지도가 조율 패널 위에 겹쳐 칠해졌습니다.**
지도 열에 자동 최소 높이(`min-height: auto`)를 돌려주어 자기 내용을 요구하게 했습니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| ≥1280px에서 세 열이 겹침 | 기본 규칙에 `grid-template-areas` 누락 → 자식의 area 이름이 line name으로 해석 | 기본 규칙에 areas 선언. `layout.test.ts` 가 "열을 정의하는 모든 폭은 자식이 쓰는 이름을 정의해야 한다"를 고정 |
| <899px에서 지도가 조율 패널을 덮음 | 다른 두 열의 min-height가 뷰포트를 넘어 지도 트랙이 0px, 자식은 제 크기로 그려짐 | 지도 열에 `min-height: auto` |
| Escape로 팝오버가 안 닫힘 | 핸들러가 **고정된**(`selectedCluster`) 카드만 처리했다. 카드 스스로 "누르면 고정"이라고 안내하므로 열려 있는 카드는 대개 hover로 열린 것이고, 그 경우 Escape가 아무 일도 하지 않았다 | 열려 있는 카드(`openKey`) 기준으로 닫고 원래 마커로 포커스 복귀 |
| 같은 사람을 '이장 (P6)'과 'P6'로 다르게 부름 | 종합·제안 payload의 actor id를 결과 비교가 그대로 출력했다 | `story.nameList()` 를 추가해 반대 평가자·충돌 양측·미경험자·영향받는 사람 목록이 전부 같은 이름 규칙을 쓴다 |
| favicon 404 | 콘솔에 유일하게 남던 에러 | index.html에 inline SVG 아이콘 (파일 추가 없음) |
| 콜드 스타트에 iteration API가 503 | `set_iteration_service()` 가 `set_service()` 안에서만 호출돼, `/api/sim/*` 를 한 번 때리기 전에는 반복 API 전체가 죽어 있었다. 화면은 village를 먼저 부르므로 드러나지 않았지만, **세션 수를 세려고 API를 직접 부르면 빈손**이었다 | `get_iteration_service()` 도 `get_service()` 처럼 지연 생성. 서버 기동 후 첫 요청이 `/iteration/sessions` 여도 200 |

브라우저에서 **정상임을 확인한 것**도 함께 기록합니다: 1440/1366/1050/860 모두
`scrollWidth == innerWidth`(가로 넘침 0), 1279px 전환, 지도 세로 비율과 좌우 여백(가로 왜곡
없음), 그룹 팝오버가 동시에 하나만 열리고 포인터를 카드로 옮겨도 유지되는 것, 같은 좌표의
P10·P11이 표식 두 개로 남는 것, OSM 크레딧, 사람 패널의 정직성 문구
("모의 리뷰입니다. 점수가 아니고 실제 주민의 만족도도 아닙니다", 발화가 없을 때
"없는 대화를 만들지 않습니다"), `undefined`/`NaN`/내부 타입 이름/세계 쪽 이유 유출 없음,
서버 재시작 후 session·버전 복원.

### 브라우저 검증 2차 — 배치는 통했고, 클릭이 안 되고 있었다

1차에서 고친 배치는 실측으로 확인됐습니다 (1440에서 세 열 left 20 / 492 / 964, 각 456px,
네 폭 모두 `scrollWidth == innerWidth`, 860에서 겹침 없음, 콘솔 에러 0). 그 위에서 2차가
찾아낸 것들입니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| 팝오버의 '더 알아보기'가 아무 반응이 없다 | 지도 프레임이 pan을 위해 `setPointerCapture`를 건다. **포인터가 잡히면 뒤따르는 click이 프레임으로 재타깃**되므로 프레임 *안*에 있는 버튼은 click 자체가 발생하지 않았다. 확대/축소 버튼도 같은 처지였다 | 오버레이(`data-overlay`)에서 시작한 pointerdown은 드래그를 시작하지 않는다 |
| Escape가 hover로 연 카드를 못 닫는다 | 1차 수정이 논리적으로는 맞았지만 무효였다. React가 상태 갱신을 배치하므로, Escape가 포커스를 마커로 되돌리며 부르는 focus 핸들러가 **아직 반영되지 않은** 값을 읽고 자기가 만든 닫힘을 즉시 되돌렸다 | 함수형 갱신. 코드를 읽어서는 못 찾고 브라우저에서만 드러났다 |
| 문장에 내부 코드가 샌다 | "도움 · 해결 · **mixed**", "부탁을 받았지만 이번에는 하지 못했다 (**driving_status_unknown**)", 현황판의 "자택 · **HOME:P6**", "마을 밖 · **TOWN**" | 세 곳을 한 곳씩으로: `ASSESSMENT_LABELS`(공유), `story.reasonText` / `reviewers.reason_ko`(같은 표를 양쪽에), `positions.placeLabel` |
| create 실패 화면에 "Internal Server Error" | 서버가 죽어 있으면 dev 프록시가 이 문구로 500을 준다. 한국어 두 줄 사이에 영어 원문이 그대로 놓였고, 읽는 사람이 할 수 있는 일을 말하지 않았다 | `describe()`가 전송 실패의 상투구만 걸러 "서버가 응답하지 않습니다"로 바꾼다. **서버 자신의 거절(400/409 한국어 detail)은 그대로 통과** |
| 하루가 끝나도 리뷰로 안 바뀐다 | 15번 5장은 "하루가 끝나면 같은 영역에서 리뷰로 전환"을 요구한다. 수동 칩만 있었다 | 커서가 로그 끝에 닿으면 리뷰가 기본. 칩을 누르면 고정되고 더는 커서를 따라가지 않는다 |
| favicon 404 · 콜드 스타트 503 | 1차 보고분 | 고쳐졌음을 2차가 확인 (콘솔 에러 0, 첫 요청 200) |

`reasonText` / `reason_ko`는 같은 표를 화면과 서버가 각각 갖습니다. 한국어로 이미 쓰인 사유는
그대로 통과시키고, **표에 없는 코드는 "기록된 사유 코드 X"로 표시**합니다 — 사유를 잃는 것이
코드를 보여 주는 것보다 나쁘기 때문입니다.

### 스크린샷은 저장소에 넣지 않습니다

`docs/research/screenshots/` 를 gitignore에 넣었습니다. 실서버 스크린샷에는 주민의 일과·장소·
현재 상태가 그대로 담기고, 그것이 `local-data/` 를 제외하는 이유와 같습니다. 파일은 로컬에
남고 필요하면 직접 전달합니다.

### 1:1:1 작업 중 검사로 발견해 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| needs_decision인데 고를 것이 없다 | 갈라진 후보는 **부모 세대**의 proposal인데 화면은 오른쪽 열의 세대에서 읽었다. 기본 쌍은 자식이 오른쪽이므로 항상 빈 목록 → 결정이 필요한 상태에서 다음 행동이 아무것도 안 보였다 (15번 9장 D 위반) | 후보를 session 전체에서 찾는다(`decisionPoint`). 그래도 없으면 상태와 갈 곳을 말하는 안내를 띄운다. 회귀 검사 2건 |
| "이장가 하겠다고 했습니다" | 조사를 문자열에 그대로 붙였다 | `withParticle()`이 받침으로 고른다. 끝자리 숫자는 읽는 소리 기준 — P1은 '일'이라 "P1이", P2는 '이'라 "P2가" |
| 같은 사람이 패널마다 이장/P6 | 패널마다 `=== 'P6' ? '이장'` 을 따로 갖고 있었다 | `story.personName()` 하나로 통일하고 전 패널이 그것을 쓴다 |
| "상대가 수락했습니다" | 수락한 사람은 사건의 `actorId`인데 `toActorId`를 읽었다. 응답 사건에는 그 필드가 없어 항상 "상대" | 응답자는 actor에서 읽는다 |
| UI fixture에 원자료가 들어갈 뻔함 | mount 검사용 fixture를 실행 중인 서버(원자료 레지스트리)에서 curl로 받았다 | 합성 마을에서만 생성하도록 `scripts/gen_ui_fixtures.py` 로 교체 |

## importer가 남긴 데이터 이슈

6건 중 2건 해결, 4건 미해결입니다. 화면 하단 패널에 상태와 함께 나옵니다.

### DI-001 해결 — 마을의 긴 축은 남북이다
방위를 서술하지 않고 측정합니다. P9 자택→미가식당 5.3°(정북) 1111 m, 동서 381 m · 남북
1153 m. 원본 시뮬레이터 산문과 일치하며, 오류는 후속 분석 문서 두 곳에 한정되어 정정했습니다.

### DI-007 해결 — 두 거리는 둘 다 맞고, 섞지만 않으면 된다
간선 350개 전부 원본 폴리라인의 연속 정점 쌍임을 import 시점에 검증하고, 아니면 실패시킵니다.
구간 거리는 `sourceM`(원본이 그린 경로)과 `graphM`(전체 도로망 최단 경로)을 둘 다 보관하며,
이동 사건이 `distanceBasis` 로 어느 쪽인지 밝힙니다.

남은 4건(DI-002 overlay 유지, DI-003 지름길 미이식, DI-004 읍내 30분 고정값, DI-006 조업 구역)은
원자료나 기관 확인이 있어야 닫히므로 `open` 입니다.

---

# 부분 구현

- **기관 역할** — 담당자 1명의 일정·근무시간·대기열은 실제로 동작합니다. **보건소장, 여러 건을
  동시에 안고 있는 실무자의 자원 배분, 다건 우선순위**는 없습니다.
- **LLM 어댑터** — 리뷰·종합·개선 역할은 실제 provider를 호출하도록 구현했고 키가 없으면
  선택할 수 없습니다. **주민의 행동 어댑터는 여전히 규칙뿐입니다** (`agents/llm.py` 는
  프롬프트 페이로드만 있고 모델을 부르지 않습니다). 그래서 온라인 모드의 정확한 이름은
  hybrid입니다. 이 환경에서는 키가 없어 실제 호출을 검증하지 못했습니다.
- **읽기 전용 주민 대화** — 규칙 기반 발화만 있습니다. 자유 대화 UI는 없습니다.

# 아직 구현하지 않은 것

이번에 하지 않은 것을 완료라고 쓰지 않습니다.

- **119** — 접수/출동/도착/인계를 분리해 모델링해야 하고, 도착 자체를 의료 해결로 세지 않아야
  합니다. 지금은 아예 없습니다. 실제 기관 전송도 물론 없습니다.
- **다일 기억** — 하루 단위 실행만 있습니다. `longitudinal` 모드는 이름만 있고 session
  생성이 400으로 거부합니다.
- **개입 대화 분기** — 읽기 전용 probe와 세계를 바꾸는 개입 대화의 구분이 설계에만 있습니다.
- **보건소장·다건 우선순위** — 담당자 1명의 일정만 있습니다. 소장의 배정과 실무자의 다건
  큐, 그 역할의 리뷰는 없습니다 (13번 문서의 I5).
- **OpenAPI → TS 타입 생성** — `src/features/simulation/api/types.ts` 는 손으로 씁니다.
- **WebSocket 스트리밍**, **원본 replay 모드**, **공개용 export 프로필**.

## 개발 기록 운영

- 설계 결정은 [DECISIONS.md](docs/research/DECISIONS.md)에 ID를 붙여 남깁니다. 대안과 아직
  확인하지 못한 가정을 함께 적습니다.
- 남은 작업은 [07_BACKLOG.md](docs/research/07_BACKLOG.md).
- 원자료는 저장소에 들어오지 않습니다. `local-data/` 와 `local-archive/` 는 git-ignored입니다.
- **기존 companion 데모는 저장소에서 제거했습니다** (2026-09-11, 사용자 요청). 음성·triage·
  RAG·아바타 서버(`server/app/{api,modules,prompts,session,ws}`, `main.py`, `config.py`),
  프런트(`src/{App.tsx,components,screens,store,styles,hooks,config,data,types}`),
  `requirements.txt`, `GPU_SETUP.md`, Pretendard 폰트와 아바타 이미지, 관련 문서가 대상입니다.
  전부 커밋 `dafb5e6` 에 들어 있으므로 `git show dafb5e6:<경로>` 로 되살릴 수 있습니다.
  `.env` 도 그 데모용 설정(STT·TTS·RAG·wav2lip)이었고, **`GOOGLE_API_KEY` 와
  `OPENAI_API_KEY` 값만 남기고** 나머지는 지웠습니다.

## 읽는 순서

1. [01_RESEARCH_POSITION.md](docs/research/01_RESEARCH_POSITION.md) — 왜 이 도구인가
2. [02_WORLD_AND_AGENTS.md](docs/research/02_WORLD_AND_AGENTS.md) — 세계·계획 계층
3. [03_ORCHESTRATION_AND_ROLES.md](docs/research/03_ORCHESTRATION_AND_ROLES.md) — 역할과 권한
4. [04_SCENARIOS_AND_ISSUES.md](docs/research/04_SCENARIOS_AND_ISSUES.md) — 시나리오
5. [05_WORKFLOW_AND_EVALUATION.md](docs/research/05_WORKFLOW_AND_EVALUATION.md) — 평가의 분리
6. [06_IMPLEMENTATION.md](docs/research/06_IMPLEMENTATION.md) — 구현 방침
7. [07_BACKLOG.md](docs/research/07_BACKLOG.md) — 남은 일

## 추가 연구·제품 제안 (2026-09-10, 아직 미구현)
- [추가 HCI·정책 시뮬레이션 레퍼런스 14건](docs/research/09_DESIGN_POLICY_REFERENCES.md)
- [디자이너·정책 담당자용 서비스 운영 설계 작업대](docs/research/10_DESIGN_POLICY_STUDIO.md)
- [후속 모델·구현 순서·사용자 연구 계획](docs/research/11_STUDIO_IMPLEMENTATION_AND_STUDY.md)

현재 구현 보고와 별개인 후속 제안입니다. 서비스 기제 편집, 가정별 실행 행렬, 실제 이해관계자의 정정이 핵심입니다.
## 사용자 확정 연구 방향: 리뷰 기반 반복
기획 문서는 다음 셋입니다. 아래 v0.3 절이 그 중 **무엇이 실제로 구현되었는지**를 말합니다.
- [최종 연구·제품 기획](docs/research/12_FINAL_RESEARCH_PLAN.md)
- [주민 리뷰→MEDial 개선→다음 Cycle 구현 지시서](docs/research/13_ITERATION_IMPLEMENTATION_SPEC.md)
- [수정용 LLM 전달 프롬프트와 내부 역할 템플릿](docs/research/14_CODING_LLM_PROMPT.md)

핵심: 초기 인터뷰→개인 에이전트→마을 MAS→Cycle→개인 리뷰→운영안 개선→여러 세대 반복→디자이너 선택→현장 재방문·정정.
이 목표가 이전 09~11의 기능 확장 순서보다 우선합니다. 현재 엔진·지도·정책·비교·발견 기능을 재사용했습니다.

## 2026-09-10 UI 정리 및 다음 재설계

사용자 요청에 따라 IterationSetup의 질문/설정 계층과 글자·여백을 정리하고, 관찰 화면에서 수동 실험 도구 묶음을 분리했습니다. 관찰 시점 선택은 유지했습니다. `npm run build` 통과. 브라우저 연결 실패로 이 변경의 시각 검증은 미완료입니다.

전체 화면 재설계는 아직 구현 전입니다. 다음 작업의 화면 기준은 [15_DESIGN_RESET_SPEC.md](docs/research/15_DESIGN_RESET_SPEC.md), 실행 프롬프트는 [16_DESIGN_LLM_PROMPT.md](docs/research/16_DESIGN_LLM_PROMPT.md)입니다. 기존 연구/데이터 계약을 유지하면서 최상위 화면을 실험 준비·마을 관찰·결과 비교로 통합합니다.
