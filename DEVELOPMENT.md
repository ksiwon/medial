# MEDial 개발 기준

이 문서가 저장소의 현재 상태를 말합니다. **구현됨 / 부분 구현 / 미구현**을 섞지 않습니다.
연구 배경은 [docs/research/](docs/research/), 설계 결정은
[docs/research/DECISIONS.md](docs/research/DECISIONS.md)에 있습니다.

## 이번에 정한 중심 구조

MEDial은 완성된 의료 서비스를 평가하는 도구가 아니라, core item에서 출발해 정책 조건을 바꾸며
여러 attempt를 실행하고 결과를 비교하면서 아이템을 구체화하는 **탐색적 디자인 도구**입니다.
그래서 아래 다섯 가지는 편의가 아니라 요건입니다.

1. **세계 / 관측 / 근거를 분리한다.** 엔진만 진실을 알고, 각 행위자는 자기에게 주소가 지정된
   사건만 읽습니다. MEDial도 예외가 아닙니다.
2. **기본 일과 / 개입으로 수정한 계획 / 실제 수행 이력을 분리한다.** 아무도 순간 이동하지
   않습니다.
3. **에이전트는 제안하고, 엔진이 검증한 사건만 상태를 바꾼다.**
4. **기록은 다시 쓰지 않는다.** 시도는 삽입 전용이고, 정책 편집은 언제나 새 revision입니다.
5. **모르는 것은 모른다고 적는다.** 원자료에 없는 값을 채워 넣지 않습니다.

---

# 지금 구현되어 동작하는 것 (v0.2, 엔진 `medial-sim/0.2.0`)

## 실행 방법

처음 한 번:

```bash
python -m pip install -r server/requirements-sim.txt
npm install
```

그다음부터:

```bash
./run.sh
```

서버(8010)와 개발 서버(5173)를 띄우고 <http://localhost:5173> 을 엽니다. 이미 떠 있는 쪽은
그대로 쓰므로 두 번 실행해도 세션이 두 벌 생기지 않고, `./run.sh --stop` 으로 내립니다.
Ctrl+C 는 **이 스크립트가 띄운 것만** 정리합니다. 직접 띄우려면 `python server/sim_main.py` 와
`npm run dev` 를 각각 실행하면 됩니다.

`run.sh`는 Git Bash뿐 아니라 Windows의 Docker Desktop/WSL 호환 `bash.exe`에서도 동작합니다.
그 환경의 Python에 서버 의존성이 없으면 의존성이 설치된 Windows Python을 PowerShell 경유로
사용하고, 포트 감지·상태 확인·종료도 Windows 프로세스를 기준으로 수행합니다. 특정 Python을
강제하려면 `MEDIAL_PYTHON`을 지정합니다. CRLF 형식의 `server/.env`는 파일을 고치지 않고 읽습니다.

기본 실행에 API 키·GPU·모델 호출이 없습니다. 원자료가 없으면 합성 마을·합성 페르소나로 돌고,
화면 상단 배지가 어느 쪽인지 항상 표시합니다.

### 모델 키는 `server/.env` 에 있습니다

키는 **서버 프로세스의 환경변수에서만** 읽습니다. 그 값을 담아 두는 곳이 `server/.env` 이고,
git-ignored 이며 `run.sh` 가 서버를 띄우기 전에 읽어 넣습니다. 양식은 `server/.env.example`.
직접 띄울 때는 `set -a; . server/.env; set +a; python server/sim_main.py`.

**공급자는 Gemini 하나입니다 (2026-09-15).** 전에는 Anthropic·OpenAI 경로도 있었지만 헷갈리기만
해서 걷어냈습니다. 다시 필요해지면 그때 새로 붙입니다 (`iteration/llm.py` 의 `_post` 하나가
요청 형태입니다). Gemini는 OpenAI 호환 엔드포인트(`/v1beta/openai`)로 붙고 거기서
`response_format: json_schema` 를 받습니다.

**어느 모델을 쓰는가.** 리뷰·개선은 한 세대가 주민 12명 + 기관 담당자 리뷰 13건에 종합과
개선 후보까지 요청하므로 한 cycle이 수십 번의 호출이고, 하는 일은 열린 추론이 아니라 **근거가
정해진 추출**입니다 — 이 사건들을 읽고, 여섯 차원을 등급 매기고, 인용한 사건 id를 대라. 그래서
`gemini-3.8-flash` 를 기본으로 둡니다. 마을을 모델로 돌릴 때는 두 층입니다 (아래 3단계).

| 변수 | 기본값 | 쓰임 |
|---|---|---|
| `GOOGLE_API_KEY` | — | 유일한 키. 없으면 온라인 어댑터를 고를 수 없습니다 |
| `MEDIAL_LLM_MODEL` | `gemini-3.8-flash` | 리뷰·종합·개선 |
| `MEDIAL_LLM_HEAD_MODEL` · `MEDIAL_LLM_RESIDENT_MODEL` | `gemini-3.8-flash` · `gemini-3.1-flash-lite` | MEDial 머리 · 주민 |
| `MEDIAL_LLM_BASE_URL` · `MEDIAL_LLM_TIMEOUT_S` · `MEDIAL_LLM_TEMPERATURE` · `MEDIAL_LLM_MAX_RETRIES` | Gemini 엔드포인트 · 60s · 0.2 · 6 | |

**모델 id는 코드보다 자주 바뀌므로** 기본값은 기본값일 뿐이고, health 응답은 실제로 설정된 id를
그대로 보고합니다. llm 실행 전에 공급자의 모델 목록으로 두 id를 확인하고 없으면 시작하지 않습니다.
| `MEDIAL_SIM_PORT` · `MEDIAL_SIM_DB` · `MEDIAL_VILLAGE_PATH` · `MEDIAL_PERSONA_PATH` | 서버 설정 |

키가 있어도 **rule 어댑터를 고르면 모델을 한 번도 부르지 않습니다.** 키가 없으면 온라인
어댑터를 고를 수 없고(session 생성 400), 호출이 실패해도 규칙 결과로 대체하지 않습니다.
응답·로그·문서·health 어디에도 키가 들어가지 않습니다.

**온라인 호출은 아직 한 번도 하지 않았습니다.** 어댑터·스키마·호출 기록·실패 처리·예산 정지는
오프라인 경계 테스트로만 확인했습니다. 위 모델 id는 각 provider의 모델 목록에서 확인한 것이지
이 저장소에서 실행해 본 것이 아닙니다.

원자료가 있을 때만:

```bash
cd scripts && python -m import_village
```

`local-data/normalized/` (git-ignored)에 레지스트리와 지도 래스터를 만듭니다. 페르소나는 서버가
실행 시점에 컴파일하며 저장소에 복사하지 않습니다.

## 구현 범위

### 사건 엔진
- 결정적 순서 `(simTimeMs, priority, stableSeq)`. 같은 규칙이면 같은 로그가 나옵니다.
- `ActionProposal → 검증 → DomainEvent → 상태`. 제안은 `actorId`/`attemptId`/`requestId`/허용
  행동/관측 참조를 모두 검사받고, 실패하면 **엔진 오류**로 기록됩니다 — 주민의 거절이 아닙니다.
- 사건 payload는 필수 키뿐 아니라 **값 타입**까지 Pydantic이 검증합니다. `bool` 자리에 `int`가
  오면 거부됩니다.
- 두 시나리오 deck: `deck-p1-no-response-v1`(P1 09:30 안부 확인 무응답),
  `deck-p9-transport-v1`(P9 읍내 이동 필요 + 두 번째 요청 충돌).

### 관측 경계
- MEDial은 각 주민의 **`home_or_away` 투영**만 보유합니다. "지금 집에 있나"는 알고 "그럼 어디
  있나"는 얻지 못합니다. 이장의 장소 단위 지역 지식은 **그의 사적 기억**이고, MEDial은 그가
  선택해서 보고할 때만 `confidence: reported` 로 획득합니다.
- 동의는 `verified / assumed / unknown` 으로 기록합니다. 이 조율 시스템에 대한 동의는 원자료에
  없으므로 전부 `assumed` 이고 근거 문구가 함께 다닙니다.
- 무응답의 **세계 쪽 이유**(밭에 있어서 못 받았다)는 연구자 전용
  `world.reachability_resolved` 에만 있습니다. 연락한 쪽이 읽는 사건에는 "응답이 없었다"만
  들어갑니다.
- 화면의 'MEDial이 아는 것' 모드는 목록 필터가 아니라 **투영**입니다. 지도에서 사람이 사라지고
  "MEDial이 위치를 모르는 사람 N명" 목록이 대신 나오며, 주민 상세도 실제 일과 대신 관측과 그
  근거를 보여 줍니다.

### 판단의 경계
- `emergencyRuledOut` 을 제거했습니다. 분류는 `clinicalStatus: unconfirmed`,
  `locationStatus: unconfirmed`, `emergencyEvidence: []` 이고, "응급이 아니라고 판정했다"가
  아니라 **"판정할 근거가 없다"** 로 기록·표시합니다.
- 이 deck에 악화 신호가 없다는 것은 **세계 설정**이고, MEDial이 가진 근거와 분리되어 있습니다.

### 기록과 계보
- 시도 ID는 UUID 기반이고 저장은 **삽입 전용**입니다. 같은 ID를 다시 쓰면 덮어쓰지 않고
  `AttemptExists` 로 실패하며, 트랜잭션이 롤백되어 부분 잔존이 없습니다.
- **rerun**(같은 초기 상태에서 정책만 바꿔 처음부터)과 **fork**(커서 시점까지의 상태·기억·
  예약·사건 큐·로그 접두부를 보존하고 그 지점부터 정책 교체)를 별도 엔드포인트·별도 라벨로
  구분합니다. fork는 부모 로그 접두부와 일치하지 않으면 409로 거부합니다.
- 정책 편집은 새 revision을 만들고 **변경 이유(자유 서술)와 변경 전후 값(계산)** 을 둘 다
  저장합니다. 부모 revision은 바뀌지 않습니다.
- `commandId` 재사용은 같은 인자면 멱등, 다른 인자면 409 충돌입니다. 명령 기록과 커서 이동은
  한 트랜잭션입니다.

### 재생
- 커서는 **시각이 아니라 사건 번호**입니다. 같은 밀리초를 공유하는 사건이 10개여도 한 번에
  하나씩 넘어갑니다. 슬라이더도 seq 단위입니다.
- 커서는 DB에 있으므로 서버·브라우저를 다시 켜도 보던 자리에서 열립니다.
- 하루 종료 평가와 커서 이후 사건은 주민 상세의 **회고 보기** 뒤에 있습니다. 09:30에 하루 전체
  결과가 섞여 보이지 않습니다.

### 정책 조건 (전부 엔진이 읽습니다)
`retryCount` · `retryIntervalMin` · `quietWindowMin` · `helperContactCap` · `disclosure` ·
`escalateToInstitutionAfterMin` · `allowHeadContact` · `rideCandidateOrder` · `maxRideDetourMin`.

정책 편집기는 이 목록을 **서버에서 받아** 폼을 만듭니다. 엔진이 읽지 않는 조건은 화면에 나올 수
없고, 범위를 벗어난 값은 API가 400으로 거절합니다.

### 기관
- 직원별 busy 캘린더. `staffCount`·`shiftStartMs`·`shiftEndMs` 가 실제로 대기와 처리 시각을
  바꿉니다. 근무시간에 들어가지 않는 업무는 흡수하지 않고 거부합니다.
- `visitTravelMinutes` 는 **편도**로 통일했고 방문 1건은 `편도×2 + 체류` 를 씁니다. 예전에는
  라벨이 왕복이라 말하고 계산은 편도를 두 번 하는 불일치가 있었습니다.
- 인계는 업무 이전이며 완료가 아닙니다. 받는 쪽 시간을 따로 셉니다.

### 이동 지원 (T004)
이동 필요 → 요청 표현 → 후보에게 제안 → 수락/거절/조건부 → **좌석 예약** → 픽업 → 도착 →
귀가 → 일과 복귀까지 동작합니다.
- 원본 일과의 동승 **결과**는 baseline에서 빼고, **이동 필요**만 남겼습니다. P9가 baseline에서
  집에 있는 것은 "외출 필요 없음"이 아닙니다.
- 좌석 수는 원자료에 없습니다. 페르소나 컴파일러가 `unknown` 으로 기록하고, 실제로 쓰는 값은
  `ResourceRevision` 의 실험 가정값이며 화면과 지표에 그렇게 표시됩니다.
- 두 번째 요청이 기존 예약과 충돌하면 `transport.conflict_detected` 가 남고 다른 사람에게
  넘어갑니다. 중복 예약·순간 이동·취소 후 잔존 좌석은 예약 장부가 막습니다.
- 운전 가능 여부가 unknown인 사람은 `driving_status_unknown` 으로 거절합니다. 운전자를
  지어내지 않습니다.

### 페르소나
- 서버 측 컴파일러가 원자료 JSON을 읽어 **행동 구조 + 근거 카드**만 만듭니다. 이름·인터뷰
  인용·역할 프롬프트는 컴파일 결과에 담기지 않고 해시만 남습니다.
- 근거 카드마다 RFC 6901 JSON pointer와 **사실 / 해석 / 가정 / 모름** 구분, 신뢰도가 붙습니다.
- P10·P11 합동 인터뷰는 신뢰도 `medium`, 인터뷰가 없는 P12는 전 카드 `low` 상한입니다.
- 주민 상세에서 근거 카드를 펼쳐 판단의 출처를 볼 수 있습니다.

### 반복 설계 흐름
정책 편집 → 새 시도(rerun/fork) → 조건·결정·결과 비교 → **DesignFinding** 기록 → 그 발견으로
다음 revision·다음 시도 생성까지 저장되고 서로 연결됩니다.

비교는 자유 서술이 아니라 **계산**입니다. 정책을 필드 단위로 diff하고 deck·자원·마을·페르소나·
baseline·엔진 버전·seed·어댑터·데이터 출처를 해시로 비교해서, 정책 말고 다른 것이 다르면
`controlled: false` 와 함께 무엇이 다른지 이름을 댑니다. **입력이 다르면 통제 비교라고 쓰지
않습니다.**

### 화면 (v0.4 재설계, 15번 문서 + 1:1:1 배치)

**최상위는 세 개뿐입니다: 실험 준비 / 마을 관찰 / 결과 비교.** 12번 문서의 여섯 연구 단계는
없어지지 않았습니다 — 서버의 루프는 여전히 cycle → 리뷰 → 종합 → 개선 → 평가 → 선택 순으로
돌고, 헤더 아래 44px 진행 표시가 어디쯤인지 말합니다. 달라진 것은 그 여섯이 **누를 곳이 아니게**
된 것입니다. 읽기 전용 진행 표시이며 네 개의 액션 버튼이 아닙니다.

| 예전 탭 | 지금 위치 |
|---|---|
| setup | 실험 준비 (`screens/PrepareScreen.tsx`) |
| cycle | 마을 관찰 (`screens/ObserveScreen.tsx`) |
| reviews | 관찰 우하단 '하루/리뷰' 전환 + 결과 비교의 주민 영향 행 |
| improve | 결과 비교의 '바꾼 운영 조건'과 '다음 시도에서 바꾼 이유' |
| generations | 결과 비교 본문, 전체 표는 '전체 시도 보기' |
| field | 결과 비교에서 여는 측면 시트 (`components/FieldSheet.tsx`) |
| manual · 발견 | 결과 비교 > 추가 도구 > 직접 비교 방식 (연구 방법으로 계속 실행 가능) |
| 해시 · JSON 경로 · 미구현 목록 | 각 화면의 '기술 정보 / 근거 펼치기' |

- 버전 선택은 **select 하나**입니다. 버전마다 버튼을 만들지 않습니다. 현재 처리 중인 버전과
  현재 읽고 있는 버전이 다르면 "읽는 중 v0 · 처리 중 v1"로 짧게 구분합니다. 과거를 열어도
  실행 중인 루프는 바뀌지 않습니다.
- **기록 재생과 루프 실행은 다른 컨트롤입니다.** 지도 아래 재생 바는 저장된 로그의 커서이고,
  진행 표시의 일시정지·재개·중지가 서버 루프의 제어입니다. 재생을 멈춰도 서버는 계속 돕니다.

**실험 준비**는 질문 · 초기 운영안 · 접힌 실험 설정 · 시작 버튼 하나입니다. 초기안은
`param`이 아니라 "먼저 이장에게 물어봅니다. 응답이 없어도 다시 걸지 않습니다" 같은 자연어
2~3줄로 설명합니다. 질문은 실행 명령으로 해석되지 않으며, 정책 생성 기능이 없으므로 질문이
운영 규칙으로 변환된 것처럼 표시하지 않습니다. 실행을 막는 오류·누락·상한은 접힌 설정 안에
숨기지 않고 기본 화면에 둡니다. 초기 운영안 목록에는 **뿌리 정책만** 나옵니다 — 지난 실험이
만든 revision(`-r<hash>`)은 이름이 겹쳐 선택을 방해했습니다. revision에서 이어가려면 결과
비교에서 버전을 고르고 새 실험을 만듭니다.

**시작 버튼 하나**가 create와 start를 잇습니다(`iterationStore.startExperiment`). 네 결과를
따로 처리합니다: create 실패(남은 것 없음) / start 실패(세션은 남아 있고 **그 세션만** 재시도) /
409 이미 실행 중(중복 클릭으로 보고 진행 중 실행을 그대로 채택) / 성공. 중복 클릭은 잠금으로
막습니다. 새로고침은 start를 반복하지 않습니다.

**불러오기는 실행하지 않습니다.** 예전 bootstrap은 로그가 비어 있으면 정책 A와 B를 한 번씩
돌려서, 새 브라우저로 열기만 해도 시도 두 개가 생겼습니다. 지금 최초 상태는 조작 가능한
준비 화면이고, 기존 session 복원은 GET뿐입니다.

#### 마을 관찰: 1 : 1 : 1

15번 5장은 지도를 왼쪽에 두고 오른쪽 400px를 요청 흐름과 사건 목록으로 나눴습니다. 사용자
요청에 따라 **세 열을 같은 폭으로** 바꿨고, 브라우저에서 본 뒤 열의 **구성**을 한 번 더
바꿨습니다. 5장이 각 패널에 **담으라고 한 내용은 그대로**입니다.

| 열 | 무엇 | 파일 |
|---|---|---|
| 1 | 실제 지도 하나. 위아래를 꽉 채우고, 아래에 마을 밖 목록과 재생 바 | `VillageMap` |
| 2 | 무슨 일이 있었나 + MEDial이 그것으로 무엇을 했나. 위 '지금 일어난 일', 아래 'MEDial · 조율 현황' | `HappeningPanel`, `OrchestratorPanel` |
| 3 | 평소에는 **마을 사람들 현황판**, 한 사람을 누르면 그 사람의 상세 | `PeopleBoard`, `PersonPanel` |

**왜 이렇게 바꿨는가** — 처음 배치는 지도와 사건 목록이 1열을 나눠 썼고, 조율 현황이 2열
전체를 혼자 썼습니다. 브라우저에서 재니 1440에서 지도가 **454×303**이었습니다. 남북 1.15 km
마을을 그만한 상자에 넣으면 표식이 서로 붙습니다. 반대로 2열은 처리 중인 요청이 없을 때
762px를 거의 비운 채로 남았습니다. 지금은 지도가 **454×606**이고, 2열은 사건과 조율이
번갈아 차서 비지 않습니다.

3열은 예전 `ResidentDetail` drawer를 대체합니다. 이전 배치에서는 **항상 한 사람을 보여 주되
아무도 고르지 않았으면 정렬 순서상 첫 사람**을 골랐습니다 — 화면이 P1에 대한 것이라고
주장하지만 아무도 P1을 물어본 적이 없는 상태였습니다. 지금은 고른 사람이 없으면 **12명 전부**를
현황판으로 보여 주고, 한 사람을 누르면 그 위로 상세가 열리며 ← 로 돌아옵니다. drawer가 지키던
두 구분은 그대로입니다: 커서 시점의 사실과 회고(하루 전체)를 토글로 분리하고, 'MEDial이 아는
것' 모드에서는 세계의 진실 대신 MEDial의 관측을 보여 줍니다. **현황판도 같은 경계를 따릅니다** —
MEDial 모드에서는 실제 위치 대신 보고받은 것만 나옵니다.

현황판의 오른쪽 숫자는 **이 시점까지** 그 사람이 겪거나 전달받은 사건 수입니다. 하루 전체
합계가 아니고, 만족도 점수도 아닙니다.

3열에 **만족도 점수는 없습니다.** 리뷰는 차원별 긍정/혼합/부정/판단 불가를 내놓고, 그것을
평균 내면 실행이 측정한 적 없는 숫자가 생깁니다. **자유 대화도 없습니다** — 이 빌드에는
대화 에이전트가 없으므로 '주고받은 말'은 로그에 실제로 저장된 발화만 보여 주고, 없으면 없다고
말합니다.

하루가 끝나면(커서가 로그 끝) 2열 위쪽이 '지금 일어난 일'에서 **주민 리뷰로 바뀝니다.** 별도
화면을 찾아가지 않습니다. 칩을 누르면 어느 쪽으로든 고정할 수 있고, 고정하면 더 이상 커서를
따라가지 않습니다.

1279px 미만에서는 지도 열이 넓어지고 조율·사람이 오른쪽에 위아래로, 899px 미만에서는 한 열로
쌓입니다. 가로 스크롤로 숨기지 않습니다.

지도:
- 실제 은점마을 **북쪽 위 평면 2D**, 원자료 비율 그대로. 남북 1.15 km 세로 마을이라 좌우
  여백이 남지만 **가로로 늘리거나 지점을 옮기지 않습니다.** (1:1:1로 열이 좁아져 여백이
  줄었습니다.) 배경은 원본 시뮬레이터의 OSM 래스터이며 git-ignored 레지스트리에서 로컬
  API로만 서빙합니다 (`지도 © OpenStreetMap 기여자` 표시 유지).
- **래스터 위에 도로망과 지명을 다시 그리지 않습니다.** 래스터에 이미 도로와 길 이름이 있어서
  중복이었습니다. 도로는 지금 **읽고 있는 요청의 경로만** 그리고, 지명은 회관·항구·읍내 방향
  등 필요한 것만, 줌 레벨에 따라 표시합니다. 실명 주택 라벨은 노출하지 않습니다.
- 표식은 화면 픽셀 기준입니다: 얼굴 지름 22px, P번호 13px, 상호작용 hit area 32px, 활동
  아이콘 8~10px **한 개**, 차량 28×16px 실루엣.
- **집단은 장소·차량 동일성으로 묶습니다** (`positions.clusterPoses`). 예전의 거리 기준
  묶음은 지나가던 이웃을 한 집단으로 만들고 줌을 바꾸면 구성이 달라졌습니다. 한 anchor에
  얼굴 최대 3개 + 인원수입니다.
- **픽셀 충돌 회피는 사회적 집단과 분리했습니다** (`positions.spreadOverlaps`). 겹치는 표식만
  화면 좌표에서 밀어내고, 밀린 표식은 실제 anchor로 leader line을 남깁니다. 같은 좌표에 사는
  P10·P11은 두 표식으로 남고 한 집단이 되지 않습니다.
- 지금 읽는 요청의 관련자와 경로만 진하게, 나머지는 약하게 유지합니다.
- 마을 밖 주민은 지도 아래 **단일 접힘 목록**입니다. 좌표가 없으므로 지도 여백에 놓지 않습니다.
- 재생 바는 지도 패널 레이아웃의 **별도 행**이고 지도 위 overlay가 아닙니다. 사건 단위
  앞뒤·처음으로는 '더' 안에 둡니다.
- 도구는 확대/축소/전체 한 곳에 묶습니다. 이름·얼굴·상태·경로 토글을 상시 나열하지 않습니다.
- 주민은 **작은 구분 가능한 얼굴**입니다. id에서 결정적으로 생성한 도형이고, 범례가 "실제
  주민의 모습이 아니다"라고 밝힙니다. 이장은 같은 한 사람이며 역할 표식만 다릅니다.
- 팝오버는 **동시에 하나만** 열립니다. hover·클릭·키보드 포커스가 같은 카드를 열고, 포인터를
  카드로 옮기는 동안 닫히지 않으며, Escape/바깥 클릭으로 닫고 원래 마커로 포커스가 돌아옵니다.
  화면 네 모서리에서 clamp/flip하고 자기 anchor를 16px 여유로 피합니다. 폭 262px, 장소명 ·
  사람 선택 가로 목록 · 현재 일 한두 줄 · '더 알아보기' 하나입니다. '더 알아보기'는 세 번째
  열이 그 사람을 보게 합니다 (중첩 모달 없음). 대화 발췌는 **로그에 그 사람의 utterance가
  있을 때만** 나오고 창작하지 않습니다.
- 원자료 좌표에서 계산한 거리는 **"원자료 기준 계산"** 으로 표시하고 현장 실측이라고 쓰지
  않습니다.

가운데 MEDial 조율 현황은 **요청 하나**만 봅니다 (`selectors/requests.ts`). 묶는 기준은 사건
자신의 `correlationId`이고 화면이 추측하지 않습니다. 예전 패널은 하루 전체에서 사건 *종류*를
세어 7단계 그래프를 칠했으므로, 다른 요청이 끝나면 이 요청도 완료로 보였습니다. 판단 이유
한 줄은 `medial.decided`에서, MEDial이 알고 있던 것과 제외한 후보는 `decisionId`로 이은
DecisionRecord에서 옵니다. `world.*` 사건은 요청의 correlation id를 갖고 있어도 흐름에서
제외합니다 — 무응답의 세계 쪽 이유는 연구자 전용이고 MEDial의 상태 문장에 들어가면 안 됩니다.

지도 아래 '지금 일어난 일'은 사건을 사람이 읽을 문장으로 바꿉니다
(`selectors/story.ts`). **원 로그는 그대로 두고 화면에서만 요약합니다**: 각 줄은 자기 사건
번호를 들고 있어 클릭하면 정확히 그 지점을 재생하고, 요약에서 뺀 기술 사건은 펼쳐서 원
이름과 순서로 볼 수 있습니다. 하루가 끝나면 같은 영역에서 '리뷰'로 전환됩니다 — 별도 화면을
찾아가지 않습니다. 부담이 늘었다고 답한 사람과 소수 의견을 먼저 두고, 12명 전부 펼칠 수
있습니다.

사람 이름과 조사는 **한 곳에서** 만듭니다 (`story.personName`, `story.withParticle`). 이장은
어느 패널에서나 "이장"이고, 조사는 받침으로 고릅니다 — "이장가"가 아니라 "이장이", 그리고
P1은 '일'로 읽히므로 "P1이", P2는 '이'로 읽히므로 "P2가"입니다.

결과 비교는 "무엇이 달라졌나요?" 표 하나입니다: 바꾼 운영 조건(이전 → 이후와 이유), 요청
해결/미해결과 소요 시간(단위·분모·집계 범위 함께), 누구의 일과가 바뀌었나(총량 + 가장 오래
바뀐 사람), 서비스 이용(이용함/거절/미경험 구분), 주민 리뷰 집계, 새로 생긴 문제. **합산 만족도
점수, fake percent, 승자 트로피, 자동 채택은 없습니다.** 데이터에 없는 값은 `미수집`이고
0으로 채우지 않습니다. 정책 외 입력이 달라졌으면 "통제 비교가 아닙니다"라고 먼저 말합니다.
기본 쌍은 **루프가 실제로 이어간 최신 버전과 그 부모**입니다 — 최신을 '최고안'이라고 쓰지
않으며, 양쪽 모두 select로 바꿀 수 있습니다.

### 만족도를 어떻게 다루는가
세 가지를 절대 섞지 않습니다.
- **객관 지표** — 로그에서 계산한 대기·부담·공개 범위. 연구자 지표이고 하루 전체 합산입니다.
- **모의 경험 평가** — 그 사람이 *실제로 겪은 사건만* 근거로 하는 규칙 기반 회고. 전체 공개
  기록이나 남의 경험은 들어가지 않습니다. 별점이 없고, 근거가 없으면 `unknown` 입니다.
- **실제 사람 평가** — v0.3의 현장 검토 폼으로 입력할 수 있습니다. `source: "human"` 을 만드는
  코드 경로는 그 제출 엔드포인트 하나뿐이고, 자동 루프는 그것을 호출하지 않으며 예시 제출을
  만들지 않습니다. 사람이 실제로 내기 전에는 0건입니다.

이용량도 요청/제안/수락/실제 사용을 나누고 "이동이 필요했던 횟수"라는 분모를 함께 표시합니다.

## 검증 결과

```bash
python -m pytest server/tests/simulation -q      # 87 passed
python server/tests/simulation/test_simulation.py # 38/38 (원자료 없이 단독 실행)
npm run build                                     # tsc + vite, 통과
```

전부 합성 픽스처를 씁니다. 원자료가 있을 때만 도는 대조 테스트는 없으면 조용히 통과합니다.

빌드 산출물은 시뮬레이터 한 덩어리입니다. 라우터와 lazy 분할은 companion 데모를 함께
담고 있을 때만 필요했으므로 같이 없앴습니다.

### 브라우저에서 확인한 것 (원자료 레지스트리, 실서버)
- 원본 지도 그림 위에 도로·집·건물·밭·항구와 주민 얼굴이 표시됨
- hover 팝오버에서 같은 위치 3명(P5·P7·P8)을 각각 고르고 '더 알아보기'로 상세 열기
- 주민 상세에서 페르소나 근거 카드 7개와 JSON pointer 확인
- 'MEDial이 아는 것' 모드에서 지도의 마커가 사라지고 "위치를 모르는 사람 12명" 목록으로 대체됨
- 정책 편집기에서 조건 9개를 서버 스펙대로 렌더링, rerun 실행 → 새 시도와 변경 전후 값 기록
- 비교 화면에서 통제 비교 배너·정책 필드 diff·결정 차이·결과 차이 확인
- 발견 기록 → 그 발견으로 새 revision·새 시도 생성까지 연결 확인
- 이동 지원 시나리오에서 예약 1건, 차량 표식, 두 번째 요청의 `driver_double_booked` 충돌 확인
- 서버 재시작 후 재생 커서(사건 6)가 복원되고 구버전 기록이 덮어써지지 않음

### 아직 브라우저에서 보지 못한 것 (정직하게)

브라우저 검증을 두 번 했습니다. 지금 **확인된 것**은: 1:1:1 배치(네 폭 실측), 3열 가독성,
지도가 열을 채우는 것, 현황판↔사람 상세 전환, 지도 팝오버의 '더 알아보기'와 Escape,
이름·사유·장소 표기, create 실패 화면, '실험 시작' 3연타에 세션 1개, 새로고침에 세션 0개,
근거 장면 클릭이 커서를 옮기고 모델을 부르지 않는 것, 서버 재시작 후 session·버전·커서 복원.

**아직 보지 못한 것:**

- `needs_decision` **화면**. 2차가 지시받은 설정(두 deck · `maxGenerations=2` ·
  `maxCandidatesPerGeneration=3` · `pareto_then_stop`)으로 실행했지만 `ready_for_designer` /
  `reached_max_generations` 로 끝났습니다. `iteration/selection.py`는 **비지배 후보가 2개
  이상**일 때만 이 상태를 냅니다. 서버 검사
  `test_a_tied_pair_stops_and_offers_its_branches_on_the_parent` 는 이 상태에 실제로 도달해
  후보 2개와 선택 이유가 있는지 확인하지만, **그 화면을 사람이 본 적은 없습니다.**
- **start만 실패하는 경로와 '이 실험 실행 다시 시도'.** 서버를 내리면 create부터 실패하므로
  이 상황을 만들지 못했습니다. 세션이 늘지 않는다는 것은 서버 계약 검사로만 확인했습니다.
- 팝오버가 지도 **네 모서리**에서 잘리는지, 차량 동승 구간에 탑승자가 보이는지, 래스터 위
  도로 이중 렌더.
- 현장 검토에서 **이유를 채우면** 기록 버튼이 풀리는지 (막히는 것만 확인).

**사용자 사용성 평가는 한 번도 하지 않았습니다.** 지금까지 전부 개발자 내부 확인입니다.
남은 절차는 `docs/research/18_BROWSER_VERIFY_PROMPT.md` 에 있습니다.

### 12명 회관 집합은 원자료에 없다 (DI 확인)

15번 9장 검증 항목의 "12명 회관 집합"을 만들지 않았습니다. 원자료 레지스트리
(`local-data/normalized/village.v1.json`)의 `plan.baseline`을 12명 전부 확인한 결과,
**HALL을 방문하는 사람은 이장(P6) 한 명뿐**이고 나머지 11명의 기본 일과에 회관이 없습니다.
하루 중 3명 이상이 한 장소에 함께 있는 가장 큰 집합은 12:10의 마을식품 4명입니다.
없는 장면을 만들지 않았고, 다인 집단 렌더링(얼굴 3개 + `+N`)은 그 4명으로 확인했습니다.

### 부담 지표는 도와준 사람과 도움받은 사람을 구분하지 않는다 (미해결)

`residentBurden.addedTaskMinutes`(= `ActorRuntime.task_ms`)는 **기본 일과에서 벗어난 모든
구간**을 셉니다. 부탁을 들어준 이웃의 우회 20분과, 읍내에 가야 했던 P9 본인의 이동 180분이
같은 필드에 들어갑니다. 화면이 이것을 "가장 부담이 큰 사람"이라고 부르면 지표가 하지 않는
구분을 주장하게 되므로, **화면 문구를 고쳤습니다**: "누구의 일과가 바뀌었나 / 가장 오래 바뀐
사람"이고, 두 종류가 섞인다고 함께 적습니다.

**지표 자체는 바꾸지 않았습니다.** 정의를 바꾸는 것은 연구 계약 변경이고, 저장된 시도의 값과
비교 가능성에 영향을 줍니다. 나누는 것이 맞다면 `world.divert` 호출부에서 "부탁을 수행하는
과업"과 "본인이 수혜자인 이동"을 구분해 별도 필드로 세는 것이 최소 변경입니다.

### 이번에 브라우저에서 발견해 고친 것
회귀 테스트로 고정했습니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| 이웃 상한 0 + 인계 기한 90분 | 09:30에 미해결로 닫혀 기한이 발동할 수 없었다 | 기한이 남아 있으면 닫지 않고 대기. 기한에 인계 → 12:15 해결 |
| 이동 지원 결정 근거 | "P9에게 0회 연락했고 응답이 없다" — 있지도 않은 무응답을 근거로 적었다 | 필요의 종류에 맞는 문구로 분리 |
| 동승 | 한 번의 이동인데 운전자는 `offmap`, 동승자는 `drive` 였다 | 같은 leg·같은 mode. 차량 세그먼트가 탑승자를 기록 |
| 정책 편집기 | 선택형 조건이 빈 드롭다운으로, 5–600 범위를 잃었다 | `anyOf[int, null]` 을 풀어 타입·범위·nullable을 정확히 전달 |
| 부트스트랩 | 시도 하나를 못 읽으면 화면 전체가 죽었다 | 못 읽은 것만 건너뛰고 id를 알림 |

---

# 폐기된 리뷰 기반 반복 (v0.3 역사 기록)

> 아래 v0.3 설명은 자동 후보 실행 구조가 어떤 문제를 가졌는지 추적하기 위한 역사 기록입니다.
> 현재 구현 계약으로 사용하지 않습니다. 현재 활성 구조는 이 섹션 아래의
> **2026-09-14 · Quest/Task Change Set 전환 상태**와 `docs/research/23_QUEST_TASK_CHANGE_SET.md`를
> 따릅니다. v0.3의 `ChangeProposal`, patch path, 후보 분기, 자동 선택 상태와 코드는 삭제됐습니다.

13번 문서의 I0~I4가 구현·검증되었습니다. I5(기관 역할 확장·119·다일)는 하지 않았고 아래
'아직 구현하지 않은 것'에 그대로 남아 있습니다.

## 실행 방법

서버·프론트는 위와 같습니다. 화면 상단 탭이 연구 루프의 순서입니다:
**1 설계 → 2 Cycle 실행 → 3 에이전트 리뷰 → 4 개선 → 5 세대 비교 → 6 현장 검토**.
기존 A/B 수동 비교와 발견 기록은 '수동 비교·발견' 탭에 그대로 있습니다.

'1 설계'에서 core item·초기 운영안·deck·세대 수·후보 수·예산·어댑터를 정하고 session을 만든
뒤 상단의 '반복 시작'을 누르면, **그 범위 안에서는 세대마다 승인을 묻지 않고** 진행합니다.

## 구현 범위

### 개인 리뷰 (AgentReview)
- 모든 주민 + 보건소 담당자에게 리뷰를 만듭니다. **미경험은 `no_experience` 이고 전 항목이
  `unknown`** 입니다. 미사용을 불만족으로 만들지 않습니다.
- 차원은 여섯 개입니다: 도움·해결 / 시간·노동 / 선택·거절 / 정보 공유 / 이해 가능성 /
  재이용 조건. '불편'은 별도 차원이 아니라 해당 차원의 `negative` 입니다.
- **`unknown` 이 아닌 항목은 반드시 자기가 겪은 사건 id를 하나 이상 인용**해야 하고, 성향
  해석은 자기 persona revision의 근거 카드만 인용할 수 있습니다. 위반은 저장 전에
  `ReviewBoundaryError` 로 거부됩니다 — rule 어댑터도 예외가 아닙니다.
- 경험 집합은 저장된 로그에서 그 actor의 `visibility` 로 투영합니다. 연구자 전용
  (`world.*`) 사건은 어떤 리뷰에도 들어갈 수 없습니다.
- 원자료가 "이 사람의 수락 조건은 확인되지 않는다"고 적어 둔 항목은 그 카드를 인용하며
  `unknown` 으로 남습니다. 채워 넣지 않습니다.
- 리뷰는 Cycle 종료 후의 회고 산출물이며 사건 로그에 주민 발화로 삽입되지 않습니다.

### 종합 (ReviewSynthesis)
- 차원별 문제 묶음에 **영향받은 사람과 같은 항목을 반대로 평가한 사람**을 함께 답니다.
- **소수 의견을 따로 표시**합니다: 반대한 사람보다 적게 제기했거나, 평가한 사람의 1/3 이하거나,
  기관 담당자 혼자 제기한 경우입니다.
- 주민 개선과 기관 부담이 반대로 움직이면 `conflicts` 로 보존합니다.
- 인과 문장은 **가설**로 표시하고 대안 설명을 함께 답니다.
- 연구자 객관 지표는 리뷰 **옆에** 두고 리뷰 안으로 합치지 않습니다.

### 개선 (ChangeProposal)
- 세계 밖의 개선 에이전트가 만들며, 화면에도 세계 안의 MEDial과 다른 역할로 표시됩니다.
- 세대당 최대 2개, **차원당 1개**입니다. 같은 불만에 다이얼 두 개를 돌리는 것은 후보 두 개가
  아닙니다.
- 후보마다 리뷰 참조 · **정확한 변경 전/후 값** · 기대 기제 · 영향받을 사람 · 예상 부작용 ·
  다음에 확인할 지표가 붙습니다.
- 기제는 그 Cycle이 **실제로 사용한 기능에만** 발동합니다. 안부 확인만 있었던 Cycle에서
  동승 우회 시간을 제안하지 않고, 아무도 도달하지 않은 상한을 낮추자고 하지 않습니다.
- 없으면 `no_supported_change` 와 이유를 반환합니다. 억지로 만들지 않습니다.

### 검증 (코드, 모델 판정 아님)
- 허용 경로: `contactStrategy` 와 `params/*` 아홉 개뿐입니다.
- **거부**: 페르소나 · 근거 카드 · 초기 기억 · deck/외생 사건 · 평가 기준 · 세계 사실 ·
  인력/차량/좌석 증원 · 지표 · 리뷰. 각 거부는 어떤 경계를 넘었는지 문장으로 말합니다.
- 변경 전 값이 현재 revision과 다르면 거부합니다(오래된 기준으로 쓴 제안).
- 타입·범위·열거값을 검사하고, **엔진이 실행을 거부할 정책**(head_first + allowHeadContact
  off)도 후보에서 뺍니다.
- 엔진에 handler가 없는 기능은 `requires_implementation` 으로 **기록만** 하고 실행하지
  않습니다. 지금 세 건이 그렇게 남습니다: 사전 가능 시간 확인, 종료 통지, 방문 전 통화 의무화.

### 세대 진행과 정지
- 상태: `created → running_cycle → collecting_reviews → synthesizing → proposing_changes →
  validating_changes → evaluating_candidates → selecting_next → …`
- 종료·차단 상태는 서로 다릅니다: `ready_for_designer` · `needs_decision` ·
  `budget_exhausted` · `no_valid_change` · `stalled` · `paused` · `failed` · `cancelled`.
  **어느 것도 '성공 완료'로 표시하지 않습니다.**
- 선택은 고정된 기준으로 계산합니다. **한쪽이 unknown인 차원은 우열의 근거로 쓰지 않고**,
  후보가 현재 세대에 지배되거나 지표가 완전히 같으면 진전이 아닙니다. 비지배 후보가 하나면
  자동 진행하고, 여럿이면 `needs_decision` 으로 멈춥니다(시작 시 우선순위를 지정했다면 그것으로
  선택하고 근거를 기록합니다).
- **마지막 세대를 자동 우승자로 만들지 않습니다.** 최종 채택은 디자이너의 `DecisionRecord`
  뿐이며, 어느 세대든 고를 수 있고 보류도 결정으로 기록됩니다.
- 선택되지 않은 후보 세대는 리뷰·지표까지 그대로 보존됩니다. 삭제하지 않습니다.

### 저장과 재시작
- `iteration_sessions` · `generations` · `agent_reviews` · `review_syntheses` ·
  `change_proposals` · `model_calls` · `model_results` · `designer_decisions` ·
  `field_packages` · `human_reviews` · `iteration_commands` 를 추가했습니다. 전부
  `CREATE TABLE IF NOT EXISTS` 이고 기존 표를 바꾸지 않으므로 **이전 빌드의 연구 DB가 그대로
  열립니다.**
- 모든 전이는 다음 전이 전에 DB에 씁니다. 각 전이는 이미 있는 산출물을 보고 건너뜁니다.
  모델 호출 결과는 (session, generation, role, subject, 입력 해시) 키로 캐시하므로 재시작이
  같은 세대를 두 번 실행하거나 같은 patch를 두 번 적용하지 않습니다.
- `commandId` 는 같은 인자면 멱등, 다른 인자면 409입니다.

### 통제 비교
- 기본 모드에서 모든 세대는 **같은 초기 하루·같은 주민 기억·같은 자원·같은 외생 사건**에서
  실행되고 정책만 다릅니다. 다음 세대가 이전 세대의 불만을 기억하지 않습니다.
- 비교 화면은 서버가 계산한 `controlled` 값을 그대로 씁니다. 정책 외 입력이 하나라도 다르면
  통제 비교라고 쓰지 않고 무엇이 다른지 이름을 댑니다.
- 세대 재생은 저장된 로그를 읽을 뿐 모델을 다시 부르지 않습니다.
- **다일(longitudinal) 모드는 이 빌드에서 실행하지 않습니다.** session 생성이 400으로
  거부하고 capability 목록에 미지원으로 나옵니다.

### 어댑터 모드 표시
- `rule` — 주민 행동·리뷰·개선 전부 규칙. 모델 호출 0.
- `hybrid` — 주민 행동은 규칙이고 리뷰/개선만 모델. 화면이 그렇게 씁니다.
  **전체 주민 판단이 LLM이라고 쓰지 않습니다.**
- `scripted` — 고정 리뷰로 흐름만 확인. 모델 결과가 아닙니다.

### 실제 LLM 어댑터
- Gemini(OpenAI 호환 chat completions) HTTP 클라이언트 하나. 구조화 출력은 json_schema로
  강제합니다. (2026-09-15에 Anthropic·OpenAI 경로를 걷어냈습니다.)
- 키는 **서버 환경변수에서만** 읽습니다(`GOOGLE_API_KEY`, `MEDIAL_LLM_MODEL`,
  `MEDIAL_LLM_BASE_URL`). 응답·로그·문서·health 어디에도 키가 없습니다.
- 호출마다 provider·model·prompt 버전·요청/응답 content hash·토큰·지연·검증 결과를
  기록합니다. 본문은 저장하지 않습니다.
- **키가 없으면 온라인 어댑터를 선택할 수 없습니다**(session 생성 400). 온라인 호출이 실패하면
  session이 `failed`/`model_failure` 로 멈추며 **scripted 결과로 대체하지 않습니다.** 모델 장애는
  주민의 거절이나 `unknown` 평가와 구분됩니다.
- 예산(호출 수·토큰)이 끝나면 `budget_exhausted` 로 멈추고 완료된 세대는 보존합니다.

### 디자이너 결정과 현장 검토
- 선택/보류, 이유, 성립 조건, 받아들인 trade-off, 반대 의견, 남은 질문을 저장합니다.
- 재방문용 장면 패키지를 만듭니다. 부정 평가가 많은 리뷰부터 3~5개를 고르고 기관 리뷰도
  포함합니다. 각 장면에 **모의 반응을 보여주기 전에 물을 질문**과 그 뒤에 물을 질문이 붙습니다.
- 실제 사람 응답 입력 폼이 있습니다. `source: "human"` 을 만드는 코드 경로는 이 제출
  엔드포인트 하나뿐이고, 자동 루프에서는 호출되지 않으며 예시 제출을 만들지 않습니다.
- 사람 응답은 모의 리뷰와 같은 표에 합산하지 않습니다.

### 화면에서 유지한 것
북쪽 위 2D 실제 지도, 작은 얼굴, 사람과 별도인 차량과 좌석 점, 같은 위치 팝오버(hover·키보드),
지도 밖 주민 목록, 'MEDial이 아는 것' 관측 모드, 재생 커서와 회고 보기가 그대로 동작합니다.
리뷰의 '근거 장면' 링크는 그 시도를 열고 해당 사건 번호로 커서를 옮깁니다 — 저장된 로그를
읽을 뿐이고 어댑터를 호출하지 않습니다.

v0.4에서 화면 구조가 세 개로 바뀌었지만 **위 계약은 그대로입니다**: 관측 경계, 불변 로그,
원자료 좌표, 합성/실제 구분, probe/fork, 세대 계보, `source: "human"` 을 만드는 단일 경로.
새 위치는 위 '화면 (v0.4 재설계)' 표에 있습니다.

## 이번에 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| 귀가 동승 (보고된 항목) | 귀가 leg에 탑승자가 기록되지 않아 지도의 차량이 빈 차로 보였다. 게다가 운전자와 동승자에게 **같은 Segment 객체**를 넘겨 `riders` 목록을 공유했다 | 두 사람에게 각각의 복사본을 주고 운전자 쪽에만 탑승자를 기록. `Segment.clone()` 이 가변 목록을 공유하지 않음. 회귀 테스트 2건 |
| `/events` 간헐 500 | 하나의 sqlite3 연결을 API 스레드풀과 반복 루프 스레드가 함께 쓰는데 **읽기에 잠금이 없었다** | 읽기도 같은(재진입) 잠금으로 보호. 쓰기 중 동시 읽기 회귀 테스트 |

기존 기록은 다시 쓰지 않으므로, 고치기 **전에** 저장된 시도는 여전히 빈 탑승자 목록을 갖고
있습니다. 이후 실행부터 올바릅니다.

## 검증 결과 (v0.3)

```bash
python -m pytest server/tests/simulation -q      # 139 passed
npm run build                                     # tsc + vite, 통과
```

전부 합성 픽스처를 쓰며 **모델을 호출하지 않습니다.**

리뷰 반복에 대한 테스트가 확인하는 것:
- v0→리뷰→종합→후보→v1→리뷰→v2까지 3세대가 저장되고 서로 연결된다 (좋아졌는지는 검사하지
  않는다 — 그것을 검사하면 '항상 개선된다'를 검사하는 것이 된다)
- 모든 actor가 리뷰를 받고 미경험은 전부 `unknown` 이다
- 긍정만 나오지 않는다(고정 리뷰 탐지)
- 남의 사건·남의 근거 카드·근거 없는 등급·세계 진실 사건 인용이 각각 거부된다
- 소수 의견과 반대 의견이 종합에서 살아남는다
- 주민 쪽 개선이 기관 부담 증가를 가리지 않는다
- 페르소나/근거/기억/deck/기준/세계/자원 경로 patch가 각각 거부된다
- 오래된 before 값, 범위 밖 값, 엔진이 거부할 정책 조합이 거부된다
- 미지원 기능 제안은 patch 없이 기록되고 실행되지 않는다
- unknown이 우열 판정의 근거가 되지 않는다
- trade-off는 `needs_decision` 으로 멈추고, 사전 우선순위가 있으면 그것으로 고르고 근거를 남긴다
- 필수 조건 위반 후보가 제외된다
- 재시작이 세대·리뷰·patch를 두 번 적용하지 않는다
- 예산 종료·모델 장애가 성공으로 표시되지 않고, 모델 장애가 unknown 리뷰로 둔갑하지 않는다
- 키 없는 온라인 어댑터 선택이 400이고 scripted로 강등되지 않는다
- 모델 설명에 키가 들어가지 않는다
- 세대 간 초기 하루·기억·자원·seed가 동일하다
- 다일 모드가 거부된다
- 저장된 세대를 읽어도 모델을 호출하지 않는다
- **사람이 제출하기 전에는 `source=human` 데이터가 하나도 없다**
- 사람 응답이 자기 패키지 밖 장면을 참조하면 거부된다
- 디자이너가 이전 세대를 고를 수 있고 보류도 기록된다

### 브라우저에서 확인한 것 (원자료 레지스트리, 실서버, rule 어댑터)
- 설계 화면에서 허용 수정 경로·미구현 목록·어댑터 선택(키 없어 온라인 비활성)을 확인하고
  session 생성
- '반복 시작' 한 번으로 v0 → v1 후보 2개 실행 → 자동 선택 → v2 후보 2개 실행까지 진행,
  `정체 (새 진전 없음)` 과 그 이유를 상단에 표시
- 리뷰 화면에서 actor별 카드, 미경험 회색 표시, 차원별 평가와 근거 사건·근거 카드 확인
- 리뷰의 '근거 장면' 클릭 → Cycle 화면이 그 시도로 바뀌고 커서가 사건 23으로 이동
- 개선 화면에서 문제 묶음·소수 의견·이해관계 충돌·후보의 변경 전/후 표·예상 부작용,
  '구현 필요 — 자동 실행 안 함' 3건 확인
- 세대 비교에서 통제 비교 배너와 snapshot 해시, 기준별 값, 정책 diff, 사람별 득실
  (P6 19.9분 → 0분, 기관 0분 → 25분) 확인
- 현장 검토에서 v1을 선택하고 이유·조건·trade-off·반대 의견·남은 질문 저장 → 장면 패키지
  5개 생성 → 실제 사람 응답 1건 제출(`source=human`), 제출 전에는 0건이었음
- 'MEDial이 아는 것' 모드에서 지도 마커가 사라지고 위치 미상 목록으로 대체되는 동작 유지
- 귀가 동승 구간에서 운전자 leg에 탑승자가 기록되고 동승자 leg는 비어 있음(원자료 실행)

**온라인 LLM 검증 범위: 없음.** 이 환경에 모델 키가 없어 실제 provider 호출은
**한 번도 수행하지 않았습니다.** 어댑터·스키마·호출 기록·실패 처리·예산 정지는 구현되어
있고 오프라인 경계 테스트로만 확인했습니다. 키가 있는 환경에서 online smoke test를 수행하고
호출 횟수·모드를 보고하는 것이 남아 있습니다.

---

## 검증 결과 (v0.4 화면 재설계)

`npm run build` 통과.
`python -m pytest server/tests/simulation -q` **151 passed** (기존 139 + `test_screen_contracts.py` 12건).
`npm test` (vitest) **41 passed** — selector 동작 23건 + 화면 mount 15건 + 그리드 정합성 3건.

새 검사는 화면이 서버에 요구하는 계약만 봅니다 (CSS 값을 복제하는 테스트는 만들지 않았습니다):
모든 사건이 `correlationId`를 갖는가, 하루에 두 요청이 스트림을 섞지 않는가,
`world.reachability_resolved` 가 MEDial 가시성에서 빠져 있는가, seek이 저장 커서를 옮기는가,
재시작 후 커서가 복원되는가, 같은 commandId 재전송이 커서를 두 번 옮기지 않는가, 같은
session에 두 번째 start가 409인가, create가 start하지 않는가, **GET이 루프를 시작하지
않는가**, session 목록 조회가 아무것도 만들지 않는가.

### 자동 검사가 무엇을 지키는가

`npm test`는 두 종류입니다. **정적인 CSS 값을 복제하는 테스트는 없습니다** (15번 9장 금지).

- `selectors/selectors.test.ts` — 화면이 하는 *주장*이 참인지. 두 요청이 섞이지 않는가,
  `world.*`가 흐름에서 빠지는가, 무응답의 세계 쪽 이유가 문장에 안 나오는가, 내부 타입 이름이
  상태로 출력되지 않는가, `knownFacts`가 DecisionRecord에서 오는가, 발화가 payload에 있을
  때만 인용되는가, 요약에서 뺀 사건이 전부 펼침 목록에 남는가, 이장이 어느 패널에서나 이장인가,
  조사가 받침에 맞는가.
- `screens/screens.test.tsx` — 두 화면을 **합성 마을에서 만든 실제 API payload**로 mount.
  세 열이 렌더되는가, `undefined`/`[object Object]`가 새지 않는가, 합산 점수·승자 표기가
  없는가, 부담 지표가 "둘을 구분하지 않는다"고 말하는가, 발화가 없을 때 없다고 말하는가.
  화면이 실제 데이터에서 throw하면 `tsc`는 모르고 이 저장소에는 CI 브라우저가 없으므로,
  이 mount 검사가 그 자리를 대신합니다.

fixture는 `fixtures/ui/` 이고 `scripts/gen_ui_fixtures.py` 로 다시 만듭니다. **합성 마을에서만**
생성합니다 — 원자료 파생 지명·나이·직업은 `local-data/`가 git-ignored인 이유 그대로 저장소에
들어오면 안 됩니다.

### 브라우저에서 확인한 것 (1440×900 · 1366×768, 원자료 레지스트리, 실서버)

아래는 **1:1:1 배치 이전** 상태에서 확인한 것입니다. 1:1:1 배치와 3열 사람 패널은 브라우저에서
보지 못했고, 위 mount 검사로만 확인했습니다 — 자세한 내용은 이 절 끝의 '아직 보지 못한 것'.
- **가로 넘침 0.** 1440/1366/1050/860 모두 `scrollWidth == innerWidth`. 예전 화면은 1440에서
  1448px였습니다 — 원인은 `body`의 기본 8px 여백과 `width: 100vw` 였고, index.html에 리셋을
  넣고 Root를 `width: 100%` 로 바꿨습니다.
- **A 실험 준비**: 질문 · 초기안(자연어 3줄) · 접힌 실험 설정(요약이 실제 상태에서 생성:
  "초기안 포함 최대 3개 버전 · 시나리오 2개 · 규칙 기반 리뷰 · 규칙 기반 개선 · 모델 호출
  없음") · '실험 시작' 하나만 보입니다. 미구현 목록·필드 경로·해시는 기본 화면에 없습니다.
- **시작 한 번**: '실험 시작'을 연속 3번 눌러 session이 1 → **2개**만 늘었고(중복 없음),
  루프가 끝까지 돌아 `stalled` 로 정지했습니다. 이후 새로고침에서 session 2개 · 시도 29개
  그대로 — **불러오기가 실험을 만들지 않습니다.**
- **B 관찰**: 그룹 팝오버가 열린 지도(은점마을식품에 4명 = 얼굴 3개 + `+1`, 사람 선택 가로
  목록, 현재 일, '더 알아보기', 'Esc 닫기'), 우상단 한 요청의 관측→판단→조율→결과 흐름,
  지도 아래 '마을 밖 · 3명 / P2 · TOWN / P3 · TOWN / P12 · TOWN'.
- **요청이 섞이지 않음**: 같은 하루의 `checkin-P1`과 `req-P1-1`이 별개 흐름으로 나오고
  select에서 '정기 안부 연락 · 응답 없음'과 '확인 요청 · 기관 인계'로 구분됩니다.
- **C 결과 비교**: 기본 쌍이 v0 ↔ v1(이웃 대신 본인 재연락)이고 "같은 하루, 운영 조건만 다름"
  으로 표시됩니다. 바꾼 조건 `연락 순서: 이장에게 먼저 → 본인 재연락 후 보건소`,
  `재연락 횟수: 0 → 1`, 소요 시간 `80.8 → 158.3 (+77.5분)`, 부담·이용·리뷰 집계, 그리고 v0의
  악화 항목(특정인에게 몰린 시간, 소수 의견 표시)이 함께 나옵니다. '다음 시도에서 바꾼 이유'가
  주민 인용 → 묶인 문제 → 제안 → 실제 변경 → 남은 질문으로 끊기지 않고 이어집니다.
- **현장 검토 시트**: 채택/보류/기각 선택, 이유·성립 조건·감수한 부담·반대 의견·현장 질문 입력,
  기록된 결정, 장면 5개와 사전/사후 질문, 그리고 `source = human` 라벨이 붙은 별도 입력 영역이
  한 겹의 시트에서 열립니다.

### 이번에 브라우저에서 발견해 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| MEDial 상태 문장에 `world.reachability_resolved` | 세계 사건이 요청의 correlation id를 갖고 있어서 흐름에 들어오고, 무응답의 **세계 쪽 이유**가 MEDial의 상태 줄에 노출됐다 | `requestFlows`가 `world.*` 를 제외. 기본 분기에서 내부 타입 이름을 상태로 출력하던 fallback도 제거 |
| "MEDial이 알고 있던 것 0가지" | `knownFacts`를 `medial.decided` payload에서 읽었는데 그 필드는 DecisionRecord에 있다. 매번 빈 목록이라 MEDial이 아무것도 모른 것처럼 보였다 | `decisionId`로 두 기록을 이음. 제외한 후보와 그 이유도 함께 (실제로 2가지 · 8명) |
| 요청 select에 같은 이름 두 개 | `checkin-P1`과 `req-P1-1`이 둘 다 "P1 안부 확인"이 됐다. id에서 사람을 뽑는 코드가 `checkin` 을 사람 이름으로 잘못 읽기도 했다 | 사람 토큰을 정규식으로 찾고, 접촉만 있는 묶음은 '정기 안부 연락', 인계가 있으면 '확인 요청 · 기관 인계'로 구분 |
| 기본 비교가 항상 "통제 비교 아님" | 기본 오른쪽이 index 최댓값이었는데 그건 대개 진행되지 않은 **분기**여서, 기본 화면이 부모 관계 없는 두 버전을 열고 차이를 설명할 수 없다고 정확히 말했다 | 기본을 '루프가 이어간 최신 버전 ↔ 그 부모'로. 최신을 최고안이라고 쓰지 않고 양쪽 모두 선택 가능 |
| 초기 운영안 select에 파생 revision 12개 | 지난 실험이 만든 revision이 전부 나왔고 "A · 이장 우선 확인 (수정)" 세 개가 이름까지 같았다 | `-r<hash>` 접미사를 가진 revision 제외. revision에서 이어가려면 결과 비교에서 버전을 고르고 새 실험을 만듭니다 |
| 버전 이름이 "v0 · v0 · …" | 저장된 label이 이미 `v0 · ` 로 시작하는데 index를 다시 붙였다 | `versionName()` 하나로 통일 |

### 브라우저 검증 1차 (1:1:1 배치) — 배치가 완전히 깨져 있었다

1:1:1로 바꾼 뒤 **처음으로** 브라우저에서 열어 본 결과, **1280px 이상에서 세 열이 나란히
있지 않고 한 자리에 겹쳐 있었습니다.** 1440에서 village · orchestrator · person 세 패널의
`getBoundingClientRect()`가 전부 동일한 `[393, 134, 1027, 746]` 이었고, 사람 패널이 위에
덮여 나머지 둘을 가렸습니다. 1366도 같았습니다.

원인은 `grid-template-areas` 가 **미디어쿼리 두 개에만** 있고 기본 규칙에 없었던 것입니다.
세 자식은 `grid-area: village | orchestrator | person` 으로 자기 자리를 **이름으로** 지정하는데,
그 이름을 정의한 areas가 없으면 CSS는 이름을 **line name**으로 해석합니다. implicit track이
생기고 셋이 같은 셀에 들어갑니다. 측정된 computed columns가
`103.14px 103.14px 103.14px 0px 1026.56px` (5트랙), areas는 `none` 이었습니다.

**mount 검사가 이것을 잡지 못한 이유**를 남겨 둡니다: jsdom은 레이아웃을 계산하지 않고,
세 열은 실제로 문서 안에 다 있었으므로 12건이 전부 통과했습니다. `tsc`도 볼 수 없습니다.
브라우저만 볼 수 있었습니다. 그래서 이제 `screens/layout.test.ts` 가 **CSS 값이 아니라 두
선언의 정합성**을 검사합니다 — 자식이 이름으로 자리를 잡는다면, 열을 정의하는 모든 폭이 그
이름들을 정의해야 한다. (수정을 되돌려 검사가 실패하는 것까지 확인했습니다.)

같은 라운드에서 899px 미만도 깨져 있었습니다. 쌓였을 때 조율·사람 열이 각각
`min-height: 420px / 480px` 를 요구해 이미 뷰포트를 넘으므로, `min-height: 0` 이 걸린 지도 열은
트랙 높이 **0px**을 받고도 자식들은 제 크기로 그려져 **지도가 조율 패널 위에 겹쳐 칠해졌습니다.**
지도 열에 자동 최소 높이(`min-height: auto`)를 돌려주어 자기 내용을 요구하게 했습니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| ≥1280px에서 세 열이 겹침 | 기본 규칙에 `grid-template-areas` 누락 → 자식의 area 이름이 line name으로 해석 | 기본 규칙에 areas 선언. `layout.test.ts` 가 "열을 정의하는 모든 폭은 자식이 쓰는 이름을 정의해야 한다"를 고정 |
| <899px에서 지도가 조율 패널을 덮음 | 다른 두 열의 min-height가 뷰포트를 넘어 지도 트랙이 0px, 자식은 제 크기로 그려짐 | 지도 열에 `min-height: auto` |
| Escape로 팝오버가 안 닫힘 | 핸들러가 **고정된**(`selectedCluster`) 카드만 처리했다. 카드 스스로 "누르면 고정"이라고 안내하므로 열려 있는 카드는 대개 hover로 열린 것이고, 그 경우 Escape가 아무 일도 하지 않았다 | 열려 있는 카드(`openKey`) 기준으로 닫고 원래 마커로 포커스 복귀 |
| 같은 사람을 '이장 (P6)'과 'P6'로 다르게 부름 | 종합·제안 payload의 actor id를 결과 비교가 그대로 출력했다 | `story.nameList()` 를 추가해 반대 평가자·충돌 양측·미경험자·영향받는 사람 목록이 전부 같은 이름 규칙을 쓴다 |
| favicon 404 | 콘솔에 유일하게 남던 에러 | index.html에 inline SVG 아이콘 (파일 추가 없음) |
| 콜드 스타트에 iteration API가 503 | `set_iteration_service()` 가 `set_service()` 안에서만 호출돼, `/api/sim/*` 를 한 번 때리기 전에는 반복 API 전체가 죽어 있었다. 화면은 village를 먼저 부르므로 드러나지 않았지만, **세션 수를 세려고 API를 직접 부르면 빈손**이었다 | `get_iteration_service()` 도 `get_service()` 처럼 지연 생성. 서버 기동 후 첫 요청이 `/iteration/sessions` 여도 200 |

브라우저에서 **정상임을 확인한 것**도 함께 기록합니다: 1440/1366/1050/860 모두
`scrollWidth == innerWidth`(가로 넘침 0), 1279px 전환, 지도 세로 비율과 좌우 여백(가로 왜곡
없음), 그룹 팝오버가 동시에 하나만 열리고 포인터를 카드로 옮겨도 유지되는 것, 같은 좌표의
P10·P11이 표식 두 개로 남는 것, OSM 크레딧, 사람 패널의 정직성 문구
("모의 리뷰입니다. 점수가 아니고 실제 주민의 만족도도 아닙니다", 발화가 없을 때
"없는 대화를 만들지 않습니다"), `undefined`/`NaN`/내부 타입 이름/세계 쪽 이유 유출 없음,
서버 재시작 후 session·버전 복원.

### 브라우저 검증 2차 — 배치는 통했고, 클릭이 안 되고 있었다

1차에서 고친 배치는 실측으로 확인됐습니다 (1440에서 세 열 left 20 / 492 / 964, 각 456px,
네 폭 모두 `scrollWidth == innerWidth`, 860에서 겹침 없음, 콘솔 에러 0). 그 위에서 2차가
찾아낸 것들입니다.

| 발견 | 문제 | 조치 |
|---|---|---|
| 팝오버의 '더 알아보기'가 아무 반응이 없다 | 지도 프레임이 pan을 위해 `setPointerCapture`를 건다. **포인터가 잡히면 뒤따르는 click이 프레임으로 재타깃**되므로 프레임 *안*에 있는 버튼은 click 자체가 발생하지 않았다. 확대/축소 버튼도 같은 처지였다 | 오버레이(`data-overlay`)에서 시작한 pointerdown은 드래그를 시작하지 않는다 |
| Escape가 hover로 연 카드를 못 닫는다 | 1차 수정이 논리적으로는 맞았지만 무효였다. React가 상태 갱신을 배치하므로, Escape가 포커스를 마커로 되돌리며 부르는 focus 핸들러가 **아직 반영되지 않은** 값을 읽고 자기가 만든 닫힘을 즉시 되돌렸다 | 함수형 갱신. 코드를 읽어서는 못 찾고 브라우저에서만 드러났다 |
| 문장에 내부 코드가 샌다 | "도움 · 해결 · **mixed**", "부탁을 받았지만 이번에는 하지 못했다 (**driving_status_unknown**)", 현황판의 "자택 · **HOME:P6**", "마을 밖 · **TOWN**" | 세 곳을 한 곳씩으로: `ASSESSMENT_LABELS`(공유), `story.reasonText` / `reviewers.reason_ko`(같은 표를 양쪽에), `positions.placeLabel` |
| create 실패 화면에 "Internal Server Error" | 서버가 죽어 있으면 dev 프록시가 이 문구로 500을 준다. 한국어 두 줄 사이에 영어 원문이 그대로 놓였고, 읽는 사람이 할 수 있는 일을 말하지 않았다 | `describe()`가 전송 실패의 상투구만 걸러 "서버가 응답하지 않습니다"로 바꾼다. **서버 자신의 거절(400/409 한국어 detail)은 그대로 통과** |
| 하루가 끝나도 리뷰로 안 바뀐다 | 15번 5장은 "하루가 끝나면 같은 영역에서 리뷰로 전환"을 요구한다. 수동 칩만 있었다 | 커서가 로그 끝에 닿으면 리뷰가 기본. 칩을 누르면 고정되고 더는 커서를 따라가지 않는다 |
| favicon 404 · 콜드 스타트 503 | 1차 보고분 | 고쳐졌음을 2차가 확인 (콘솔 에러 0, 첫 요청 200) |

`reasonText` / `reason_ko`는 같은 표를 화면과 서버가 각각 갖습니다. 한국어로 이미 쓰인 사유는
그대로 통과시키고, **표에 없는 코드는 "기록된 사유 코드 X"로 표시**합니다 — 사유를 잃는 것이
코드를 보여 주는 것보다 나쁘기 때문입니다.

### 스크린샷은 저장소에 넣지 않습니다

`docs/research/screenshots/` 를 gitignore에 넣었습니다. 실서버 스크린샷에는 주민의 일과·장소·
현재 상태가 그대로 담기고, 그것이 `local-data/` 를 제외하는 이유와 같습니다. 파일은 로컬에
## 2026-09-14 · Quest/Task Change Set 전환 상태

[23_QUEST_TASK_CHANGE_SET.md](docs/research/23_QUEST_TASK_CHANGE_SET.md)에서 MEDial의 최소 Quest,
Task, Change Set 계약을 확정했고 활성 개선 경로를 이 계약으로 교체했습니다.

- **구현됨:** 점수 없는 근거 기반 주민 평가, 다섯 개선 대상, Quest/Task의 의미 있는 전후 규칙,
  내부 실행 바인딩의 단일 변환 경계, AI/규칙 초안, 연구자 수정본·독립 가설 저장, 실행 전 이유가
  있는 명시적 확인, 확인된 Change Set 하나만으로 새 revision 실행.
- **삭제됨:** `PatchOp`·`ChangeProposal`, 허용 patch path, 후보 실행 분기, Pareto/지표 기반 선택,
  `needs_decision`·`evaluating_candidates`, 자동 우승자와 관련 API·UI·테스트.
- **UI:** 주민 평가에서 나온 Change Set의 관련 Quest/Task와 전후 규칙만 먼저 보인다. 기대 효과,
  가능한 부담, 다음 관찰 항목을 함께 보여 주며 내부 실행값은 기술 세부사항 안에 접는다.
- **감사 이력:** 초안을 수정해도 원본을 덮어쓰지 않는다. 원본은 `superseded`, 수정본은
  `researcher_hypothesis`로 남으며, 저장과 실행 확인은 별도 명령이다.
- **범위 한계:** Quest/Task는 무응답 안부 확인·의료 이동 지원 두 사례와 닫힌 Task/필드 목록만
  지원한다. 다일 기억, 119, 범용 정책 언어, 실제 주민 대신의 타당성 주장은 여전히 미구현이다.
- **최종 자동 검증:** `python -m pytest server/tests -q` 148 passed, `npm test -- --run` 42 passed,
  `npm run build` 통과, `git diff --check` 통과.
- **실제 Chromium 검증:** 구형 v0.3 세션은 활성 목록에서 분리되어 첫 화면이 오류 없이 열렸다.
  새 session은 Change Set 확인 전 revision 1개만 보였고, 연구자 수정본 저장 후에도 실행되지
  않았다. 이유를 입력해 확정한 뒤에만 revision이 2개가 됐다. 브라우저 콘솔 오류 0건.
- **구형 로컬 기록:** 삭제·변환하지 않는다. 활성 계약 목록에서는 제외하고 직접 접근은 409와
  명확한 이전 형식 안내를 반환한다.

## 2026-09-14 · MAS 재구축 1단계 — 환경을 버전 붙은 고정 입력으로

MAS를 제대로 세우기 위한 여덟 단계 중 **1단계만** 끝냈습니다. 사용자에게 보이는 동작은
하나도 바뀌지 않았고, 바뀐 것은 "무엇이 입력으로 기록되는가"뿐입니다.

**왜 했는가** — 세계의 규칙이 세 곳에 모듈 상수로 흩어져 있었습니다. 가시성은
`contracts.WORLD_TRUTH_EVENTS`, 스케줄링은 `engine.PRIORITY`, 전화 도달 여부는
`agents/rule_agents.PHONE_REACHABILITY`였습니다. 특히 세 번째는 **세계의 사실인데 주민
어댑터 안에** 있어서, 어댑터를 LLM으로 갈아 끼우면 규칙이 같이 사라지는 구조였습니다.
그리고 셋 중 무엇도 `inputHashes`에 없어서 **가정을 바꿔도 두 attempt의 입력 해시가
같았습니다** — v0/v1 평가 차이가 정책 때문인지 가정을 손댔기 때문인지 구분할 수
없었다는 뜻입니다.

- **구현됨:** `EnvironmentRevision` 계약(`contracts.py`)과 `env-v1`(`environment.py`).
  스케줄링·도달 규칙이 데이터로 올라갔고 엔진이 주입받습니다. `Attempt.environmentRevisionId`와
  `inputHashes["environment"]` 추가. `RoutineVariation` 계약은 **필드만 정의**했습니다.
- **잠금:** 환경은 doc 22의 고정 사례 입력입니다. `validation.FIXED_INPUT_PREFIXES`가
  환경·마을·페르소나·일과·덱·자원·평가를 대상으로 삼는 Change Set을 거부하고,
  "지원하지 않는 필드"가 아니라 **"고정 사례 입력"**이라는 이유를 돌려줍니다. MEDial이
  전화가 더 잘 닿게 만들어서 좋아 보이는 경로를 기계적으로 막은 것입니다.
- **삭제됨:** `engine.PRIORITY`, `rule_agents.PHONE_REACHABILITY`, `PHONE_DEFAULT`,
  `phone_rule`, `device_rule`. 호환 계층을 남기지 않았습니다.
- **검증:** 기존 148개가 그대로 통과했습니다(행동 변화 0). 신규 21개는 `env-v1`이 옛 상수를
  **값 단위로 재현**하는지, 도달 규칙 중 `source-adapted`가 밭 하나뿐인지, 환경을 바꾸면
  입력 해시가 달라지는지, 고정 입력을 노린 Change Set이 거부되는지를 고정합니다.
  `python -m pytest server/tests -q` 169 passed, `npm test -- --run` 42 passed, 빌드 통과.

### 1b · 일과의 확률적 변이 (구현 완료)

원본의 출발 시각은 **하루치 기록**이지 법칙이 아닙니다. 그래서 실행마다 seed에서 가까운 하루를
뽑고, 기록과 달라진 모든 지점에 이유를 남깁니다. 이것은 주민이 변덕스럽다는 주장이 아니라
**그 기록의 정확도에 대한 불확실성**입니다.

- **구현됨:** `DayRealization`·`ResidentDay`·`StepChange` 계약, `day.py`의 생성기.
  ±15분 출발 지터, 외출 결석 확률 0.15, 반복 외출 확률 0.0(기본 꺼짐), 변경별 사유 기록,
  `classification`(`source_baseline` / `source_jittered` / `plausible_extension`),
  `inputHashes["day"]`와 `Attempt.dayRealizationId`.
- **하루는 `(마을, 환경, seed)`의 순수 함수입니다.** rerun과 fork는 부모의 seed를 이어받으므로
  **아무것도 복사하지 않고** 같은 날에 떨어집니다. seed를 바꾸는 것은 "다른 날을 돌린다"는
  명시적 실험이고 입력 해시가 달라집니다.
- **지어내지 않는 것:** 새 목적지를 만들지 않습니다(반복 외출도 본인 기록에 있는 장소만).
  방문 순서를 바꾸지 않습니다. baseline이 한 단계뿐인 **P9·P10·P11은 제외**하고, 화면에
  "원자료에 일과 기록이 없어 흔들면 창작이 된다"는 이유를 남깁니다.
- **물리적으로 가능한 하루:** 두 단계 사이의 최소 간격은 상수가 아니라 **실제 이동 시간**입니다.
  `_retime`이 `world.build_baseline`과 같은 방식으로 도로망 leg를 누적해 clamp하며, clamp도
  기록에 남습니다.
- **난수 스트림은 `(seed, 주민 id)`로만 키잉합니다.** 마을 해시를 넣었더니 주민 한 명을
  추가하거나 오타를 고치는 것만으로 전원의 하루가 다시 뽑혔습니다. 어느 registry를 썼는지는
  이미 `inputHashes["village"]`에 있습니다.
- **`env-v1-fixed` 추가:** 변이를 끈 같은 환경. 시나리오 역학 테스트(엔진이 상황에 맞게
  동작하는가)는 이쪽에서 돌립니다. 뽑힌 하루가 그 상황을 만드는가는 별개 질문이고
  `test_day.py`가 따로 봅니다.
- **검증:** `python -m pytest server/tests -q` 247 passed(신규 `test_day.py` 78개),
  `npm test -- --run` 42 passed, 빌드·`git diff --check` 통과.

**작업 중 발견한 것 두 가지**

| 발견 | 문제 | 조치 |
|---|---|---|
| 한 사람이 동시에 두 곳에 | 첫 지터가 최소 간격을 1분 상수로 봤다. 실제로는 앞 단계의 **이동이 끝나야** 다음 출발이 가능하다. 기존 불변 검사(`test_actor_is_in_one_place_at_a_time`)가 잡았다 | `_retime`이 `leg()`로 도착 시각을 누적해 clamp. 여러 seed에 대해 같은 불변을 거는 회귀 검사 추가 |
| 두 이동 정책이 더 이상 다르지 않음 | 뽑힌 하루에서는 `policy-T-A`와 `policy-T-B`의 "부탁한 사람 수"가 같아졌다 | 버그가 아니라 **결과**다. 정책 역학 검사는 `env-v1-fixed`로 옮기고, 이 현상 자체는 감도 분석(7단계)에서 다룰 신호로 남겼다 |

두 번째가 이 변경의 실제 가치입니다. 지금까지 `seed`는 `Attempt`에 있으면서 아무것도 흔들지
않는 무의미한 필드였고, 그래서 "이 차이가 어떤 날에나 나타나는가"를 물을 수 없었습니다.

**아직 안 한 것:** 뽑힌 하루를 화면에 보여 주는 UI. `metrics["dayRealization"]`에 실려 있지만
비교 화면이 읽지 않습니다. "이 비교는 같은 날을 공유한다 / 이 실행은 결석을 가정한
plausible_extension이다"가 아직 연구자에게 보이지 않습니다.

**남은 단계:** 2 어댑터 per-actor 분리와 모델 호출 기록·재생 / 3 2단계 의사결정과 LLM 실제 연결 /
4 주민 간 상호작용 / 5 보건소 다건·소장 / 6 119 5단계 / 7 감도 분석 / 8 문서 19·23 경계 갱신.

**주의:** 4·5·6은 doc 19와 23이 "연구 질문이 직접 요구하기 전에는 확장하지 않는다"고 배제해 둔
항목입니다. 사용자 지시로 여는 것이며, 8단계에서 두 문서를 함께 고쳐야 문서와 코드가 어긋나지
않습니다.

## 2026-09-14 · MAS 재구축 2단계 — 모델 호출을 기록하고 재생한다

LLM을 붙이기 **전에** 해야 하는 일입니다. 모델이 주민 대신 대답하는 순간, 비교 화면의
중심 주장("이 둘은 정책만 다르다")은 같은 대답을 다시 받아낼 수 있는지에 통째로
걸립니다. temperature=0은 결정론이 아닙니다 — 공급자는 배치하고 양자화하고 라우팅하며,
몇 주 뒤에는 모델이 은퇴해 있습니다. 그래서 **잘 부탁하는 대신 받아 적어 두고 재생합니다.**

- **구현됨:** `ModelPolicy`·`ModelCallRecord` 계약, `agents/model_calls.py`의
  `call_key()`·`ModelCallLog`, `model_calls` 테이블과 `Store.model_calls()`,
  `Attempt.modelPolicy`와 `inputHashes["modelPolicy"]`, `metrics["modelCalls"]`.
- **호출 키는 내용 주소입니다.** 모델·온도·프롬프트 판·행위자·그 행위자의 몇 번째 호출·
  프롬프트 본문. **벽시계 시각은 일부러 뺐습니다** — 같은 날을 몇 시간 뒤에 다시 돌려도
  같은 키가 나와야 하기 때문입니다. `latencyMs`와 `createdAt`은 비용 계산용으로 기록만
  하고 키에는 넣지 않습니다.
- **재생 모드는 빈칸을 스스로 메우지 않습니다.** 기록이 없는 프롬프트는
  `MissingModelCall`(= `AdapterError`)로 실패합니다. 조용히 다시 부르면 재생처럼
  보이면서 돈을 쓰고 결과가 달라집니다 — 이 모듈이 존재하는 이유가 그 실패입니다.
- **fork는 부모의 기록을 물려받습니다.** 공유 구간에서는 프롬프트가 같으니 키가 맞고
  부모의 대답이 그대로 재생됩니다. 체크포인트 이후의 갈라짐은 정책의 몫입니다.
  **rerun은 물려받지 않습니다** — 첫 분부터 다른 정책으로 다시 도는 실행이라 우연한 키
  일치가 검증되지 않은 연속성으로 통과할 여지를 주지 않습니다.
- **어댑터를 행위자별 인스턴스로 분리했습니다.** 전에는 하나의 객체를 전원이 공유했습니다.
  어댑터가 무상태인 동안에는 괜찮지만, 호출 카운터·검색 캐시·대화 이력이 생기는 순간
  **마지막에 말한 사람의 것**이 됩니다. 엔진은 각자가 무엇을 *볼* 수 있는지는 이미
  격리하고 있었고(`observations`), 이건 말하는 쪽의 같은 격리입니다.
- **모델 정책도 고정 사례 입력입니다.** Change Set이 온도를 올리거나 모델을 바꿀 수
  있으면 그건 MEDial이 좋아진 게 아니라 모델이 좋아진 것입니다. `FIXED_INPUT_PREFIXES`에
  `model`·`modelPolicy`·`prompt`·`temperature`를 추가했습니다.
- **모델 실패는 어댑터 실패입니다.** 형식이 깨진 응답도 마찬가지입니다 — 스키마 대신
  산문을 돌려준 모델이 주민을 거절하게 만든 것은 아닙니다. 침묵(`action: null`)은
  정상 응답이고, 깨진 응답과 구분해 둡니다.
- **검증:** `python -m pytest server/tests -q` 276 passed(신규 `test_model_calls.py` 29개),
  `npm test -- --run` 42 passed, 빌드 통과.

**여전히 모델 공급자는 연결되어 있지 않습니다.** `record` 모드에서 공급자가 없으면 조용히
넘어가지 않고 그렇게 말하며 실패합니다. 실제 연결은 3단계입니다.

**작업 중 확인한 것 — MAS라고 부를 만한 것이 아직 없습니다**

`adapter="llm"`으로 전체 시나리오를 돌리면 **모델 호출이 실행당 정확히 1회** 일어납니다.
정책 넷 모두 그렇습니다.

| 정책 | 호출 대상 | 시각 | 허용 행동 |
|---|---|---|---|
| policy-A-v1 | P6 (이장) | 09:30 | accept / decline / defer |
| policy-B-v1 | HC_NURSE | 10:50 | accept / decline |
| policy-T-A-v1 | P6 | 09:30 | accept / decline / defer |
| policy-T-B-v1 | P6 | 09:30 | accept / decline / defer |

MEDial 자체는 규칙 정책이고 주민은 "이웃에게 부탁한다" 한 지점에서만 의견을 묻습니다.
주민끼리는 서로 말을 걸지 않고, 보건소는 한 건만 받고, 119는 없습니다. 즉 **지금 구조에서
LLM을 붙여도 에이전트 하나가 한 번 대답할 뿐입니다.** 4·5·6단계가 채워야 할 빈칸이
무엇인지 이 표가 정확히 말해 줍니다.

**남은 단계:** 3 2단계 의사결정과 LLM 실제 연결 / 4 주민 간 상호작용 / 5 보건소 다건·소장 /
6 119 5단계 / 7 감도 분석 / 8 문서 19·23 경계 갱신.

## 2026-09-14 · MAS 재구축 4단계 — 주민이 주민에게 일을 넘긴다

doc 19이 **'자유 주민 채팅'과 '완전 자율 행동 LLM'을 이름으로 배제**하고 있습니다
([19_RESIDENT_AGENTS_AS_EVALUATORS.md:131](docs/research/19_RESIDENT_AGENTS_AS_EVALUATORS.md:131)).
그래서 이 단계는 대화가 아닙니다. 원자료의 5개 퀘스트에는 주민 간 대화가 하나도 없고
**일의 이동**만 있습니다 — 동행, 태워 줌, 가는 김에, 곁에 있어서. 이번에는 그중
**전가(relay)와 동석(notice)** 둘을 구현했습니다.

**왜 필요한가** — 주민이 말단 노드였습니다. `_offer`는 accept / decline / defer 세
갈래뿐이고 decline이면 곧장 `_unresolved`였습니다. 그런데 doc 19이 사례 1을 정의하며
쓴 말이 **"확인 업무의 전가"**이고 사례 2는 **"반복 부탁, 이웃 부담의 충돌"**입니다.
**연구 질문이 이름 붙인 현상을 엔진이 표현조차 못 하고 있었습니다.**

- **구현됨:** `RelationEdge`·`RelationRevision`·`InteractionRules` 계약,
  `relations.py`의 `rel-v1`, `ProposalAction.relay`, `request.relayed` /
  `world.copresence` / `world.relay_resolved` 이벤트, 엔진의 `_relay`·`_on_relay`,
  `ActorView.nearby`·`relations`, `Attempt.relationRevisionId`와
  `inputHashes["relations"]`.
- **MEDial은 전가를 보지 못합니다.** `request.relayed`의 visibility는
  `[넘긴 사람, 받은 사람, RESEARCHER]`입니다. MEDial이 듣는 것은 부탁받은 사람의
  `request.accepted` 하나뿐이고, 이후의 `task.*`도 보지 못합니다. 연구자 전용
  `world.relay_resolved`가 `reportedBy`와 `performedBy`를 따로 남깁니다.
  이미 있던 `world.reachability_resolved`("연구자 전용, 연락한 쪽은 '응답 없음'만
  안다")와 같은 관용구입니다.
- **부담 장부를 쪼갰습니다.** `asked_by_medial` / `asked_by_neighbour`.
  하나로 뭉쳐 있으면 "MEDial이 이웃 부담을 줄였다"가 거짓인 경우를 볼 수 없습니다.
- **관계 그래프는 고정 사례 입력입니다.** registry에는 `group` 소속만 있고 간선이
  없습니다. `cousin`은 **혼자 있는 그룹**이며 그 뜻인 P6–P12 간선을 데이터는 말하지
  않고, `solo`인 P3는 원자료에서 P9를 태워 주고 P11에게 약을 전달합니다. 13개 간선을
  전부 동행군 또는 5개 퀘스트 채록에 붙였고 `provenance`는 모두 `source-adapted`입니다.
  `FIXED_INPUT_PREFIXES`에 `relation`·`edge`·`interaction`을 추가했습니다 — Change Set이
  외로운 사람에게 친구를 만들어 주면 퀘스트는 닫히고 MEDial은 안 좋아집니다.
- **지어내지 않는 선:** 원자료에 간선이 없으면 넘길 수 없습니다. P1의 상대는 P6
  하나, P2는 P5 하나뿐이고 **그것은 모델의 공백이 아니라 원자료가 기록한 그대로입니다.**
  자기 자신에게, 이미 거쳐 간 사람에게, 확인 대상 본인에게는 넘길 수 없습니다.
  홉 상한 1이며 초과하면 제안 자체가 허용 목록에 오르지 않습니다.
- **검증:** `python -m pytest server/tests -q` 300 passed(신규 `test_relations.py` 24개),
  `npm test -- --run` 42 passed, 빌드·`git diff --check` 통과.

**실제로 돌려 본 장면 (14:00, subject P9, MEDial이 P12에게 부탁)**

```
14:00  request.offered    MEDial → P12    vis=MEDial,P12
14:00  request.accepted   P12             vis=MEDial,P12        ← MEDial이 듣는 전부
14:00  request.relayed    P12 → P6 (kin)  vis=P12,P6,RESEARCHER
14:10  request.accepted   P6              vis=P12,P6,RESEARCHER
14:21  task.check_performed  P6           vis=P6,P9,RESEARCHER  ← MEDial 없음
14:21  need.resolved                      byActorId=P12         ← MEDial은 P12가 했다고 안다
14:21  world.relay_resolved  reportedBy=P12 performedBy=P6  vis=RESEARCHER
```

12:30에 P6에게 부탁하면 다릅니다. 점심 시간에 **기록된 4인 동행군이 실제로 같은
장소에 있고**(`world.copresence` = P4·P6·P7·P8), P6는 동석자에게 넘깁니다
(`basis: copresent`). 그런데 그 사람도 점심 중이라 못 가고, **MEDial의 장부에는
수락으로 남은 채 아무도 가지 않습니다.** 이때 MEDial에게 주는 이유는
"수락 이후 확인이 완료되지 않았다"입니다 — MEDial이 스스로 도달할 수 있는 결론까지만.

**작업 중 발견한 것 두 가지**

| 발견 | 내용 | 조치 |
|---|---|---|
| **현재 두 덱에서는 전가가 한 번도 일어나지 않는다** | 정책 넷 모두 09:30에 이장 P6에게만 부탁하고, 그 시각 P6는 순찰 중(끼어들 수 있음)이라 **본인이 간다**. `relayed=0`. 전가는 12:30이나 14:00처럼 **다른 시각**이거나 **다른 사람**에게 부탁할 때만 발화한다 | 고치지 않았다. 메커니즘은 엔진 수준 검사로 고정했고, 실제 시나리오에서 보려면 덱/정책이 부탁하는 시각이나 대상이 달라져야 한다. 그건 사례 입력 변경이라 연구자 결정이다 |
| **집에 있는 사람은 아무도 끼어들 수 없게 되어 있다** | `INTERRUPTIBLE_PLACES = {"HOME", "PATROL", "HALL", "FARM"}`인데 체류 구간의 `place`는 `HOME:P1` 형태라 **`"HOME"` 항목은 영원히 매칭되지 않는다**. 결과적으로 순찰 중인 이장 말고는 사실상 아무도 수락할 수 없다 | **고치지 않았다.** 이전부터 있던 결함이고, 고치면 14:00에 P3·P9·P10·P11이 모두 가용해져 기존 모든 실행이 달라진다. 원자료의 "배정 가능 7인데 갈 수 있는 사람은 이장 하나"와 결과가 우연히 일치하지만 **이유가 다르다**(장소가 아니라 페르소나여야 한다). 사용자 판단이 필요하다 |

**아직 안 한 것**

- **동행(accompany)·가는 김에(piggyback)** 는 구현하지 않았습니다. 사용자가 이번
  범위를 relay + notice로 정했습니다.
- **동석(notice)은 절반입니다.** `ActorView.nearby`와 `world.copresence`는 있지만,
  근접이 *스스로* 무언가를 시작하게 하지는 않습니다. P7이 P8에게 2분 만에 간 Q4는
  6단계(119)에서 덱이 생겨야 표현됩니다.
- **전가가 화면에 보이지 않습니다.** `world.relay_resolved`와 부담 장부는 기록되지만
  비교·평가 화면이 읽지 않습니다. "MEDial은 1명에게 부탁했고 2명이 움직였다"가 아직
  연구자에게 보이지 않습니다. 1b의 `metrics["dayRealization"]`와 함께 묶어야 합니다.
- **기본 제안은 여전히 0초에 해결됩니다.** `_offer`는 같은 밀리초에 묻고 답을 받고
  출발합니다. 이번에는 전가 구간에만 10분 지연을 넣었습니다(연쇄가 한 밀리초에
  일어나는 것을 막기 위해). 기본 응답 지연은 별도 변경입니다.

**남은 단계:** 3 2단계 의사결정과 LLM 실제 연결 / 5 보건소 다건·소장 / 6 119 5단계 /
7 감도 분석 / 8 문서 19·23 경계 갱신.

**주의:** doc 19은 4·5·6을 "연구 질문이 요구하기 전에는 확장하지 않을 것"으로 두고
있습니다. 사용자 지시로 여는 것이며, 8단계에서 문서를 함께 고쳐야 어긋나지 않습니다.

## 2026-09-14 · 거절이 생겼다 — 자리 규칙, 거절 표, 이웃 우선 정책

지금까지 **갈 수 있으면 무조건 갔습니다.** 그래서 정책 비교가 "누구에게 물었나"만 재고
"부탁하는 비용"은 재지 못했습니다. 거절할 수 없는 마을에는 보고할 부담이 없습니다.

세 가지를 순서대로 했습니다. 순서가 중요했는데, 첫째를 고치기 전에는 둘째를 적용할
사람이 하루에 두세 명뿐이었기 때문입니다.

### (1) 자리를 뜰 수 있는지를 환경으로 — `env-v2`

`world.INTERRUPTIBLE_PLACES`에 `"HOME"`이 들어 있었지만 체류 구간의 장소는 `HOME:P1`
형태라 **그 항목은 한 번도 매칭되지 않았습니다.** 집에 있는 사람은 전원 "자리를 뜰 수
없음"이었고, 점심과 저녁에는 **12명 중 0명**이 가용했습니다.

이건 1단계에서 `PRIORITY`·`PHONE_REACHABILITY`를 환경으로 옮길 때 **빠뜨린 같은 범주**입니다.

- `AvailabilityRule`을 `EnvironmentRevision.availability`로. 해석 규칙(정확히 일치 →
  접두사 → 기본값)은 전화 표와 같습니다.
- **`env-v1`은 기존 동작을 결함까지 그대로 재현합니다.** 사유에 "의도한 규칙이 아니라
  구현상 결과이며 저장된 실행은 모두 이 조건에서 돌았다"고 적었습니다. 상수를 조용히
  고치면 지난 기록의 의미가 달라집니다.
- **`env-v2`가 한 줄만 바꿉니다** — `HOME:*` → 가용. 새 기본값입니다.
- **비교 목록에 빠진 것도 채웠습니다.** `environment`·`day`·`relations`·`modelPolicy`를
  `inputHashes`에 넣어 놓고 `CONTROLLED_INPUTS`에는 안 넣어서, **다른 세계에서 돌린 두
  실행이 여전히 통제된 쌍으로 읽혔습니다.** 1·2·4단계의 장치가 절반만 연결돼 있었습니다.

### (2) 거절 표 — 더하지 않고 줄 세운다

수치화해서 합산하는 방식은 쓰지 않았습니다. 3.2점이 "측정 2 + 추측 1.2"인지 "추측 3 +
측정 0.2"인지 사라지고, 점수는 잰 것처럼 보여서 결과가 그럴듯해질 때까지 조정하게
됩니다. D016이 전화 도달을 확률 하나로 뭉치지 않은 것과 같은 이유입니다.

**먼저 걸리는 줄이 이유가 됩니다.** 근거가 강한 것부터 묻습니다.

| 순서 | 줄 | 결과 | 근거 |
|---|---|---|---|
| 1 | 페르소나에 못 나간다는 조건이 있다 | 거절 | **인터뷰 (원자료)** |
| 2 | 지금 있는 곳에서 자리를 뜰 수 없다 | 넘기거나, 넘길 사람 없으면 보류 | 환경 (가정) |
| 3 | 오늘 이미 `helperContactCap`만큼 불렸다 | 거절 | 기존 정책 조건 (가정) |
| 4 | 거기까지 25분 넘게 걸린다 | 거절 | 거리는 도로망, 25분은 **가정** |
| — | 아무것도 안 걸림 | 수락 | |

- **줄 하나씩 끌 수 있습니다**(`enabledDeclineRules`). 어느 줄이 결과를 바꿨는지 보려면
  이게 필요하고, 합산 점수로는 못 합니다.
- **로그에 `rule`(키)과 `reason`(문장)이 따로 실립니다.** 화면은 "3.2점" 대신
  **"영업 중 가게를 비울 수 없다"** 를 보여 주고, 테스트·지표는 키로 맞춥니다.
- **25분은 지형에서 고른 값입니다** — 마을 안 도보는 최대 10.5분, 읍내 왕복은 30분.
  인터뷰 근거가 아니며 `note`에 그렇게 적힙니다.
- **오래 `unknown`이던 것 하나를 닫았습니다.** P12의 페르소나에 "가까운 친척의 부탁이면
  경로 효율과 무관하게 간다"가 있는데, 엔진은 요청이 친척을 통해 왔는지 알 수 없어
  `kinExceptionApplies: unknown`으로 남겼습니다. 4단계의 관계 그래프가 생겨서 이제
  답할 수 있습니다.
- **수락 확률은 넣지 않았습니다.** 변덕은 원자료에 없고, 변이는 이미 하루 실현(1b)에서
  seed로 들어옵니다. 감도 분석(7단계)이 결과가 지나치게 딱딱하다고 말하면 그때 맨 아래
  줄 하나에만 넣습니다.

### (3) `policy-C · 가까운 이웃 우선`

doc 19이 사례 1의 연구 대상으로 **"연락 순서"** 를 적어 두었는데 그걸 바꿀 정책이
없었습니다. `head_first`가 이장을 하드코딩했고(`rule_policies.py:179`), 엔진이 계산한
후보 목록은 `DecisionRecord`에 실려 화면에 표시된 뒤 **버려졌습니다.**

거절당하면 `neighbourAskLimit`(기본 3)까지 다음 사람에게 묻습니다. 한 명만 묻고 끝나면
"이장 대신 다른 한 명"일 뿐이라 순서를 연구할 수 없습니다.

### 실제로 돌려 본 결과 (실제 마을, 09:30 P1 무응답)

| 정책 / 환경 | 벌어진 일 | 건드린 사람 | 결과 |
|---|---|---|---|
| A 이장 우선 | 이장이 순찰 중에 들러 밭까지 찾아감 | **1명** | 해결 |
| C 이웃 우선 / env-v1 | P10 거절(가게) → P11 거절(가게) → P9 수락했으나 집에서 못 나감 → P12에게 넘김 → P12도 못 감 | **4명** | 미해결 |
| C 이웃 우선 / env-v2 | P10 거절 → P11 거절 → P9가 실제로 감 → P1은 밭에 있어 부재 | **3명** | 미해결 |

**이게 이번 작업의 결과입니다.** 지금까지는 정책을 바꿔도 "이장이 가서 해결" 하나뿐이라
비교할 것이 없었습니다. 이제 **부담이 한 사람에게 몰리는 것과 여러 사람에게 퍼지는 것의
교환 관계**가 화면에 나타납니다. 어느 쪽이 낫다는 판정은 하지 않습니다.

그리고 C/env-v2의 결말은 원자료의 Q3와 같습니다 — 사람은 갔고, 대상은 밭에 있었고,
확인은 오탐이었습니다.

**검증:** `python -m pytest server/tests -q` 332 passed(신규 `test_decline_rules.py` 23개),
`npm test -- --run` 42 passed, 빌드 통과. 새 결정 D075~D078.

### 고친 것 — `model_calls` 이름 충돌로 서버가 뜨지 않았다

2단계에서 만든 `model_calls` 테이블·메서드 이름이 **이미 쓰이고 있었습니다.**
`IterationTables`가 리뷰 세션의 모델 호출을 같은 이름으로 들고 있고 `Store`가
그것을 상속합니다.

- **기존 DB에서는 서버가 죽었습니다.** `CREATE TABLE IF NOT EXISTS`가 조용히
  아무것도 안 하고, 뒤따르는 인덱스가 `no such column: attempt_id`로 실패합니다.
  `run.sh`는 health 응답을 30초 기다리다 멈춥니다.
- **새 DB에서는 반대로 제 테이블이 이겨서** iteration 쪽 `CREATE`가 no-op이
  됐습니다. 그리고 `Store.model_calls()`가 `IterationTables.model_calls()`를
  **덮어써서** 리뷰 루프가 빈 목록을 받고 있었습니다.
- **테스트가 못 잡은 이유:** `Store(":memory:")`는 항상 새 DB라 인덱스가
  성공했고, `test_iteration.py`의 `len(store.model_calls(session.id)) == 0`은
  덮어쓴 메서드가 빈 목록을 돌려줘서 **통과하고 있었습니다.**
- 제 쪽을 `attempt_model_calls`로 바꿨습니다. 두 테이블이 공존하고 두 메서드가
  서로를 가리지 않는다는 회귀 검사를 추가했습니다.

실제 DB(51건 저장됨)에서 서버 기동·`/api/sim/health`·시도 생성까지 확인했습니다.

### 같은 종류를 다시 못 놓치게 — 구버전 DB 검사 + run.sh가 원인을 보여 주기

**(a) 실제로 오래된 DB 파일을 연다.** 지금까지 store 검사는 전부 `:memory:`였고,
그건 **항상 새 DB**입니다. "이미 있는 테이블 때문에 `CREATE`가 조용히 no-op이
된다" 같은 실패는 구조적으로 못 잡습니다.

`fixtures/legacy-db/simulation.pre-mas.sqlite3`를 커밋했습니다. 3d1660a 시점의
스키마를 **git에서 읽어** 만든 것이라 그 빌드가 실제로 만들던 모양이고, 안에 든
행 두 개(시도 1건, iteration 모델 호출 1건)는 전부 **지어낸 값**입니다 — 실제 마을도
실제 사람도 참조하지 않습니다.

`test_store_upgrade.py` 6개: 픽스처가 정말 구버전인지(아니면 검사가 무의미해짐),
열리는지, 기존 기록이 남는지, iteration 테이블이 자기 행을 지키는지, 업그레이드 후
쓰기가 되는지, 두 번 열어도 같은지.

**확인:** 고치기 전 스키마를 이 픽스처에 걸면 `no such column: attempt_id`로
실패합니다. 버그를 실제로 재현하는 픽스처입니다.

**(b) `run.sh`가 세 가지를 구분합니다.** 프로세스가 죽었다 / 아직 안 떴다 /
**떴는데 요청이 터진다.** 세 번째가 이번 경우였는데, 30초 동안 아무 말도 없다가
"로그를 보세요"로 끝났습니다. **원인은 그때 이미 로그 끝에 적혀 있었습니다.**

`http_ok`(예/아니오) 대신 `http_status`(상태 코드)를 씁니다. `000`이면 아직 안
뜬 것이라 기다리고, 5xx면 **그 자리에서 로그 마지막 25줄을 띄우고 끝냅니다.**
프런트 쪽도 같습니다.

### 아직 안 한 것 / 드러난 구멍

- **P9가 400m 오르막을 걸어 내려갔습니다.** 거리 규칙(9.7분)에 걸리지 않고, 페르소나에도
  경사나 접근성 조건이 없습니다. 원자료 인터뷰에는 "펜션이 완전 오르막 끝, 본인 운전
  안 함 → 양방향 접근 차단"이 있는데 **페르소나 registry에 옮겨져 있지 않습니다.**
  이건 제가 지어낼 수 없고 전사 확인이 필요합니다. 결함 하나가 다른 결함을 가리고
  있던 것이 이 수정으로 드러났습니다.
- **이동 경로의 거절은 아직 자기 표를 씁니다.** `does_not_drive` 같은 키가 `reason`에
  그대로 들어가서, 안부 확인 경로의 `reason`(문장)과 뜻이 다릅니다. `rule`은 양쪽 다
  실리므로 기계 매칭은 일관되지만, 문구 통일은 3단계에서 해야 합니다.
- **기본 제안은 여전히 0초에 해결됩니다.** 전가 구간에만 10분 지연이 있습니다.

**남은 단계:** 3 2단계 의사결정과 LLM 실제 연결 / 5 보건소 다건·소장 / 6 119 5단계 /
7 감도 분석 / 8 문서 19·23 경계 갱신.

## 2026-09-15 · 로그에만 있던 셋을 화면에 — 거절 사유, 전가, 뽑힌 하루

원칙 그대로: **뒷단은 엄밀하게, 화면은 읽기만.** 노브는 하나도 늘지 않았다 (D079).

### 뒷단 — 셋 다 커밋된 사건에서 지표로 계산한다

| 지표 | 어디서 | 왜 엔진 상태가 아니라 사건에서 |
|---|---|---|
| `metrics.handovers` | `request.relayed` + `world.relay_resolved` | `engine.relay_chains`는 MEDial이 차례로 물은 사람도 담는다. P10→P11→P2를 MEDial이 순서대로 물은 것은 전가가 아니다. 행마다 `askedActorId`(MEDial 장부가 믿는 사람)·`chain`·`performedBy`(실제로 간 사람, 아무도 안 갔으면 null)·`outcome` |
| `metrics.refusals` | `request.declined` / `request.deferred` | 행마다 `rule`(기계 키)·`reason`(문장)·`seenByMedial`(그 사건에 MEDial이 수신자였는가). `byRule` 은 규칙별 횟수 — 합산 점수가 아니라 셈 |
| `residentBurden[*].askedByMedial / askedByNeighbour` | `ActorRuntime` 장부 | 4단계에서 갈라 둔 것을 지표까지 올렸다 |
| `metrics.dayRealization` | 1b 그대로 | 이미 있었다. 읽는 쪽만 없었다 |

`compare()`의 `outcomeDifference`에도 `handovers`·`refusals` 건수와 `dayRealization`을 실었다.
예전에 저장된 실행에는 이 키가 없고 다시 계산하지 않으므로 프런트 타입은 선택(optional)이다.

### 화면 — 세 곳, 전부 읽기

| 화면 | 보이는 것 | 안 보이는 것 |
|---|---|---|
| 관찰 · 조율 패널 | **누구에게 부탁했고, 뭐라고 했나** — 물은 순서대로 한 줄씩: 이름 · 수락/거절/나중에/넘김 · 그 사람이 남긴 **문장**. 이웃이 대신 부탁한 줄은 "← 이장이 부탁". MEDial이 수신자가 아니었던 줄엔 `MEDial은 모름` | 규칙 키(`persona_condition`). 원 사건 목록에만 있다 |
| 관찰 · 일어난 일 | `request.relayed` → "P12가 이장에게 P9 확인을 넘겼습니다. MEDial은 이것을 모릅니다." / `world.relay_resolved` → "MEDial은 P12가 확인했다고 알고 있지만, 실제로 간 사람은 이장입니다." | 둘 다 **MEDial이 아는 것** 관점에서는 사라진다 — 화면이 감추는 게 아니라 `visibility` 필터가 거른다. `world.copresence`는 부기(bookkeeping)로 접힘 |
| 관찰 · 지도 헤더 | 하루 태그: `기록된 하루 그대로` / `기록된 시각이 조금 다른 하루` / `외출 하나가 다른 하루`. 마우스를 올리면 사람별 편집 목록 ("P1 · FARM 09:00 → 09:12 (12분 늦게)") | |
| 관찰 · 사람 | 받은 연락 N건 **(MEDial n · 이웃 m)** — 이웃 몫이 있을 때만 | |
| 비교 | 새 행 **어느 하루였나** (양쪽 하루 이름 + 바뀐 것 N개 접기), **누가 거절했나** (사람 · 거절/나중에 · 문장), "누구의 일과가 바뀌었나"에 "MEDial이 부탁한 사람 X명 · 이웃이 대신 끌어들인 사람 Y명 — MEDial은 모름". `달라진 입력` 은 `day`→"뽑힌 하루" 처럼 우리말로 | 점수. 규칙 키 |

정책 낱말(`neighbour_first`→"가까운 이웃에게 먼저", `neighbourAskLimit`→"한 건에 이웃 몇 명까지
물어보는가", 입력 이름, 규칙 이름, 하루 이름)은 `selectors/words.ts` 한 곳으로 모았고,
비교 화면·준비 화면·정책 편집기에 각각 있던 사본은 지웠다.

### 확인한 것

- 서버 343 · 프런트 52 · `tsc` · `vite build` 통과.
- 실제 마을 policy-C: 거절 2건 (P10·P11 "영업 중 가게를 비울 수 없다", 둘 다 MEDial이 봄),
  전가 0건, MEDial이 부탁한 사람 3명, 하루 `plausible_extension`(편집 41개).
- `fixtures/ui/`에 합성 마을 policy-C 실행(`attempt-neighbours.json`, `events-neighbours.json`)을
  추가해, 조율 패널이 거절 **문장**을 찍고 규칙 **키**는 찍지 않는 것을 mount 검사로 고정.
  전가는 실행 중인 덱에서 안 일어나므로 selector 단위 검사(`handoffLog`)로 고정:
  연구자 관점엔 `relayed → accepted(← P12)`, MEDial 관점엔 `accepted` 하나.
- **Playwright로 확인했다** (아래 절). 조율 패널은 `P10 [거절] 영업 중 가게를 비울 수 없다 /
  P11 [거절] … / P9 [수락]` 로 읽힌다. 비교 표에서는 첫 실행에서 두 가지가 틀려 있었고 고쳤다:
  (1) 실행 중인 버전을 열면 전부 미수집인데 "하루 기록 없음 (예전 실행)"이라고 했다 → 시도가
  없으면 다른 행처럼 미수집; (2) 세대 `metrics.objective`가 지표의 일부만 옮기고 있어서
  `dayRealization`·`refusals`·`handovers`가 없었다 → 옮긴다(하루는 `steps` 를 뺀 형태).

### 화면은 Playwright로 본다

```
npm run e2e          # .run/e2e/ 에 별도 DB, 합성 마을·페르소나, API 8011 · vite 5174
```

`playwright.config.ts`가 두 서버를 띄우고 `e2e/screens.spec.ts`가 세 화면을 지나간다:
policy-C 실행을 만들고 관찰 화면에서 거절 문장을 읽는지, 준비 → 실행 → 비교에서 표가
채워졌는지. 스크린샷은 `.run/e2e/screens/*.png` (gitignored — 합성 마을이라 원자료는 없지만
저장소에 그림을 쌓지 않는다). 패널이 내부 스크롤이라 "누구에게 부탁했고" 구역은 따로 찍는다.
연구자의 `local-data/runs/` 는 건드리지 않는다.

### 기본 하루는 같은 일과의 반복이다 — `env-v3`

D080. 실제 마을에서 기본(env-v2)으로 돌리면 하루가 거의 매번 `plausible_extension`(편집 41개)
으로 뽑혔다. 생략 확률 0.15가 12명 × 외출 수에 걸리니 예외가 기본이 된 것이다. 연구자의
전제는 반대다 — 마을은 매일 같은 일상을 반복하고 바리에이션은 약간뿐. 그래서 `env-v3`:
출발 시각 ±10분, 생략·반복 0. 하루 태그는 `기록된 시각이 조금 다른 하루`로 뜬다.
v2는 이름으로 남아 있고(저장된 실행의 뜻을 바꾸지 않는다), 외출이 빠진 하루를 보고 싶으면
v2를 골라야 하며 그 쌍은 통제 비교가 아니다.

## 2026-09-15 · MAS 재구축 3단계 — 마을이 모델로 말한다

연구자 결정 (D082–D085): MEDial 머리는 `gemini-3.8-flash`, 주민은 `gemini-3.1-flash-lite`.
머리는 후보까지 직접 고르고, 주민은 LLM이 전부 정하며 거절 표는 기록만 한다.

### 어디서 누가 말하나

```
MEDial 머리 (3.8-flash)                          주민 (3.1-flash-lite × 12)
① read  : 관측 → 상황 읽기 (confirmed엔 사건 id 필수)   ③ 부탁을 받음: 내 일과·내 인터뷰 조건·
② decide: 후보 전원 판정 + askOrder + 부탁 말          부탁 말(message) → accept/decline/defer/
   ↳ 울타리 검사, 어기면 1회 되돌림                      relay/report_observation + 한마디
        │ message ────────▶ request.offered.payload.message ──▶ 주민 observation
        ◀── utterance ───── request.accepted/declined.payload.utterance ◀── 주민의 답
```

말은 전부 **사건에 실려** 오간다. 옆길 채팅은 없다(문서 19의 "자유 주민 채팅 제외"는 그대로다).
머리의 다음 `read`는 MEDial이 수신자였던 사건만(`_medial_events`) 본다 — 전가·아래쪽 거절·
세계의 이유는 visibility 필터 하나로 걸러진다.

| 자리 | 코드 | 모델 |
|---|---|---|
| 상황 읽기·다음 행동·자택 부재 뒤 | `policies/llm_policy.py` | 머리 |
| 주민의 답 | `agents/llm.py` | 주민 |
| 재연락 예산 소진 뒤 인계, 이동(동승) 요청, 보건소 담당자 | 규칙 그대로 | — |
| 인계 기한(`escalateToInstitutionAfterMin`) | 코드가 먼저 본다. 머리에게 "기한을 지킬지" 묻지 않는다 | — |

### 울타리 — 노브는 고른 뒤에 검사한다

`_fence()`: 후보 목록에 전원이 있는가 · 본인에게 부탁하지 않았는가 · 이장은 `allowHeadContact`일 때만 ·
`neighbourAskLimit` 이하 · `head_first`면 첫 사람이 이장 · `retry_then_clinic`이면 이웃 부탁 금지 ·
`schedule_contact`는 `retryCount` 안에서 · `wait`는 `waitMinutes` 5~120, 한 건 두 번까지.
어기면 `rejected.problems`를 붙여 한 번 다시 묻고, 두 번째도 어기면 `medial.waiting adapter_error`
(actorId=MEDial). **규칙 정책의 답으로 갈아치우지 않는다.**

### 기록·재생

`ModelPolicy`에 `headModelId`·`residentModelId`가 따로 있고 호출 키는 역할별 모델로 만든다.
머리 모델을 바꿔도 주민 기록은 그대로 재생된다. 분기는 체크포인트 앞의 머리·주민 호출을 모두
재생한다(`test_a_fork_replays_both_tiers_without_asking_again`). 마을의 호출은 반복 루프의
`callsUsed`에도 합산된다(재생분 제외).

### 설정과 켜는 곳

- `server/.env`: `MEDIAL_LLM_HEAD_MODEL` / `MEDIAL_LLM_RESIDENT_MODEL` (기본값이 위 둘),
  `MEDIAL_LLM_TEMPERATURE`(0.2), `MEDIAL_LLM_MAX_RETRIES`(6). 첫 llm 실행 전에 공급자 모델 목록으로
  두 id를 확인하고 없으면 시작하지 않는다.
- 화면: 실험 준비 → "마을의 행동 (MEDial 머리 + 주민)" 하나. 키가 없으면 비활성. 새 노브 없음.
- 관찰 화면: `medial.classified`에 머리의 `summary`가 있으면 "MEDial의 상황 읽기: …"로, 부탁 말은
  인용으로, 표와 어긋난 주민 답에는 `규칙표라면 수락` 태그.

### 실제로 돌려 본 것 — 합성 마을, 실제 Gemini (`python scripts/llm_smoke.py`)

| 시도 | 결과 |
|---|---|
| 3.8-flash 머리, `read` | **성공** (10.7s). "1회 연락 시도, 응답 없음. 위치·건강 상태를 확인할 근거 없음 → 모두 unconfirmed." 원칙대로 |
| 3.8-flash 머리, `decide` | 503 "high demand" 반복 → 이후 **429: 무료 등급 하루 20회** (`GenerateRequestsPerDayPerProjectPerModel-FreeTier`, quotaValue 20). 오늘 소진 |
| 양쪽 3.1-flash-lite | read·decide·wait 재판단까지 흐름 확인. 첫 실행에서 `wait`에 깨우는 장치가 없던 것을 발견해 고침(D084). 두 번째 실행은 wait 뒤 재판단에서 503 |

| 새 키, 3.8-flash 머리 + flash-lite 주민 (1차) | read·decide 성공 → P9 수락 → 집 방문, 부재 → **P9에게 "어디 있을지"를 물었는데 P9는 아는 곳이 없어** 빈 답 → 어댑터 실패. 엔진이 지역 지식이 없는 사람에게 그 질문을 던진 것이 원인 |
| 같은 조건 (2차, 고친 뒤) | **끝까지 돎.** read → decide(P9·P10·P11 순, retryCount 0이라 재연락 없음을 스스로 언급) → P9 수락 → 부재 → 확인처 근거 없음으로 미해결. 호출 3회, 실패 0 |

고친 것: 자택 부재 뒤 "어디 있을지"는 그 사람의 일과를 장소 단위로 아는 사람(지금은 이장)에게만
묻는다. 모르는 사람에게는 묻지 않고 바로 "근거 없음"으로 간다
(`test_only_someone_who_knows_the_persons_day_is_asked_where_they_would_be`).

2차 실행이 **3.8-flash로 하루를 끝까지 돌린 첫 기록**이다. 한 번이고 합성 마을이다 — 여러 번
돌려 흔들림을 본 것은 아니다. 무료 등급이면 3.8-flash는 하루 20회라 한 실행(머리 3~5회)을 몇 번
못 돌린다.

### 반복 실행과 반복 루프 — 실제 모델 (2026-09-15, 유료 키)

**같은 하루 15번** (`python scripts/llm_repeat.py`, 합성 마을, seed 17, A·B·C × 5).
3.8-flash 머리 호출 60회 이상에 429 없음 → 유료 등급 확인.

| 정책 | 모델 머리 (5회) | 규칙 머리 (같은 하루) |
|---|---|---|
| A 이장 우선 | 5/5 이장 → 집 부재 → 이장이 밭 제안 → 해결. 첫 판단의 후보 순서는 `[P6]` 3회, `[P6,P9,P10]` 1회, 한도 넘김 1회(되돌림 후 통과) | 같음 |
| B 재연락 후 보건소 | 5/5 재연락 예약 → 인계 → 보건소 전화에 본인 응답 | 같음 |
| C 이웃 우선 | 5/5 **P9를 먼저** → 수락 → 집 부재 → 미해결. 한 번 이장을 4번째로 넣어 한도 넘김(되돌림 후 통과) | P10 거절 → P11 거절 → P9 수락 → 부재 → 미해결 |

어댑터 실패 0, 되돌림 2회(둘 다 `neighbourAskLimit` 초과), 주민 답이 규칙표와 어긋난 것 0.
C에서 모델 머리는 자리를 비울 수 없는 P10·P11을 건너뛰어 **거절 두 번을 만들지 않았다.**

**반복 루프 2세대** (`python scripts/llm_iteration.py`, 행동·리뷰·개선 모두 llm). 끝까지 돎:
291초, 루프 호출 34회 + 마을 호출 10회, 실패 0. 확정은 스크립트가 첫 유효안으로 대신했다
(연구자 판단 아님). 리뷰는 경험한 사람(P1·P6)만 등급을 매겼고 나머지는 `no_experience`.
종합은 근거 없는 주장(P1의 "왕래 없는 이웃")을 잡았지만, **근거가 있는 것 하나(P6의 밭 제안 —
`medial.observed local_knowledge` 사건)도 근거 없음으로 잘못 분류했다.**

**드러난 구멍 — `retryCount`가 `head_first`에서 아무 일도 안 한다.** 1세대 Change Set
"이웃 요청 전 1회 재호출"(`retryCount 0→1`)이 기계 검증을 통과하고 확정됐지만, 2세대 결과는
부모와 한 글자도 다르지 않았다. 규칙 머리로 같은 변경을 돌려도 똑같다 — 재연락은
`retry_then_clinic`에서만 실행된다. 모델 머리는 울타리(`retryCount`는 상한)만 지키면 되니 역시
재연락 없이 이장에게 갔다. 즉 **검증기는 실행되지 않는 변경을 "valid"로 통과시키고, 비교 화면은
"정책만 다르게 실행했다"며 차이 0을 보여 준다.** 디자이너는 "재연락은 효과가 없다"고 읽게 된다.
→ **고침 (D086, 같은 날).** 아래 "연락 순서 정리" 참조.

### 연락 순서 정리 — 재연락이 먼저, 그리고 정책 D (D086·D087)

- **재연락은 모든 연락 순서의 첫 단계.** `retryCount`만큼 `retryIntervalMin` 간격으로 본인에게
  **전화**하고, 그래도 닿지 않으면 그 정책의 다음 단계로 간다. 규칙 머리·모델 머리 모두
  `MedialPolicy._retry_due`가 먼저 처리하고, 모델 머리의 `decide`는 재연락이 끝난 뒤에만 불린다
  (`schedule_contact`는 머리의 선택지에서 빠졌고, `policy.retriesAlreadyMade`로 알려 준다).
  `prompt-v3`, engine `medial-sim/0.3.0`.
- **정책 D (`relation_first`)**: 재연락 1회 → 기록된 가까운 관계 → 이장. 모델 머리에게는
  `people[].recordedRelationToSubject`를 주고, 울타리가 "관계 없는 사람 금지 · 이장은 마지막"을 본다.
- **실험 준비 화면 문구**도 이 순서대로 말한다 ("응답이 없으면 먼저 40분 간격으로 본인에게 1번 더
  전화합니다. 그래도 닿지 않으면 …"). 같이 고친 것: `helperContactCap`(한 사람이 하루에 받는 부탁
  상한)을 "이웃 최대 N명"으로 읽던 문장.

P1 사례, 같은 하루 (seed 17, 합성 마을):

| 정책 | 규칙 머리 | 모델 머리 (3.8-flash, 3회) |
|---|---|---|
| A 이장 우선 | 09:30 이장 → 해결, 이장 18.8분 | 3/3 같음 |
| C 이웃 우선 | P10 거절 · P11 거절 · P9 → 미해결 | 3/3 P9 먼저 → 미해결 (되돌림 1회) — **이 표는 아래 "빈집 다음" 수정 전** |
| D 재연락 → 관계 → 이장 | 10:10 전화도 무응답 → 이장 → 해결, **이장 23.0분** | 3/3 같음 |

**D는 P1에게 A와 같은 사람(이장)에게 간다.** P1의 기록된 관계가 이장 하나뿐이기 때문이다. 달라진
것은 40분 늦은 확인과, 그 사이 P1이 더 멀리 가 있어 이장의 수고가 4.2분 늘어난 것이다. 재연락이
이웃의 시간을 아껴 주는 경우는 본인이 두 번째 전화를 받을 때인데, 이 하루의 P1은 밭에 있어 받지
않는다. 즉 이 사례에서 "누구의 시간으로"의 답은 순서가 아니라 **P1의 관계가 이장 하나에 걸려
있다**는 기록이 정한다.

### 빈집 다음 — 짐작 가는 곳이 없으면 이장에게 묻는다 (D088, engine 0.4.0)

정책 C가 늘 미해결로 끝난 이유는 순서가 아니라 **구멍**이었다. P9가 빈집을 확인하면 그 자리에서
요청이 닫혔고, P1의 평소 일과를 아는 이장은 한 번도 물어지지 않았다.

- 빈집을 확인한 사람에게 장소 단위 지식이 없으면, MEDial이 **"자택에 없고 짐작 가는 곳도 없을 때
  누구에게 물을 것인가"** 를 판단한다 (`on_absent_no_lead`). 규칙 머리: `allowHeadContact`이고 이장이
  아직 이 건에 물어지지 않았으면 이장에게 **전화로 어디 있을지 묻는다** (`ask_whereabouts`,
  `request.offered purpose=whereabouts`). 모델 머리: 같은 질문을 `no_lead` 단계로 받고
  `ask_whereabouts · handoff · stop` 중 고른다. 울타리는 같은 두 조건을 본다.
- 이장이 장소를 대면 기존 흐름(`medial.observed local_knowledge` → `on_absent_report` → 그곳으로)을 탄다.
  이장에게도 짐작이 없으면 `medial.observed no_local_knowledge`를 남기고 미해결.
- 물어본 것은 이장의 **연락 1회**로 부담에 잡히고, 걸어간 시간은 가서 확인할 때만 잡힌다.
- MEDial이 수신자가 아닌 방문(주민끼리 넘긴 일)은 전과 같이 끝난다 — MEDial은 빈집을 모른다.

P1 사례, 같은 하루, 고친 뒤:

| 정책 | 규칙 머리 | 모델 머리 (3.8-flash, 3회) |
|---|---|---|
| A 이장 우선 | 이장 → 집 → 밭 → 해결. 이장 18.8분 (연락 1) | 앞과 같음 |
| C 이웃 우선 | P10 거절 · P11 거절 · P9 빈집 → **이장에게 전화** → 밭 → 해결. P9 11.7분 · 이장 10.5분, **4명에게 연락** | 3/3 P9 → 빈집 → 이장에게 물음 → 해결. P9 11.7분 · 이장 10.5분, **2명** |
| D 재연락 → 관계 → 이장 | 10:10 전화 → 이장 → 해결. 이장 23.0분 | 3/3 같음 |

이제 C도 해결되지만 공짜가 아니다: 이장 한 사람의 18.8분이 두 사람의 22.2분으로 나뉘었고, 규칙
머리는 거절할 사람 둘에게도 물었다. 모델 머리는 가게를 비울 수 없는 P10·P11을 건너뛰어 두 명만
건드렸다. **"이장에게 몰리지 않게"를 목표로 한 C도 결국 이장의 지식이 있어야 끝난다** — 이것이 이
사례의 발견이다.

### 화면에서 고친 것 (Playwright로 확인)

`e2e/screens.spec.ts`에 정책 D 관찰 검사를 더했고, C 검사에 빈집 다음 단계를 더했다. 캡처를 직접 보고 고친 것:

| 보인 것 | 고침 |
|---|---|
| "이렇게 정한 이유"가 마지막 판단 하나뿐 — D에서 "밭에서 계속 확인"만 보이고 **왜 이장에게 갔는지**가 가려짐 | 한 요청의 판단 이유를 시간순으로 모두 (`reasonsInOrder`) |
| 판단 문장이 "다음 상대로 이장을 골랐습니다" 두 번 — 질문이 달라도 같은 문장 | "MEDial의 판단 — {질문}: {선택}", 재연락이면 "P1 본인에게 다시 연락" |
| "이장이 PATROL에서 FARM로 떠났습니다", "(neighbour_visit)", 결정 사유 "확인처(FARM)" | 장소는 마을의 이름(`learnPlaces`/`placeWord`), 해결 경로는 말로(`pathWord`, 비교 표 포함), 서버 사유도 "밭" |
| "안내 시계 연락했습니다", "P1는" | "안내 시계로/전화로 연락했습니다", 조사는 `withParticle` |
| 이장에게 어디 있을지 물은 줄이 "답 기다리는 중"으로 남음 | `told` — "어디 있을지 답함" + 이장의 말 |
| 비교 화면에 버전이 하나뿐일 때 v0 대 v0를 "통제 비교가 아닙니다"로 경고 | "같은 버전을 양쪽에 골랐습니다 · 아직 비교할 다음 버전이 없습니다" |
| D 결정 사유가 "관계 중 지금 부탁할 수 있는 사람이 없어" — 사실은 관계가 이장뿐 | 두 경우를 나눔: "기록된 가까운 관계는 이장 한 사람뿐" / "관계는 있지만 지금은 부탁할 수 없다 (사람별 이유)" |

### 채록 장부 — 물었고 없는 것과 안 물어본 것 (D090)

일반화 검토에서 첫째로 꼽힌 문제: 관계·지역 지식·연락 조건이 손으로 옮겨졌고, 그 빈칸이 코드
안에서 '없음'으로 읽혔다. `server/app/simulation/ledger.py`와 `docs/research/25_ELICITATION_GUIDE.md`.

- 사람×항목 다섯 개(`companions` · `help_contacts` · `routine_knowers` · `reachability` ·
  `decline_conditions`)의 상태 넷. 마을 파일 옆 `ledger*.json`, 없으면 `ledger-legacy`.
- "누가 누구의 평소 장소를 아는가"가 장부의 `routineKnowledge`로 옮겨졌다. 이장 하드코딩
  (`_local_knowledge`, `_build_routines`, 빈집 다음 단계)이 사라지고, 규칙 주민 어댑터 누구나
  장부가 아는 사람이면 "어디 있을지"를 답한다. `RuleVillageHeadAdapter`는 이름만 남았다.
- 정책 D 사유와 빈집 다음 판단이 상태를 말한다. `metrics.elicitation` = 관련된 사람의 공백 +
  결정이 기댄 가정. 비교 화면 "기록에 없던 것" 행 (Playwright `compare-gaps.png`).
- 합성 장부 `fixtures/synthetic/ledger.synthetic.json`(원자료 구조를 흉내 냄). 실제 마을 초안
  `local-data/normalized/ledger.v1.json`(git 제외, **연구자 검토 전**): 함께 다니는 사람만
  `recorded/asked_none`, 나머지 대부분 `not_asked`, 이장의 지식은 동행군·사촌·Q3만
  `source-adapted`이고 P1·P2·P3·P5·P9·P10·P11은 `researcher-assumption`.

### 시뮬레이터 검사 세 가지 — 노브·하루·모델 주민 (D091·D092)

일반화 검토 ②③④. `server/app/simulation/audit.py`(순수 함수, 엔진 변경 없음) + `scripts/audit.py`
(`.run/audit/`에 씀) + `scripts/llm_shadow.py` + `test_audit.py` 5건. 결과 표와 해석은 연구노트 15장.

- **노브 효과** `knob_effects`: 지원 노브 10개 × 정책 6개, 값을 뒤집고 같은 하루(seed 17, env-v3-fixed)의
  로그 지문(`log_fingerprint`)을 비교. 정책 id는 그대로 두고 돌린다 — 로그의 `medial.decided`에 policyId가
  실려서 이름만 바꿔도 지문이 달라진다. 읽을 수 없는 칸(재연락 0회의 간격, head_first의 allowHeadContact)은
  이유를 적고 `logChanged=None`. 죽은 노브 0. 표는 `test_audit.py`의 `EXPECTED`에 고정.
- 검사 중 고친 것: `helperContactCap`이 동승 부탁(`_on_ride_offer`)에서 읽히지 않았음 → 안부 확인 표와 같은
  `asked_too_often` 줄 추가. `catalog().policyFields.strategies`가 넷 중 둘만 → `ContactStrategy` 전부.
  `registry.py`의 "모든 조건이 두 덱에서 읽힌다" 주석 → 사실대로. runner의 죽은 `adapters` 블록 삭제
  (service가 덮어쓰고 있었다). 수락 사건에도 `ruleTableSaid`를 최상위에 실음(전에는 `params` 안에만).
- **안정성** `stability`: seed 1–20(env-v3, ±10분)에서 정책별 최소/중앙/최대와, 중앙값 순서가 유지된 날 수.
  D는 3/20일 미해결(seed 3·7·15: 10:10 이장이 순찰 중 자리를 못 떠 P4에게 넘기고 P4 유예, MEDial 장부엔
  수락). 이동 덱은 20일 모두 같은 숫자 — 하루 변이가 동승 결정에 안 닿는다.
- **모델 주민 vs 표** `table_disagreement`(llm_repeat 행) + `shadow_summary`(llm_shadow 행). 모델 머리 아래
  20/20 일치는 전부 수락 칸. 그림자(규칙 마을을 돌리며 같은 view로 모델 주민에게도 묻고 기록만) 82문항:
  수락·관측 56/56 일치, 거절·유예·전달 26건 중 모델이 안 된다고 한 것 1건. D083 유지, prompt-v5는 연구자 결정.
- **종합 오분류** `synthesis_sheet` → `.run/audit/synthesis-labels-<세션>.json`에 `humanLabel`/`draftLabel`.
  비율은 사람 라벨로만; AI 초안 기준 13개 중 2개(근거 없음 6 중 2가 사실은 근거 있음).
- 연구노트의 결과 표마다 engine·prompt·마을 해시를 달았다(11.2·11.3은 engine 0.2.0·prompt-v2, 11.4·15장은
  0.4.0·prompt-v4).

### 아직 안 한 것

- 모델 주민이 거절 조건을 알면서도 수락·전달하는 문제(15.3)는 재기만 했다. 고치려면 prompt-v5인데, 표 쪽으로
  조정하는 것이라 전사 대조(식당 부부가 실제로 "못 간다"고 했는지) 뒤 연구자가 정한다.
- 종합 오분류의 사람 라벨은 아직 0개다. 시트의 `humanLabel`을 채우면 `scripts/audit.py`가 사람 기준 비율을 낸다.
- 이동 덱의 감도 분석은 하루가 아니라 다른 입력(운전 가능 여부 등)을 흔들어야 한다.
- 실제 마을 장부 초안은 익명본 페르소나 JSON의 '관계'·'하루' 필드만 보고 적었다. 전사 확인 후 확정.
- 연락 조건·거절 조건은 장부에 상태만 있고, 값은 여전히 `environment.py`·`decline_rules`에 있다.
  장부에서 읽도록 옮기는 것은 하지 않았다.
- 이동(동승) 요청과 보건소 담당자는 규칙. 5단계에서 보건소를 키울 때 같이 본다.
- 이장에게도 짐작이 없는 경우(`no_local_knowledge`)는 코드 경로와 화면 문구만 있고, 이 덱에서는
  일어나지 않아 실행으로 확인하지 못했다.
- 모델 머리가 D에서 "관계가 이장뿐"을 스스로 말하는지는 확인하지 않았다(규칙 머리의 사유만 화면 검사).
- 이장은 장소를 알면서도 늘 집부터 간다. A·D의 이장 이동 시간에 그 헛걸음이 들어 있다.
- 머리가 `adapter_error`로 넘어지면 그 건은 그대로 끝난다. 시간을 두고 다시 묻는 장치는 없다 — "실패는 실패"를 지키기 위해 일부러 두지 않았다.
- 문서 19의 "주민의 완전 자율 행동 LLM 제외"는 유지된다: 주민 LLM은 허용된 제안 안에서만 답한다(자유 이동·자유 대화 없음). 8단계에서 문구를 손본다.

### 1:1:1 작업 중 검사로 발견해 고친 것

| 발견 | 문제 | 조치 |
|---|---|---|
| needs_decision인데 고를 것이 없다 | 갈라진 후보는 **부모 세대**의 proposal인데 화면은 오른쪽 열의 세대에서 읽었다. 기본 쌍은 자식이 오른쪽이므로 항상 빈 목록 → 결정이 필요한 상태에서 다음 행동이 아무것도 안 보였다 (15번 9장 D 위반) | 후보를 session 전체에서 찾는다(`decisionPoint`). 그래도 없으면 상태와 갈 곳을 말하는 안내를 띄운다. 회귀 검사 2건 |
| "이장가 하겠다고 했습니다" | 조사를 문자열에 그대로 붙였다 | `withParticle()`이 받침으로 고른다. 끝자리 숫자는 읽는 소리 기준 — P1은 '일'이라 "P1이", P2는 '이'라 "P2가" |
| 같은 사람이 패널마다 이장/P6 | 패널마다 `=== 'P6' ? '이장'` 을 따로 갖고 있었다 | `story.personName()` 하나로 통일하고 전 패널이 그것을 쓴다 |
| "상대가 수락했습니다" | 수락한 사람은 사건의 `actorId`인데 `toActorId`를 읽었다. 응답 사건에는 그 필드가 없어 항상 "상대" | 응답자는 actor에서 읽는다 |
| UI fixture에 원자료가 들어갈 뻔함 | mount 검사용 fixture를 실행 중인 서버(원자료 레지스트리)에서 curl로 받았다 | 합성 마을에서만 생성하도록 `scripts/gen_ui_fixtures.py` 로 교체 |

## importer가 남긴 데이터 이슈

6건 중 2건 해결, 4건 미해결입니다. 화면 하단 패널에 상태와 함께 나옵니다.

### DI-001 해결 — 마을의 긴 축은 남북이다
방위를 서술하지 않고 측정합니다. P9 자택→미가식당 5.3°(정북) 1111 m, 동서 381 m · 남북
1153 m. 원본 시뮬레이터 산문과 일치하며, 오류는 후속 분석 문서 두 곳에 한정되어 정정했습니다.

### DI-007 해결 — 두 거리는 둘 다 맞고, 섞지만 않으면 된다
간선 350개 전부 원본 폴리라인의 연속 정점 쌍임을 import 시점에 검증하고, 아니면 실패시킵니다.
구간 거리는 `sourceM`(원본이 그린 경로)과 `graphM`(전체 도로망 최단 경로)을 둘 다 보관하며,
이동 사건이 `distanceBasis` 로 어느 쪽인지 밝힙니다.

남은 4건(DI-002 overlay 유지, DI-003 지름길 미이식, DI-004 읍내 30분 고정값, DI-006 조업 구역)은
원자료나 기관 확인이 있어야 닫히므로 `open` 입니다.

---

# 부분 구현

- **기관 역할** — 담당자 1명의 일정·근무시간·대기열은 실제로 동작합니다. **보건소장, 여러 건을
  동시에 안고 있는 실무자의 자원 배분, 다건 우선순위**는 없습니다.
- **LLM 어댑터** — 리뷰·종합·개선 역할과, 3단계부터 MEDial 머리·주민의 행동이 Gemini를
  부릅니다. 이동(동승) 요청·보건소 담당자·재연락 소진 뒤 인계는 여전히 규칙입니다. 실제 모델로
  하루를 끝까지 돌린 기록은 합성 마을 1회(2026-09-15)이고, **리뷰·개선 루프를 LLM 마을 위에서
  실제 모델로 돌린 적은 아직 없습니다.**
- **읽기 전용 주민 대화** — 규칙 기반 발화만 있습니다. 자유 대화 UI는 없습니다.

# 아직 구현하지 않은 것

이번에 하지 않은 것을 완료라고 쓰지 않습니다.

- **119** — 접수/출동/도착/인계를 분리해 모델링해야 하고, 도착 자체를 의료 해결로 세지 않아야
  합니다. 지금은 아예 없습니다. 실제 기관 전송도 물론 없습니다.
- **다일 기억** — 하루 단위 실행만 있습니다. `longitudinal` 모드는 이름만 있고 session
  생성이 400으로 거부합니다.
- **개입 대화 분기** — 읽기 전용 probe와 세계를 바꾸는 개입 대화의 구분이 설계에만 있습니다.
- **보건소장·다건 우선순위** — 담당자 1명의 일정만 있습니다. 소장의 배정과 실무자의 다건
  큐, 그 역할의 리뷰는 없습니다 (13번 문서의 I5).
- **OpenAPI → TS 타입 생성** — `src/features/simulation/api/types.ts` 는 손으로 씁니다.
- **WebSocket 스트리밍**, **원본 replay 모드**, **공개용 export 프로필**.

## 개발 기록 운영

- 설계 결정은 [DECISIONS.md](docs/research/DECISIONS.md)에 ID를 붙여 남깁니다. 대안과 아직
  확인하지 못한 가정을 함께 적습니다.
- 남은 작업은 [07_BACKLOG.md](docs/research/07_BACKLOG.md).
- 원자료는 저장소에 들어오지 않습니다. `local-data/` 와 `local-archive/` 는 git-ignored입니다.
- **기존 companion 데모는 저장소에서 제거했습니다** (2026-09-11, 사용자 요청). 음성·triage·
  RAG·아바타 서버(`server/app/{api,modules,prompts,session,ws}`, `main.py`, `config.py`),
  프런트(`src/{App.tsx,components,screens,store,styles,hooks,config,data,types}`),
  `requirements.txt`, `GPU_SETUP.md`, Pretendard 폰트와 아바타 이미지, 관련 문서가 대상입니다.
  전부 커밋 `dafb5e6` 에 들어 있으므로 `git show dafb5e6:<경로>` 로 되살릴 수 있습니다.
  `.env` 도 그 데모용 설정(STT·TTS·RAG·wav2lip)이었고, **`GOOGLE_API_KEY` 값만 남기고**
  나머지는 지웠습니다 (OpenAI 키는 2026-09-15에 마저 지웠습니다).

## 읽는 순서

1. [01_RESEARCH_POSITION.md](docs/research/01_RESEARCH_POSITION.md) — 왜 이 도구인가
2. [02_WORLD_AND_AGENTS.md](docs/research/02_WORLD_AND_AGENTS.md) — 세계·계획 계층
3. [03_ORCHESTRATION_AND_ROLES.md](docs/research/03_ORCHESTRATION_AND_ROLES.md) — 역할과 권한
4. [04_SCENARIOS_AND_ISSUES.md](docs/research/04_SCENARIOS_AND_ISSUES.md) — 시나리오
5. [05_WORKFLOW_AND_EVALUATION.md](docs/research/05_WORKFLOW_AND_EVALUATION.md) — 평가의 분리
6. [06_IMPLEMENTATION.md](docs/research/06_IMPLEMENTATION.md) — 구현 방침
7. [07_BACKLOG.md](docs/research/07_BACKLOG.md) — 남은 일

## 추가 연구·제품 제안 (2026-09-10, 아직 미구현)
- [추가 HCI·정책 시뮬레이션 레퍼런스 14건](docs/research/09_DESIGN_POLICY_REFERENCES.md)
- [디자이너·정책 담당자용 서비스 운영 설계 작업대](docs/research/10_DESIGN_POLICY_STUDIO.md)
- [후속 모델·구현 순서·사용자 연구 계획](docs/research/11_STUDIO_IMPLEMENTATION_AND_STUDY.md)

현재 구현 보고와 별개인 후속 제안입니다. 서비스 기제 편집, 가정별 실행 행렬, 실제 이해관계자의 정정이 핵심입니다.
## 사용자 확정 연구 방향: 리뷰 기반 반복
기획 문서는 다음 셋입니다. 아래 v0.3 절이 그 중 **무엇이 실제로 구현되었는지**를 말합니다.
- [최종 연구·제품 기획](docs/research/12_FINAL_RESEARCH_PLAN.md)
- [주민 리뷰→MEDial 개선→다음 Cycle 구현 지시서](docs/research/13_ITERATION_IMPLEMENTATION_SPEC.md)
- [수정용 LLM 전달 프롬프트와 내부 역할 템플릿](docs/research/14_CODING_LLM_PROMPT.md)

핵심: 초기 인터뷰→개인 에이전트→마을 MAS→Cycle→개인 리뷰→운영안 개선→여러 세대 반복→디자이너 선택→현장 재방문·정정.
이 목표가 이전 09~11의 기능 확장 순서보다 우선합니다. 현재 엔진·지도·정책·비교·발견 기능을 재사용했습니다.

## 2026-09-10 UI 정리 및 다음 재설계

사용자 요청에 따라 IterationSetup의 질문/설정 계층과 글자·여백을 정리하고, 관찰 화면에서 수동 실험 도구 묶음을 분리했습니다. 관찰 시점 선택은 유지했습니다. `npm run build` 통과. 브라우저 연결 실패로 이 변경의 시각 검증은 미완료입니다.

전체 화면 재설계는 아직 구현 전입니다. 다음 작업의 화면 기준은 [15_DESIGN_RESET_SPEC.md](docs/research/15_DESIGN_RESET_SPEC.md), 실행 프롬프트는 [16_DESIGN_LLM_PROMPT.md](docs/research/16_DESIGN_LLM_PROMPT.md)입니다. 기존 연구/데이터 계약을 유지하면서 최상위 화면을 실험 준비·마을 관찰·결과 비교로 통합합니다.

## 2026-09-11 지도·인물 원본 표현 복원

사용자가 제안한 1:1:1 열 구성은 유지했습니다. 원본 은점마을_디오라마.html의 _디오라마_assets/medial.js 및 medial.css를 참조했습니다.

- 지도 기본 뷰는 원자료 raster 경계를 기준으로 칸을 채우는 cover. 지형 비율은 보존하고 상하 일부가 잘립니다. 전체 버튼은 마을 전체 fit, 채움 버튼은 cover로 복귀합니다.
- 원본의 옷/머리/안경이 있는 SVG 인물 일러스트와 색상, 둥근 이름 마커를 지도와 사람 목록에 공통 적용했습니다.
- 과도한 지도 탈색과 비관련 주민 opacity 0.35를 제거했습니다.
- 팝오버는 주민 선택 + 현재 활동 + 작은 더 알아보기로 축소. 원본의260ms hover 종료 지연을 복원하고 실제 팝오버 크기로 위치를 제한합니다.
- 마우스 위치를 기준으로 wheel 확대, passive:false 등록, 배경 클릭·Esc 닫기, 상세 열기를 확인했습니다.
- 같은 좌표에 있는 별개의 그룹을 분리할 때 0벡터 때문에 겹침이 풀리지 않는 버그를 고쳤습니다.
- 연구 엔진, 일과, 원자료 좌표, 1:1:1 화면 구조는 변경하지 않았습니다.

검증: npm run build, 프런트 테스트, Playwright 실제 브라우저의 hover 유지/Escape/집단 내 인물 선택/상세 열기/커서 기준 확대/1440·1366·800px 화면 확인.
스크린샷 및 재현 스크립트: local-archive/map-restored-*.png, local-archive/check-map-restoration.cjs (git 제외).
이번 검토는 지도 표현 및 상호작용 범위이며 전체 연구 엔진에 대한 재검증 보고가 아닙니다.
