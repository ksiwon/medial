# MEDial 설계 결정 기록

## 2026-09-10 · v0.1

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D001 | 완성품 검증이 아닌 탐색형 디자인 도구 | 사용자의 반복 개발 목적; 고정 평가 루프보다 변경·발견의 계보 | 툴 사용 연구에서 설계 변경의 근거 추적 |
| D002 | 원본 replay와 experiment 분리 | 기존 일정에 수행 결과 포함 | 새 정책 거절 시 원본 동승이 발생하지 않는지 |
| D003 | 기본 일과+부분 재계획 | 완전 자유 생성은 지리·시간 일관성 약화, 완전 고정은 정책 무효 | 예약·이동·복귀 테스트 |
| D004 | 세계 진행기와 MEDial 분리 | 전지적 정책/정답 누출 방지 | actor 관측 접근 테스트 |
| D005 | 논리 actor별 상태 격리 | 멀티에이전트는 모델 개수보다 역할·지식·행동의 분리 | 사적 기억 누출 테스트 |
| D006 | 보건소 소장/담당자, 119 상황실/출동대 구분 | 운영·판단·현장 수행 권한이 다름 | 기관 담당자 역할 검토 |
| D007 | 에이전트 평가·행동 로그·인간 평가 분리 | synthetic satisfaction의 과신 방지 | source 필드와 UI 표시 |
| D008 | 정책은 실행 사이 revision | 온라인 자기강화가 개선인 것처럼 보이는 문제 | 부모 불변·미사용 사건 검증 |
| D009 | React+FastAPI+SQLite부터 | 기존 골격 활용, 대규모 프레임워크 불필요 | 첫 세로 흐름 동작 후 확장 판단 |
| D010 | 기존 코드 삭제 보류 | 목적은 새 설계 기록, 미커밋 변경 2개 존재 | 원본 파일 hash/상태 유지 |
| D011 | P12 unknown, 12명 부분 모델 | 자료의 모순을 과장 없이 반영 | importer provenance 보고서 |
| D012 | 원본 임상 escalation 미이식 | 조율 연구와 임상 판단의 범위 다름 | 기관 검토 전 threshold 생성 금지 |
| D013 | 차별점은 실제 근거·역할·개입 추적의 결합 | AgentSociety 2 등 기존 반복 실험 도구 존재 | 후속 문헌 검토·툴 사용 연구 |

## 2026-09-10 · v0.2 (첫 세로 흐름 구현)

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D014 | 지도를 북쪽 위로 회전하고 변환식을 registry에 기록 | 원본은 북쪽이 오른쪽인 가로 이미지. 대안은 원본 방위를 그대로 두는 것이었으나 UI 요구가 북쪽 위다. 방위는 추정하지 않고 원본 산문에서 유도했다 | 회전 후 읍내 진출로가 북북서에 놓여 원본의 "남해읍은 북서쪽" 서술과 일치. 거리 5종이 원본 산문과 일치 |
| D015 | 사건 deck은 "문진 발송"만 고정하고 응답 여부는 엔진이 계산 | deck이 `contact.no_response` 를 단정하면 다른 시각에 연락해도 결과가 같아져 정책 비교가 무의미해진다 | 정책 B의 12:15 통화가 P1의 기본 일과(12:02 귀가) 때문에 닿는 것을 테스트로 고정 |
| D016 | 장소별 연락 도달 여부를 표로 두고 각 항목에 provenance를 붙임 | 밭 미응답만 원본 사건에서 온 것이고 나머지는 연구자 가정이다. 하나의 확률값으로 뭉치면 근거의 차이가 사라진다 | 모든 무응답 사건이 `reachabilityBasis` 를 payload에 싣는다 |
| D017 | MEDial이 아는 일과는 baseline(공개 동의된 평소 일과)이며 realized가 아님 | 실시간 위치를 주면 시뮬레이션이 무의미해진다. 그러나 아무 일정도 모르면 후보 필터가 성립하지 않는다 | 이장 보고 전까지 MEDial 관측에 `FARM` 이 없음을 테스트로 확인 |
| D018 | 이장의 지역 지식은 그의 관측이며 MEDial은 보고를 통해서만 획득 | 이장을 MEDial의 하위 실행자로 두지 않는다는 03 문서의 요구를 구조로 구현 | 획득한 관측의 confidence가 `reported` 로 저장됨 |
| D019 | 기관 담당자가 방문 대신 귀가 시각에 맞춰 통화하도록 규칙화 | 인계 시 공개 동의된 일과가 함께 넘어가므로 그것을 쓰는 것이 자연스럽고, 동시에 "공개량이 늘어난다"는 비용이 드러난다 | 정책 B의 공개 항목이 3개, 정책 A는 1개로 비교 화면에 나타남 |
| D020 | 실행은 attempt 생성 시 1회 계산하고 재생 커서만 서버에 기록 | 규칙 어댑터는 결정적이고 하루가 밀리초에 끝난다. 대안(실시간 진행)은 복잡도만 늘리고 "재생이 모델을 다시 부르지 않는다"는 요구를 지키기 어렵다 | seek/step/play 후 로그 fingerprint 불변 테스트 |
| D021 | 지도 애니메이션용 timeline을 실행 결과에서 만들어 함께 저장 | 지도·추적·지표가 각각 계산되면 어긋날 수 있다. 같은 실행에서 파생시키면 어긋날 수 없다 | 스냅샷과 사건 로그 일치 테스트 |
| D022 | Pydantic을 계약의 단일 원천으로 삼고 JSON Schema는 호환 대상으로 유지 | 06 문서의 방침. 사건별 payload 필수 키와 world-truth 가시성을 모델 검증으로 강제 | 생성 사건이 기존 domain.schema.json 검증을 통과 |
| D023 | 신규 도메인을 별도 서버(sim_main.py)와 별도 번들 청크로 분리 | 기존 서버는 Whisper·FAISS·트리아지를 로드한다. 연구 도구가 그것에 의존하면 API 키 없이 못 돌린다 | 빌드에서 SimulationApp/App 청크 분리 확인. `/api/sim/health` 가 `modelCalls: none` |
| D024 | 원자료 없이도 합성 마을로 실행되게 하고 화면에 출처 배지를 상시 표시 | 테스트·첫 실행이 git 제외된 원자료에 의존하면 안 되고, 합성 지형이 실제처럼 보이면 더 위험하다 | 27개 테스트 전부 합성 픽스처 사용. 상단 배지로 구분 |

## 2026-09-10 · v0.2.1 (남긴 데이터 이슈 해결)

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D025 | 방위를 서술하지 않고 **측정해서 기록**한다. 그 결과로 분석 문서의 "동서 1.11 km"를 "남북"으로 정정했다 | 대안은 코드에서 방위를 계속 회피하는 것이었으나, 그러면 오기가 문서에 남아 논문까지 간다. 측정값(P9→미가식당 5.3°, 동서 381 m·남북 1153 m)이 원본 시뮬레이터 산문("남쪽 끝", "읍내는 북쪽")과 일치하므로 오류는 후속 분석 문서 두 곳에 한정된다. 거리 값과 논증은 방위와 무관해 그대로 두었다 | `geometry.orientationChecks` · 남북/동서 폭과 방위각 테스트 · 원자료 있을 때 1111 m·바다 동쪽·읍내 북쪽 확인 |
| D026 | 도로망 간선 출처를 **import 시점에 강제 검증**한다 | "병합이 없는 도로를 만든 것 아닌가"를 추측으로 남기지 않는다. 검사 결과 간선 350개 중 원본에 없는 것 0개였고, 짧은 경로는 다른 원본 경로 구간을 공유 정점에서 이은 실제 도로였다. 대안(허용 오차로 스냅)은 없는 교차로를 만들 수 있어 채택하지 않았다 | `roadGraphProvenance.allEdgesFromSourcePolylines` · 위반 시 import 실패 · 테스트에서 독립 재계산 |
| D027 | 구간 거리를 하나로 합치지 않고 `sourceM`/`graphM` 둘 다 보관하고, 사건 payload가 어느 쪽인지 밝힌다 | 둘 다 맞는 값이고 뜻이 다르다(원본이 고른 경로 vs 전체 도로망 최단 경로). 평균이나 택일은 근거를 지운다. `distanceBasis` 를 로그에 실어 읽는 사람이 헷갈릴 수 없게 했다 | `namedRouteDistances` 74구간 · `graphM > sourceM` 이면 import 실패 · travel 사건의 `distanceBasis` 테스트 |
| D028 | 데이터 이슈에 `open`/`resolved` 상태를 두고 화면에 노출한다 | 해결된 것과 남은 것이 같은 목록에 섞여 있으면 "기록했다"가 "처리했다"처럼 읽힌다 | 지도 하단 데이터 이슈 패널 · 상태 필드 테스트 |
| D029 | 지도 timeline과 결정 trace를 실행 결과와 함께 SQLite에 저장한다 | 메모리에만 두었더니 서버를 다시 켠 뒤 지난 시도를 열면 지도가 비었다. 실행의 일부이지 캐시가 아니다. 구버전 DB는 ALTER로 이관한다 | 새 프로세스에서 같은 DB를 열어 timeline·trace·metrics 동일 확인, 스냅샷·비교 동작 확인 |
| D030 | registry에 schemaVersion을 두고 구버전이면 재생성 명령과 함께 거부한다 | 구버전 registry는 화면 한참 뒤에서 정의되지 않은 값으로 터진다. 원인 자리에서 실패시키는 편이 낫다 | `village/2` 미만 또는 필수 블록 누락 시 StaleRegistryError |

## 2026-09-10 · v0.2 (검토 R01~R08 대응, 페르소나·T004, 화면)

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D031 | 시도 ID를 UUID로 바꾸고 저장을 **삽입 전용**으로 한다. 충돌은 덮어쓰지 않고 실패시킨다 | 프로세스 내 카운터가 재시작마다 1로 돌아가고 `INSERT OR REPLACE` 가 어제 기록을 조용히 다시 썼다. 조용히 바뀌는 연구 기록은 없는 기록보다 나쁘다. 대안(기존 DB 삭제)은 계보를 버리므로 채택하지 않았다 | 재시작 후 동일 정책 생성·fork에서 이전 시도 불변 · 중복 ID 시 부분 잔존 없음 · 구버전 행은 읽을 때 `lineage: root` 로 보정만 하고 다시 쓰지 않음 |
| D032 | **rerun**(초기 상태에서 정책만 교체)과 **fork**(커서 시점 분기)를 별도 개념·엔드포인트·라벨로 나눈다 | 기존 fork는 `parentSeq` 만 적고 0부터 다시 돌려서, 시점 분기처럼 보이지만 이전 결정까지 바뀌었다. 초기 상태 비교는 유효한 기능이므로 없애지 않고 이름을 바로잡았다 | fork는 부모 로그 접두부와 불일치하면 409 · 화면 라벨이 `재실행`/`분기 @seq` 로 구분 |
| D033 | `emergencyRuledOut` 을 제거하고 `clinicalStatus`/`locationStatus`/`emergencyEvidence` 로 대체한다 | 무응답은 위치도 상태도 확정하지 않는다. "응급 분류 0건"과 "응급 배제 판정"은 다른 주장이며, 후자는 이 시나리오에서 아무 근거가 없다. 악화 신호가 없다는 것은 세계 설정이지 MEDial의 근거가 아니다 | 분류 사건 payload · 화면 문구 · 지표의 `emergencyRuleOutClaims` |
| D034 | MEDial에게는 `home_or_away` 투영만 준다. 장소 단위 지식은 이장의 사적 기억이다 | MEDial이 전체 baseline 장소를 받으면서 "이장만 밭을 안다"고 설명하는 모순을 없앤다. 아무 일정도 안 주면 후보 필터가 성립하지 않으므로 "지금 집에 있나"까지만 준다 | 이장 보고 전까지 MEDial 관측에 장소 없음 · 후보 사유가 "어디인지는 공개되지 않는다"로 기록 |
| D035 | 무응답의 세계 쪽 사유를 `world.reachability_resolved`(연구자 전용)로 분리한다 | 연락한 쪽이 "밭이라 못 받았다"를 읽을 수 있으면 관측 분리가 이벤트 타입 수준에서만 성립한다 | MEDial-visible payload에 `reachabilityBasis` 없음 · 로그·화면 모두 "이유는 연락한 쪽이 알 수 없음" |
| D036 | 동의를 `verified/assumed/unknown` 으로 기록하고 근거 문구를 함께 보관한다 | 원자료에 이 조율 시스템에 대한 동의가 없다. 확인하지 않은 동의를 확인된 사실처럼 만들지 않는다 | `SharedRoutine.consent` · 전 항목 `assumed` |
| D037 | 재생 커서를 **사건 번호**로 하고 시각과 분리한다 | 같은 밀리초를 공유하는 사건이 최대 10개인 실행이 있어, seq를 시각으로 바꾸면 한 단계가 10개를 건너뛰었다 | 한 단계 = 정확히 1 사건 · 슬라이더도 seq · 커서가 DB에 있어 재시작 후 복원 |
| D038 | 노출한 정책 조건은 전부 엔진이 읽게 하고, 편집기 폼을 **서버 스펙에서 생성**한다 | 화면에 있는데 결과를 바꾸지 않는 조건은 "효과가 없다"는 잘못된 결론을 만든다. 고정 `helperContactCap=0` 버튼을 실제 편집기로 바꿨다 | 미지원 필드 400 · 범위 밖 값 400 · `quietWindowMin`/`disclosure`/`escalate*`/`staffCount` 가 각각 결과를 바꾸는 테스트 |
| D039 | 인계 기한이 남아 있으면 막다른 길에서도 요청을 닫지 않는다 | 이웃 상한 0 + 기한 90분으로 두었더니 09:30에 미해결로 닫혀 기한이 발동할 수 없었다. 기한이 존재하는 이유가 바로 "갈 사람이 없는 경우"인데 그 경우에만 무력했다 | 브라우저에서 발견 · `awaiting_escalation_deadline` 대기 후 11:00 인계 → 12:15 해결 · 기한이 없으면 종전대로 미해결 |
| D040 | 기관을 직원별 busy 캘린더로 모델링하고 `visitTravelMinutes` 를 **편도**로 통일한다 | `initialQueueDepth×reviewMinutes` 는 대기열이 아니라 곱셈이었고, 라벨은 왕복이라 말하면서 계산은 편도를 두 번 했다. 기존 165분을 맞추려고 논리를 고정하지 않았다(고치고 나서도 165분이 나왔다) | 근무시간 밖 업무 거부 · `staffCount` 가 대기를 바꿈 · 방문 = 편도×2 + 체류 |
| D041 | 비교의 통제 여부를 **계산**한다. 정책은 필드 단위 diff, 나머지 입력은 해시 비교 | 자유 서술 `changes` 는 연구자가 타이핑한 문장이지 입력이 같았다는 증거가 아니다. 입력이 다른데 "같은 초기 상태에서 정책만 변경"이라고 표시하면 실험에 대한 거짓 주장이다 | `controlled: false` 와 `differingInputs` · 화면 배너가 통제/비통제를 구분 |
| D042 | 엔진 동작이 바뀌면 `ENGINE_VERSION` 을 올리고, 비교는 그것도 통제 입력으로 본다 | 같은 조건인데 결과가 다른 두 시도가 있었고 원인은 정책이 아니라 코드 변경이었다. 버전이 같으면 비교 화면이 그것을 정책 효과로 읽게 만든다 | 0.1.0 시도와 0.2.0 시도를 비교하면 `engineVersion` 이 differingInputs에 뜬다 |
| D043 | 페르소나는 서버에서 컴파일하고 **행동 구조 + 근거 카드**만 내보낸다 | 이름·인터뷰 인용·역할 프롬프트는 엔진에도 저장소에도 프론트 번들에도 필요 없다. 필요한 것은 "운전하는가·혼자 사는가·누구 부탁이면 가는가·무엇을 모르는가"다 | 카드에 JSON pointer와 사실/해석/가정/모름 · 전문은 해시만 · 합성 픽스처로 테스트 |
| D044 | 좌석 수를 `unknown` 으로 두고 실행에 쓰는 값은 `ResourceRevision` 의 실험 가정으로 분리한다 | 동승 흐름이 의존하는 바로 그 숫자가 원자료에 없다. 4라고 적으면 없는 사실을 만드는 것이고, 없다고 실행을 포기하면 흐름을 못 본다 | 페르소나 카드 `seatCapacity: unknown` · 지표에 `seatAssumption` 문구 동반 |
| D045 | 이동 필요를 baseline에서 분리하고 동승 결과는 정책이 만들게 한다 | P9가 baseline에서 집에 있는 것은 "외출 필요 없음"이 아니다. 원본의 동승은 한 정책의 결과이지 그날의 사실이 아니다 | 운전 가능 unknown인 사람은 `driving_status_unknown` 으로 거절 · 두 번째 요청이 `driver_double_booked` 로 충돌 후 다른 사람에게 이동 |
| D046 | 원본 지도 래스터를 git-ignored 레지스트리에 두고 **로컬 API로만** 서빙한다 | 요구는 원본 지도 배경·해안선 복원인데 그 배경은 래스터 한 장이고 연구 자료다. 저장소에 커밋하거나 번들에 넣지 않으면서 화면에는 띄울 수 있는 자리가 로컬 API뿐이다 | `dist/` 에 이미지 없음 · `GET /api/sim/village/map` · 없으면 도로선 지도로 자동 강등 |
| D047 | 지도 표식 크기를 **화면 픽셀 기준**으로 고정한다 | 은점마을은 남북 1.15 km 세로 띠라 전체 보기에서 1 SVG 단위가 0.25 px이다. 지도 단위로 그린 얼굴은 2 px이라 보이지 않았다 | 전체 보기에서 얼굴·집·건물·라벨이 읽힘 · 확대해도 같은 크기 유지 |
| D048 | 기존 companion을 삭제하지 않고 **활성 빌드 밖**으로 옮긴다 | 미커밋 사용자 변경 2건이 그 안에 있다. 되돌리거나 지우면 남의 작업을 잃는다 | 기본 빌드에 App 청크(74 kB) 없음 · `VITE_INCLUDE_COMPANION=1` 로 복귀 · 사본·diff·해시를 `local-archive/companion/` 에 보관 |
| D049 | 발견(DesignFinding)을 정책 편집기에 연결해 **다음 revision·다음 시도**까지 잇는다 | 비교만으로는 한 번짜리 실행 두 개가 남고 그 사이의 추론은 연구자 머릿속에만 있다. 조건→결정→결과→발견→다음 조건이 저장되어야 반복 설계 도구다 | 발견에 `resultingPolicyId`/`resultingAttemptId` 가 채워짐 · 새 revision의 changes에 발견 ID가 남음 |

## 2026-09-10 · v0.3 (리뷰 기반 반복, 13번 문서 I0~I4)

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D050 | 리뷰의 근거 경계를 **코드로** 강제하고 rule 어댑터도 예외로 두지 않는다 | 프롬프트에만 적힌 경계는 경계가 아니다. rule 어댑터를 면제하면 그 경계는 "LLM 경로의 성질"이지 시스템의 성질이 아니게 된다. 대안(모델 출력만 검사)은 규칙 쪽이 조용히 규칙을 어겨도 알 수 없다 | 남의 사건·남의 근거 카드·근거 없는 등급·연구자 전용 사건 인용이 각각 `ReviewBoundaryError` · 세 어댑터 모두 같은 검증을 통과 |
| D051 | `unknown` 이 아닌 평가 항목은 **자기가 겪은 사건을 반드시 인용**해야 한다 | 근거 없는 의견은 근거 없는 의견이지 나쁜 평가가 아니다. 이 규칙이 없으면 리뷰가 서술의 그럴듯함으로 개선을 정당화하게 된다 | 등급만 바꾸고 eventRefs를 비운 리뷰가 거부됨 |
| D052 | 미사용·미경험을 별도 `usageStatus` 로 두고 전 항목을 `unknown` 으로 둔다 | 미사용은 관측 결과다. 불만족으로 세면 "서비스를 안 쓴 사람이 많을수록 나쁜 정책"이 되어 결론이 뒤집힌다 | `no_experience` 리뷰에 등급이 하나라도 있으면 검증 실패 |
| D053 | '불편'을 일곱 번째 차원으로 만들지 않고 해당 차원의 `negative` 로 둔다 | 같은 불만이 두 곳에서 세어져 종합에서 두 배로 보인다. 사용자 요청의 여섯 항목은 12번 문서의 여섯 차원으로 모두 표현된다 | 시간·노동의 negative가 곧 부담 불만 |
| D054 | 동의가 `assumed` 인 채로 제3자에게 전달된 정보 공개는 `mixed`(열린 문제)로 두고, 본인이 그것을 원했는지는 `unknown` 으로 남긴다 | 전부 `unknown` 으로 두면 공개 범위 차원이 개선을 이끌 수 없다. 그렇다고 "싫어했다"고 쓰면 없는 선호를 만든다. 일어난 것은 사실이고 동의는 확인되지 않았다 — 그것이 설계 문제라는 것까지가 근거 있는 주장이다 | 리뷰 문구가 "동의한 기록은 없고 연구자 가정으로 두었다 / 원했는지는 내가 답해야 안다" · requestedChange 없음 |
| D055 | 개선 기제는 그 Cycle이 **실제로 사용한 기능에만** 발동한다 | 초기 구현이 안부 확인만 있었던 Cycle에서 동승 우회 시간을 줄이자고 제안했다. 차원이 맞고 다이얼이 존재한다고 근거가 되지는 않는다. 아무도 도달하지 않은 상한을 낮추는 것도 같은 문제다 | `signals()` 게이트 · 연락/이동/공개/충돌/상한 도달/반복 연락 신호 |
| D056 | 세대당 후보는 **차원당 하나**로 다양화한다 | 같은 불만에 다이얼 두 개를 돌리면 같은 실험을 두 번 하는 것이고, 둘 다 효과가 없으면 다른 문제는 시험조차 되지 않는다 | v0에서 시간·노동 1개 + 해결 1개가 나옴 |
| D057 | 지표가 현재 세대와 **완전히 같은** 후보는 trade-off가 아니라 '변화 없음'이다 | Pareto 지배는 엄격 우위를 요구하므로 동일한 후보는 비지배로 남는다. 그대로 두면 "두 후보가 서로 다른 지표에서 낫다"며 디자이너에게 자기 자신과의 선택을 시킨다 | 동일 벡터 후보가 `unchanged` 로 분류되고 전부 그러면 `stalled` |
| D058 | 한쪽이 `unknown` 인 차원은 우열의 근거로 쓰지 않는다 | 해결된 요청이 없어 평균 대기가 없는 실행을 0으로 읽으면 "즉시 응답"이 된다. 모르는 값으로 이긴 후보는 이긴 것이 아니다 | `dominates()` 가 unknown이 있으면 항상 False와 이유 반환 |
| D059 | 반복의 v0을 A/B 정책이 아닌 **별도의 초기 운영안**(`policy-IT-v0`)에서 시작한다 | A/B는 이미 손으로 비교하려고 만든 두 대안이다. 그중 하나를 v0으로 쓰면 "조율 절차가 아직 정해지지 않은 상태"라는 4단계의 의미가 사라지고, 개선 여지도 이미 소진되어 있다 | v0이 이웃 상한 1·재연락 0·기한 없음·named 공개로 시작하고 v1에서 실제로 갈라짐 |
| D060 | 종료 상태를 하나로 합치지 않는다 | `ready_for_designer`·`needs_decision`·`budget_exhausted`·`no_valid_change`·`stalled`·`failed`·`cancelled` 는 서로 다른 사실이다. 하나의 "완료"로 합치면 예산이 끊긴 실행과 세대를 다 돈 실행이 같은 보고가 된다 | 상태별 문구가 `STOP_REASON_TEXT` 에 따로 있고 화면 배지가 경고/보통을 구분 |
| D061 | 모델 실패를 scripted 결과로 대체하지 않는다 | 대체하면 "오프라인에서도 전체 흐름이 돈다"가 "온라인 실패를 감춘다"로 바뀐다. 모델 장애는 도구의 실패이지 주민의 거절이나 unknown 평가가 아니다 | 키 없이 온라인 어댑터 선택 시 400 · 호출 실패 시 `failed`/`model_failure` 이고 리뷰가 하나도 저장되지 않음 |
| D062 | 모델 결과를 (session, generation, role, subject, 입력 해시) 키로 캐시하고 각 전이를 멱등으로 만든다 | 재시작이 같은 세대를 다시 실행하면 비용이 두 배가 되는 것보다 **같은 patch가 두 번 적용되는 것**이 더 나쁘다 | 4스텝 후 중단하고 새 프로세스에서 이어 실행 → 리뷰 중복 없음, `resultingPolicyRevisionId` 중복 없음 |
| D063 | 자동 patch의 허용 목록을 정책 경로로 한정하고, 금지 경로마다 **어떤 경계인지** 말한다 | "invalid path"는 왜 안 되는지 가르쳐 주지 않는다. 페르소나를 고쳐 좋은 리뷰를 만드는 것과 범위를 벗어난 오타는 다른 종류의 실패다 | 페르소나·근거·기억·deck·기준·세계·자원 경로가 각각 자기 문장으로 거부됨 |
| D064 | 엔진이 지원하지 않는 절차는 `requires_implementation` 으로 **기록하고 실행하지 않는다** | 지원 목록만 제안하게 하면 설계 공간이 구현된 것으로 조용히 줄어든다. 그렇다고 실행하면 없는 기능을 실행한 척하게 된다 | 사전 가능 시간 확인·종료 통지·방문 전 통화 3건이 patch 없이 남고 `not_run` |
| D065 | 세계 안의 MEDial과 세계 밖의 개선 에이전트를 **화면에서도** 다른 것으로 표시한다 | 둘을 같은 패널에 두면 오케스트레이터가 하루 중에 자기를 고쳐 쓰는 것처럼 보인다 | 개선 화면 상단 배너 · 우상단 MEDial 운영 상태와 별도 |
| D066 | `source="human"` 을 만드는 코드 경로를 제출 엔드포인트 **하나**로 좁힌다 | 시연용 예시 한 줄이 나중에 참여자의 말로 인용되는 것이 이 도구에서 가장 위험한 실패다. 경로가 하나면 "사람이 내기 전에는 없다"가 검사 가능한 성질이 된다 | 자동 루프 어디서도 호출하지 않음 · 실행 직후 `human_review_count() == 0` |
| D067 | 다일 모드를 이름만 두고 **거부**한다 | 하루 단위 통제 비교와 다일 적응은 다른 실험이다. 부분 구현으로 열어 두면 "정책 효과"와 "익숙해짐"이 섞인 결과가 나오고 그것을 구분할 방법이 없다 | session 생성 400 · capability 목록에 `longitudinal: false` |
| D068 | 하나의 sqlite3 연결에서 **읽기도** 잠금으로 보호한다 | 브라우저에서 `/events` 가 간헐적으로 500이었다. API 스레드풀과 반복 루프 스레드가 같은 연결을 쓰는데 읽기 경로에만 잠금이 없었다. 대안(연결 풀)은 삽입 전용·단일 트랜잭션 보장을 다시 설계해야 한다 | 쓰기 중 4스레드 동시 읽기 회귀 테스트 · 재시작 후 브라우저 요청 전부 200 |

## 2026-09-14 · 연구 초점 및 개선 구조 재설정

| ID | 결정 | 이유·대안 | 확인 |
|---|---|---|---|
| D069 | 연구 제목은 `Resident Agents as Evaluators`, 평가 대상 시스템 이름은 `MEDial`로 고정한다 | AI Care Orchestrator는 제품명이 아니라 범주다. MEDial 자체 개발이 연구 주제처럼 보이지 않게 위계를 고정한다 | 문서·UI 문구에서 명칭 혼용 검사 |
| D070 | 주민 평가는 점수 없이 `positive/mixed/negative/unknown`과 근거를 보존한다 | 합산 점수는 경험하지 않은 수치와 자동 우열을 만든다 | 평가 항목별 event/evidence ref 검증 |
| D071 | 자동 우승자 선정을 제거하고 연구자가 이유와 함께 실행할 Change Set을 확인한다 | 서로 다른 주민의 이익과 부담을 하나의 규칙으로 자동 환산하지 않는다 | `awaiting_confirmation`에서 멈추며 확인 전에는 새 revision이 생기지 않음 |
| D072 | MEDial 개선 대상을 Quest 완료·인계, Task 구성·흐름, 배정·거절, 시간·부담, 설명·정보 공개의 다섯 영역으로 제한한다 | 기존 파라미터는 이 규칙을 실행하는 바인딩이지 독립 설계 어휘가 아니다 | 평가 차원에서 관련 개선 대상만 여는 UI 라우팅 |
| D073 | 개선 단위를 Quest/Task의 의미 있는 전후 규칙과 내부 바인딩을 함께 담는 단일 Change Set으로 한다 | 파라미터 패치만으로는 무엇을 왜 바꾸는지 알 수 없고, Quest/Task/파라미터를 따로 편집하면 같은 규칙의 원천이 셋이 된다 | `23_QUEST_TASK_CHANGE_SET.md`; PatchOp 활성 계약 제거, 연구자 확인 전 실행 금지 |
| D074 | 연구자 수정은 초안을 덮어쓰지 않고 새 `researcher_hypothesis` Change Set으로 저장한다 | 원본 생성 근거와 사람이 바꾼 내용을 구분해야 사후 감사와 실제 주민 검토가 가능하다 | 원본 `superseded` 보존, 수정본 별도 id/hash, 저장과 실행 확인 분리 |
| D075 | 자리를 뜰 수 있는지를 `EnvironmentRevision.availability` 표로 옮기고, `env-v1`은 기존 동작을 **결함까지 그대로** 재현하고 `env-v2`가 고친다 | `INTERRUPTIBLE_PLACES`에 `"HOME"`이 있었지만 체류 장소는 `HOME:P1` 형태라 한 번도 매칭되지 않았다. 집에 있는 사람은 전원 '자리를 뜰 수 없음'이었고 점심·저녁에는 12명 중 0명이 가용했다. 상수를 조용히 고치면 저장된 실행의 의미가 달라지므로 새 리비전으로 나눴다 | `env-v1.available("HOME:P1")`이 False이고 사유에 '구현상 결과'라고 적혀 있음 · v1↔v2가 한 줄만 다름 · 두 리비전 비교가 `controlled: false` |
| D076 | 거절 사유를 **순서 있는 규칙 목록**으로 두고 먼저 걸리는 줄이 이유가 된다. 점수로 합산하지 않는다 | 바쁜 정도·관계·호출 횟수를 수치화해 더하면 3.2점이 '측정 2 + 추측 1.2'인지 '추측 3 + 측정 0.2'인지 사라진다. 게다가 점수는 잰 것처럼 보여서 결과가 그럴듯해질 때까지 조정하게 된다. D016이 전화 도달을 확률 하나로 뭉치지 않은 것과 같은 이유다 | 줄마다 provenance · 줄 하나씩 끌 수 있음 · 여러 줄이 동시에 걸려도 답은 하나 · 로그에 `rule`(키)과 `reason`(문장)이 따로 실림 |
| D077 | 수락 확률을 지금은 넣지 않는다 | 변덕은 원자료에 없고, 변이는 이미 하루 실현(1b)에서 seed로 들어온다. 결정에까지 주사위를 넣으면 근거 없는 변이를 두 번 만드는 것이다. 감도 분석(7단계)이 결과가 지나치게 딱딱하다고 말하면 그때 맨 아래 줄 하나에만 넣는다 | `decline_rules` 모듈 문서에 명시 · 신뢰도 점수·거절의 관계 감점은 계속 부재 |
| D082 | MEDial 머리(LLM)는 **후보까지 직접 고른다.** 규칙 코드는 후보를 계산하지 않고, 고른 뒤에 설계자 조건(`allowHeadContact`·`neighbourAskLimit`·`contactStrategy`·`retryCount`)을 검사한다. 어기면 이유를 돌려주고 한 번 다시 고르게 하고, 두 번째도 어기면 어댑터 실패다. 규칙 정책의 답으로 바꿔치우지 않는다 | 연구자: "AI Care Orchestrator라 LLM이 컨텍스트를 최대한 이해해야 하고, 후보 고르는 것도 상황을 다 꿰고 있는 LLM이 해야 한다." 노브는 울타리로 남아야 뜻을 잃지 않는다 | `test_llm_village.py`: 본인에게 부탁 → 되돌려짐, 두 번 → 실패·offer 없음 |
| D083 | 주민이 LLM일 때 **LLM이 전부 정하고 거절 표는 기록만** 한다. 같은 view로 표가 냈을 답을 `ruleTableSaid`·`agreesWithRuleTable`로 사건 옆에 적고 적용하지 않는다 | 연구자 선택. 표를 앞에 세우면 LLM 주민이 아니라 표에 말을 입힌 것이 된다. 대신 표와 어긋난 횟수를 셀 수 있게 남긴다 | 거절 사건 `rule=llm_judgement`, `reason`=본인 말, `ruleTableSaid.action` |
| D084 | 머리의 `wait`는 시계가 달린 결정이다. `waitMinutes`(5~120) 뒤에 같은 질문이 돌아오고, 한 건에 두 번까지만 기다릴 수 있다 | 실제 모델(flash-lite)이 첫 판단에서 "40분 기다린다"를 골랐고, 깨우는 장치가 없어 하루가 그대로 끝났다 | `medial.waiting reason=head_wait untilMs`, 세 번째 wait → 어댑터 실패 |
| D085 | 두 층의 모델: 머리는 `gemini-3.8-flash`, 주민은 `gemini-3.1-flash-lite`. `ModelPolicy`에 둘 다 들어가 attempt 입력 해시에 잡히며, 호출 키는 역할별 모델로 만든다 | 머리는 상황 전체를 읽고 후보를 고르는 한 번의 무거운 판단, 주민은 하루 열두 번의 가벼운 한 답 | `call_key(…, role)`; 머리 모델을 바꿔도 주민 기록은 유효 |
| D086 | `retryCount`는 **모든 연락 순서의 첫 단계**다. 재연락 횟수만큼 본인에게 **전화로** 다시 연락한 뒤에야 이장·이웃·관계·보건소로 간다. 규칙 머리와 모델 머리 모두 코드가 먼저 실행하고, 모델 머리에게는 재연락 여부를 묻지 않는다 | 반복 루프에서 `head_first` 정책에 `retryCount 0→1` Change Set이 검증·확정됐지만 로그가 부모와 같았다. 재연락이 `retry_then_clinic`에서만 실행됐기 때문이다. 연구자: 재연락 → 가까운 관계 → 이장 | `test_contact_order.py`; engine `medial-sim/0.3.0`; 두 번째 시도는 집 기기로 다시 가지 않는다 |
| D087 | 새 연락 순서 `relation_first`와 정책 D. 대상자와 **기록된 관계**(동행군·친척·도움 기록, `relations.py`)가 있는 사람에게 먼저, 이장은 마지막. 기록 밖의 사람에게는 부탁하지 않는다. MEDial이 대상자의 관계를 안다는 것은 "가까운 연락처로 등록했다"는 가정(`assumption-registered-contacts`)이다 | 연구자 제안을 고정 규칙이 아니라 비교할 정책 하나로 둔다. P1의 기록된 관계는 이장 하나뿐이라 D는 결국 재연락 → 이장이 되며, 결정 사유가 그 이유를 말한다 | 규칙 D: 10:10 전화 → 이장, 이장 23.0분(A는 18.8분) |
| D088 | 빈집을 확인한 사람에게 짐작 가는 곳이 없으면 요청을 닫지 않고, MEDial이 **이장에게 전화로 어디 있을지 묻는다** (`allowHeadContact`이고 이 건에 아직 묻지 않았을 때만). 모델 머리는 같은 질문을 `no_lead` 단계로 받아 묻기·인계·멈춤 중 고른다 | 정책 C가 늘 미해결이었던 것은 순서의 결과가 아니라, 대상자의 일과를 아는 사람이 한 번도 물어지지 않은 구멍이었다. 연구자: "정책 C 개선" | C 규칙: P9 빈집 → 이장 전화 → 밭 → 해결, 4명 연락. engine `medial-sim/0.4.0`, `prompt-v4` |
| D089 | 관찰 화면은 한 요청의 **모든 판단 이유를 시간순으로** 보이고, 문장에 엔진 키(장소 `FARM`, 경로 `neighbour_visit`)를 쓰지 않는다. 같은 버전끼리의 비교는 경고가 아니라 "비교할 다음 버전이 없다"로 말한다 | 연구자: "뒷단은 엄밀하게, 화면은 딱 직관적이게". D의 핵심 이유가 마지막 판단에 가려졌다 | Playwright `observe-policy-d-reasons.png`, 키 문자열 0건 검사 |
| D090 | **채록 장부**(`ledger*.json`, `ledger.py`): 사람×항목(함께 다니는 사람·도움을 청하는 사람·일과를 아는 사람·연락 조건·거절 조건)마다 `recorded / partial / asked_none / not_asked`를 적고, "누가 누구의 평소 장소를 아는가"를 provenance와 함께 여기서 읽는다. 이장을 코드로 박지 않는다. 장부는 attempt 입력 해시에 들어가고, 결정이 기댄 연구자 가정은 `metrics.elicitation.leanedOn`과 비교 화면 "기록에 없던 것"에 보인다 | 은점마을 인터뷰는 '함께 다니는 이웃'만 물었다. 그런데 관계 그래프의 빈칸이 '없음'으로 읽혀 "P1은 어떤 순서를 짜도 이장에게 간다"가 마을의 사실처럼 보였다. 물었고 없는 것과 안 물어본 것을 가르지 않으면 다른 공동체로 옮길 수 없다 | `test_ledger.py` 8건; 정책 D 사유에 "묻지 않았다 (채록 공백)"; 장부 없는 실행은 `ledger-legacy`로 모두 `not_asked` + 이장 가정 |
| D091 | **시뮬레이터 자체 검사** 셋을 한 모듈(`audit.py`)과 한 스크립트(`scripts/audit.py`)로 둔다: ① 노브마다 값을 뒤집어 같은 하루의 로그 지문이 달라지는지(읽을 수 없는 칸은 이유를 적고 건너뜀), ② 여러 seed의 하루에서 정책 순서가 며칠이나 유지되는지, ③ 모델 주민의 표 이탈과 종합의 오분류(사람 라벨로만 셈). 노브 표는 `test_audit.py`에 고정한다 | 일반화 검토 ②③④. 노브가 죽어도, 결과가 하루에 흔들려도, 종합이 틀려도 알 길이 없었다. 검사 중에 고친 것: `helperContactCap`이 동승 부탁에서 읽히지 않았고, 편집기의 연락 순서 목록이 넷 중 둘만 있었고, 등록부 주석이 "모든 조건이 두 덱에서 읽힌다"고 거짓을 말했다 | 죽은 노브 0; D는 20일 중 3일 미해결(10:10의 이장이 넘기고 P4가 유예); 이동 덱은 하루 변이에 반응 없음 |
| D092 | 모델 주민의 표 이탈은 **규칙 마을을 그림자로** 잰다(`scripts/llm_shadow.py`): 규칙 머리·규칙 주민이 하루를 끌고 가고, 주민에게 질문이 갈 때마다 같은 view로 모델 주민에게도 물어 답을 **기록만** 한다. D083(표는 옆에 기록만)은 유지하고, 이 비율을 모델 마을 결과 옆에 항상 같이 둔다. prompt-v5(자기 검토·거절 정당화)는 연구자 결정으로 남긴다 | 모델 머리 아래서는 주민이 거절 상황에 놓이지 않아(늘 P9 먼저, P9는 늘 수락) 이탈률이 0으로 보였다. 그림자로 재니 표가 수락·관측이라 한 56건은 56건 일치, 표가 거절·유예·전달이라 한 26건 중 모델이 안 된다고 한 것은 1건 — 모델 마을은 마을의 응할 의사를 높게 잡는다 | 82문항, `.run/llm-shadow/`; 프롬프트를 바꾸면 같은 표로 효과를 잰다 |
| D080 | 기본 하루는 **같은 일과의 반복**이다. `env-v3`(기본)는 출발 시각만 ±10분 흔들고 외출을 거르거나 더하지 않는다. 외출이 빠진 하루는 `env-v2`를 이름으로 골라야 하고 다른 입력으로 표시된다 | 연구자의 전제: 시골 마을은 매일 똑같은 일상을 반복하고 바리에이션은 약간뿐이다. v2의 생략 확률 0.15는 실제 마을에서 거의 매번 '외출 하나가 다른 하루'를 만들어 예외가 기본이 되어 있었다 | `get_environment().variation.skipOutingProbability == 0`, 시드 5개 모두 `source_jittered` 이하 |
| D081 | 화면 확인은 Playwright로 한다 (`npm run e2e`). 합성 마을·별도 DB(`.run/e2e/`)로 실서버와 실브라우저를 띄우고, 세 화면을 찍어 `.run/e2e/screens/`에 남긴다. 스크린샷은 커밋하지 않는다 | jsdom mount 검사는 "던지지 않는다"까지만 본다. 첫 실행에서 바로 두 가지가 드러났다: 실행 중인 버전을 열어 표가 전부 미수집이었고, 세대 objective에 하루·거절·전가가 실려 있지 않아 방금 돌린 실행이 '예전 실행'으로 표시됐다 | `e2e/screens.spec.ts` 2건 통과, 스크린샷 3장 |
| D079 | 거절 사유·전가·뽑힌 하루는 화면에서 **읽기만** 한다. 새 노브 없음. 어느 관점에 보이는가는 사건의 `visibility` 필터 하나로만 정하고, 화면이 따로 감추거나 보여 주지 않는다 | 셋 다 로그와 지표에는 있었지만 화면이 읽지 않아 연구자가 결론을 낼 수 없었다. 화면이 따로 감추기 시작하면 "MEDial이 아는 것" 관점이 두 군데서 정의된다 | `metrics.handovers`·`metrics.refusals`·`residentBurden.askedBy*` 는 커밋된 사건에서 계산. 조율 패널 "누구에게 부탁했고, 뭐라고 했나", 비교 표 "어느 하루였나"·"누가 거절했나", 관찰 헤더의 하루 태그 |
| D078 | `neighbour_first` 정책을 추가하고, 거절당하면 `neighbourAskLimit`까지 다음 사람에게 묻는다 | doc 19이 사례 1의 연구 대상으로 '연락 순서'를 적어 두었는데 그것을 바꿀 정책이 없었다. `head_first`가 이장을 하드코딩하고, 엔진이 계산한 후보 목록은 화면에 표시된 뒤 버려졌다. 한 명만 묻고 끝나면 '이장 대신 다른 한 명'일 뿐이라 순서를 연구할 수 없다 | 이장 정책은 1명을 건드리고 해결, 이웃 정책은 3명을 건드리고 미해결 — 교환 관계가 화면에 나타남 |
| D093 | **하나의 의미 규칙**: 지원 규칙마다 타입이 있고, 그 값에서 전후 문장·실행 바인딩·실행 해시를 전부 생성한다. 문장과 바인딩을 따로 편집하는 경로를 없앴다. 자유 서술은 이유이고 실행이 아니다 | 편집기가 afterRule 문장과 executionBindings를 따로 바꿀 수 있었다. "재연락 2회"라고 적고 `retryCount: 1`로 저장해도 검증을 통과했고, 실행은 1회였다 — 평가에서 수정으로 이어지는 추적이 거기서 끊긴다. LLM에게 두 표현의 일치를 판정시키는 계층을 얹지 않고 원천을 하나로 만들었다 | `semantic_rules.py`; 저장된 문장이 값에서 만든 문장과 다르면 거부 · 설명만 바꾼 초안은 같은 해시 · 화면에서 재연락 2회로 바꾸면 자식 실행 로그에 전화 재연락 2건 |
| D094 | **저장은 실행이 아니다**를 명령으로 분리하고, 초안 없이도 지원 규칙에서 직접 작성할 수 있게 하며, '이번에는 수정하지 않음'을 이유와 함께 기록되는 종료 경로로 둔다 | 유효 AI 초안이 없으면 연구자가 아무것도 쓸 수 없었다(사실상 초안의 복제만 가능). 그리고 "아무것도 바꾸지 않는다"는 결정이 기록될 자리가 없었다 | `no_valid_change`에서 작성 → 확정 시 자식 1개 · `decline_changes`는 초안을 declined로 남기고 실행 0 · 같은 commandId 재전송은 실행 전에 걸러짐 |
| D095 | **공동체는 자료다.** 주민·역할·관계·장부·시나리오를 `CaseBundle`로 묶고 엔진의 이장 상수를 없앤다. 이장이 없는 공동체가 유효하며, 이장을 거치는 운영안은 '역할이 없다'는 이유로 거부한다 | `VILLAGE_HEAD_ID = "P6"`가 엔진에, 관계가 코드 리터럴에, 장부 없음의 이장 전지 가정이 fallback에 있었다. P번호를 재사용하는 다른 공동체는 은점의 관계를 그대로 물려받았을 것이다 | `case_bundle.py`; R1~R5·이장 없음 사례가 자기 장부로 돌고 로그에 P번호 0건 · 다른 공동체의 관계/시나리오가 섞이면 로드·생성·실행 모두 거부 |
| D096 | **'적용됐는가'와 '차이가 없는가'를 나눠 기록한다.** 확정된 규칙마다 지원됨 · 적용 상황이 생김 · 실제로 그 분기를 지남을 자식 실행의 로그에서 판정한다 | D086에서 드러난 실패(재연락을 켰는데 로그가 부모와 같았다)를 화면이 "효과 없음"으로 읽게 두면 디자이너가 틀린 결론을 낸다. 사건이 같다는 것만으로 죽은 기능이라고 판정하지 않는다 | `rule_application.py`; 비교 표의 '바뀐 규칙이 실행됐나' 행 · 판정할 수 없으면 `unknown`이고 추측하지 않음 |
| D097 | **현장 기록은 순서를 강제한다.** 공개 전 독립 응답 → 공개 기록 → 공개 후 비교. 응답자(가명)·응답자 역할·대상 주민을 분리하고, 명시적 `disagreement`와 정정 대상을 적는다 | 기존 폼은 공개 전/후를 사용자가 dropdown으로 고르고 일치 값에 '충돌'이 없었다. 가족의 답과 본인의 답, 연구자 메모와 주민 응답이 한 표에 섞일 수 있었다 | 공개 전 응답 없이 공개 기록 불가 · 공개 기록 없이 비교 응답 불가 · 공개 전 응답에 correspondence 금지 · `responseKind`로 연구자 메모 분리 |
| D098 | **종합이 제외한 주장은 원본으로 되돌아갈 수 있어야 한다.** 제외 주장마다 평가 항목 참조, 기계 검사 결과, 의미 판정(기본 `unreviewed`), 라벨 주체를 따로 둔다 | 8.4에서 종합이 근거 있는 주장 2건을 '근거 없음'으로 걸렀고, 어느 리뷰도 하지 않은 주장을 스스로 만들어 스스로 걸렀다. 문자열 목록으로는 둘 다 보이지 않는다 | `ExcludedClaim`; 참조 없는 주장은 '종합이 스스로 만든 주장'으로 표시 · 모델이 자기 출력에 단 라벨은 `ai_draft`이고 사람 판정으로 승격되지 않음 |
| D099 | **새 실행을 만드는 경로는 하나다.** 수동 A/B·독립 정책 편집기·'이 발견으로 새 revision'을 기본 화면에서 제거한다. RQ2의 비교 조건은 같은 자료를 읽기 전용으로 다르게 제공하는 것으로 구현한다 | 세 경로가 각각 revision과 attempt를 만들 수 있었고, 어느 경로로 생긴 실행인지가 화면에서 구분되지 않았다. '두 방법 비교'는 옛 기능의 유무 비교가 아니다 | 기록된 발견과 과거 시도는 고급 영역에서 읽기 전용 · `PolicyEditor`·`FindingPanel` 삭제 · 서버의 rerun/fork/findings 엔드포인트는 감사용으로 보존 |
| D100 | **화면의 기본값은 판단에 필요한 것만.** 각 수치·항목의 단서 조항(무엇이 아닌지, 무엇과 무엇을 섞어 세는지, 빈칸이 '없음'이 아니라는 것)은 지우지 않고 그 대상 옆의 `?`(hint)나 접기로 옮긴다. 막는 문제·상태·실패 이유는 접지 않는다 | 연구자·정책 결정자가 화면을 열면 무엇을 먼저 봐야 할지 알 수 없었다. 비교 표는 칸마다 각주가 붙어 숫자보다 각주가 길었고, 개선 화면은 저장 버튼 앞에 빈 서술 상자가 네 개였다. 문장을 지우면 화면이 거짓말을 하게 되므로 같은 문장을 한 번 클릭 뒤로 옮겼다 | `Hint`(primitives) · 표의 각주는 행 이름에 한 번 · 선택 입력은 `Disclosure` · 검사는 힌트를 열어 문장을 확인한다 (vitest `hint()`, Playwright `hint()`) |
| D101 | 한 줄에 들어가야 하는 것은 줄이지 않고 **접거나 줄임표로** 말한다: 이름은 폭을 지키고 부가 라벨이 먼저 줄고, 44px 진행 막대는 좁아지면 현재 단계만 남기며, 지도 헤더의 버튼은 줄바꿈한다 | 브라우저에서 '보건소 담당자'가 'P..'로, '하루 실행'이 '하루'로, '주민 평가 읽기'가 세로 한 글자씩으로 잘려 있었다. flex가 기본값으로 모든 것을 같이 줄인 결과다 | 800·1366·1440에서 가로 넘침 0 (Playwright) · 사람 목록의 이름은 잘리지 않고 시나리오 라벨만 줄임표 |
| D102 | **마을의 하루는 05:00–22:00이고 22:00에 주민이 하루를 평가한다.** 재생 막대는 사건 번호가 아니라 시각이며, 원본 디오라마와 같은 네 눈금(05·12·17·22)을 둔다. 재생 줄에는 재생·시계·시각·⚙만 남기고 배속·한 사건 이동·MEDial 관점·새 사례는 ⚙ 뒤로 옮긴다. 세 화면 이름은 ①→②→③ ↺ 한 바퀴로 그리고, 지도 위 요청 경로에서 마을 밖 구간은 그리지 않는다 | 실행 지평이 20:00이라 P6·P12의 21:00 귀가가 잘렸고, 사건 번호 막대는 아무 일도 없는 시간을 건너뛰어 하루가 흐르는 느낌이 없었다. 지도 헤더의 '새 사례 준비'·'연구자 전체보기'는 관찰과 무관한 노브였고, 세 화면 이름은 탭처럼 보여 순환이 읽히지 않았다. 마을 밖(남해읍) 이동의 직선 폴리라인이 지도를 가로지르는 초록 막대로 그려졌다 | deck `HORIZON_MS = 22h`, `world.day_started`는 05:00 · 세 열 폭 456/456/456 (Playwright) · 사람 목록·조율 문장에 `PATROL`·`FARM` 키 0건 · 헤더 태그는 원자료일 때 '실제 남해군 은점마을 주민 12명' |
| D103 | **운전 여부는 공동체 진술로 채운다.** 장부(`ledger`)에 `mobility` 진술(누가 운전하는가, provenance `researcher-statement`, 이유)을 두고, 인터뷰가 운전 여부를 말하지 않은 사람에게만 페르소나 위에 덧씌운다. 인터뷰가 '운전 안 함'이라 한 사람은 그대로다. 덧씌운 값마다 장부를 가리키는 `assumption` 근거 카드가 붙는다 | 은점마을 인터뷰는 운전을 묻지 않았고, 컴파일러는 모르는 것을 모른다고만 적었다. 그 결과 동승 요청은 11명 전원이 `driving_status_unknown`으로 거절했다 — 차 없이는 다니기 어려운 마을에서. 현장을 아는 연구자의 진술을 컴파일러가 지어내는 것과 구분해 기록할 자리가 없었다 | `apply_community_facts`; 장부는 입력 해시에 들어가므로 진술을 바꾸면 다른 입력이다 · 동승 덱: P2가 P9를, P10이 P11을 태움 · 합성 마을은 진술이 없어 종전대로 거절 · 좌석 수는 여전히 원자료에 없음 |
| D104 | **v0의 사례는 원자료의 하루다.** `deck-eunjeom-day-v1`이 Q1(08:00 P9 읍내)·Q3(09:30 P1 무응답)·Q2(10:40 P11 약)·Q5(귀가편)를 원자료 시각에 두고, 준비 화면의 기본 선택이 된다. 단일 사건 덱은 좁은 질문용으로 남는다. 한 덱이 여러 퀘스트를 가질 수 있다(`DECK_QUEST_SETS`). **Q4(20:30 위급)는 넣지 않았다** — 엔진에 119 접수·출동·인계 흐름이 없어, 아무도 처리할 수 없는 사건을 로그가 조용히 떨어뜨리게 된다 | v0가 09:30 사건 하나와 열두 시간의 정적이었다. 연구자: "v0는 5퀘스트를 먼저 보여줬으면". Q2는 심부름인데 엔진에 심부름 흐름이 없어 '읍내에 가야 한다'로 변환했고 덱 assumptions에 적었다 | 하루 실행 48 사건: P2가 P9를 태우고(08:04 출발, 13:29 귀가), 이장이 P1을 밭에서 확인(09:40), P10이 P11을 태움(10:41) · pytest 418 · e2e 6 · Q4는 다음 결정(위급 흐름) |
| D105 | **보건소와 119는 에이전트다.** `Attempt.institutionAdapter`(rule/llm)를 주민 어댑터와 따로 둔다. **119 상황실**(`EMS_DISPATCH`)은 위급 신고를 신고자의 말 그대로 받아 출동(accept)·보류(defer)·거절(decline)만 답하고, 구급대 도착 시각은 자원 가정(`emsResponseMinutes`, 원자료 Q4의 22분)이다. **보건소**(`HC_NURSE`)는 종전의 인계 접수에 더해 17:00(`dailyReportAtMs`)에 MEDial의 하루 보고를 받고 주민마다 none·note·followup_call·home_visit 중 하나를 정한다; 전화 확인은 그날 근무시간 안에서 실행하고 방문은 계획으로만 남긴다. 모델 버전은 head 계층(gemini-3.8-flash), 프롬프트 `institution-prompt-v1` | 연구자: "보건소와 119 에이전트도 있어야 할듯. gemini-3.8-flash로. 보건소는 주기적으로 보고받고 조치, 119는 긴급 출동." 이전에는 119가 없어 Q4를 돌릴 수 없었고, 보건소는 인계된 건만 다루는 대기열이었다 | 하루 덱: Q4 20:30 신고 → 119 접수 → 20:52 인계, P7은 걸어가 곁에서 대기 · 17:00 보고 12명 → 조치 12건, 실제 Gemini 2회 호출 확인 · 모델 답이 스키마 밖이면 어댑터 실패이지 기관의 거절이 아님 · 보고서에 없는 주민에 대한 조치는 거부 · `test_institutions.py` 10건 |

## 다음 결정 템플릿

- 날짜 / ID:
- 해결하려는 문제:
- 선택한 설계:
- 검토한 대안:
- 사용한 근거:
- 아직 확인하지 못한 가정:
- 바뀌는 코드·계약:
- 확인할 사건·테스트:
- 채택/수정/폐기 결과:
